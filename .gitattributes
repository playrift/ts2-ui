# ts2 is TypeScript with JSX, but Linguist has no entry for the `.ts2`
# extension, so GitHub renders these files as plain text by default.
#
# TSX is the closest built-in language and a very good fit: its grammar scope is
# `source.tsx`, which is what ts2's own editor tooling extends.
*.ts2 linguist-language=TSX
