import { createRef, useInterval } from 'ts2';

import { Anchor, overlayRelayout } from '../lib/lib.overlay';
import { GuideAnim, GuideModel, GuideOverlay, Layout } from './guide-eggs-overlay.constants';

const timerHost = createRef('0000_100guide_eggs_overlay_timer_host');

const content = createRef('100guide_eggs_overlay_content');

export function GuideEggsOverlay(): Component {
  useInterval(() => overlayRelayout(timerHost, content, GuideOverlay.DODGE, Anchor.RIGHT, Anchor.BOTTOM, 0), timerHost);

  return (
    <layer w="fill" h="fill">
      <layer w="fill" h="fill" ref={timerHost}>
        <layer ref={content} x="right" y="bottom" w={Layout.CONTENT_W} h={Layout.CONTENT_H}>
          <layer hide x="right" y={{ bottom: Layout.LAYER_BOTTOM }} w={Layout.LAYER_W} h={Layout.Q_LAYER_H}>
            <model
              x={92}
              y={230}
              w={Layout.MODEL_W}
              h={Layout.MODEL_H}
              model={GuideModel.QUESTION}
              anim={GuideAnim.QUESTION}
              zoom={756}
            />
          </layer>

          <layer x="right" y={{ bottom: Layout.LAYER_BOTTOM }} w={Layout.LAYER_W} h={334}>
            <model
              x={92}
              y={238}
              w={Layout.MODEL_W}
              h={Layout.MODEL_H}
              model={GuideModel.EGG}
              anim={GuideAnim.EGG}
              zoom={934}
            />
          </layer>

          <layer hide x="right" y={{ bottom: Layout.LAYER_BOTTOM }} w={Layout.LAYER_W} h={Layout.Q_LAYER_H}>
            <model
              x={49}
              y={230}
              w={Layout.MODEL_W}
              h={Layout.MODEL_H}
              model={GuideModel.QUESTION}
              anim={GuideAnim.QUESTION}
              zoom={756}
            />
          </layer>

          <layer x="right" y={{ bottom: Layout.LAYER_BOTTOM }} w={Layout.LAYER_W} h={338}>
            <model
              x={49}
              y={242}
              w={Layout.MODEL_W}
              h={Layout.MODEL_H}
              model={GuideModel.EGG}
              anim={GuideAnim.EGG}
              zoom={934}
            />
          </layer>

          <layer hide x="right" y={{ bottom: Layout.LAYER_BOTTOM }} w={Layout.LAYER_W} h={Layout.Q_LAYER_H}>
            <model
              x={110}
              y={260}
              w={Layout.MODEL_W}
              h={Layout.MODEL_H}
              model={GuideModel.QUESTION}
              anim={GuideAnim.QUESTION}
              zoom={756}
            />
          </layer>

          <layer x="right" y={{ bottom: Layout.LAYER_BOTTOM }} w={Layout.LAYER_W} h={330}>
            <model
              x={110}
              y={264}
              w={Layout.MODEL_W}
              h={Layout.MODEL_H}
              model={GuideModel.EGG}
              anim={GuideAnim.EGG}
              zoom={756}
            />
          </layer>

          <layer hide x="right" y={{ bottom: Layout.LAYER_BOTTOM }} w={Layout.LAYER_W} h={Layout.Q_LAYER_H}>
            <model
              x={31}
              y={259}
              w={Layout.MODEL_W}
              h={Layout.MODEL_H}
              model={GuideModel.QUESTION}
              anim={GuideAnim.QUESTION}
              zoom={756}
            />
          </layer>

          <layer x={{ right: 1 }} y={{ bottom: Layout.LAYER_BOTTOM }} w={Layout.LAYER_W} h={329}>
            <model
              x={31}
              y={262}
              w={Layout.MODEL_W}
              h={Layout.MODEL_H}
              model={GuideModel.EGG}
              anim={GuideAnim.EGG}
              zoom={756}
            />
          </layer>

          <layer hide x="right" y={{ bottom: Layout.LAYER_BOTTOM }} w={Layout.LAYER_W} h={Layout.Q_LAYER_H}>
            <model
              x={73}
              y={284}
              w={Layout.MODEL_W}
              h={Layout.MODEL_H}
              model={GuideModel.QUESTION}
              anim={GuideAnim.QUESTION}
              zoom={656}
            />
          </layer>

          <layer x="right" y={{ bottom: Layout.LAYER_BOTTOM }} w={Layout.LAYER_W} h={331}>
            <model
              x={73}
              y={289}
              w={Layout.MODEL_W}
              h={Layout.MODEL_H}
              model={GuideModel.EGG}
              anim={GuideAnim.EGG}
              zoom={656}
            />
          </layer>
        </layer>
      </layer>
    </layer>
  );
}
