import { createRef } from 'ts2';
import { GuideFlourOverlay } from './guide-flour-overlay.component';

const guideFlourOverlayRef = createRef('100guide_flour_overlay');

export const guideFlourOverlayUi = (): UI => (
  <ui ref={guideFlourOverlayRef}>
    <GuideFlourOverlay />
  </ui>
);
