import { createRef } from 'ts2';
import { CorpBeast_ } from './corp-beast.component';

const corpBeastRef = createRef('corp_beast');

export const corpBeastUi = (): UI => (
  <ui ref={corpBeastRef}>
    <CorpBeast_ />
  </ui>
);
