import { createRef } from 'ts2';
import { BankpinSettings } from './bankpin-settings.component';
import { setMobileFullscreen } from '../lib/lib.toplevel';

const bankpinSettingsRef = createRef('bankpin_settings');

export const bankpinSettingsUi = (): UI => (
  <ui ref={bankpinSettingsRef}>
    <BankpinSettings />
    <layer x={0} y={0} w={0} h={0} onLoad={() => setMobileFullscreen(1)} />
  </ui>
);
