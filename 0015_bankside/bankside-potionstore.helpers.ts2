import { useServerState } from 'ts2';
import { Varps } from '../lib/game/varps';

export function potionStoreDoses(family: Int): Int {
  if (family === 0) {
    return useServerState(Varps.POTIONSTORE_STRENGTH_POTION);
  }
  if (family === 1) {
    return useServerState(Varps.POTIONSTORE_ATTACK_POTION);
  }
  if (family === 2) {
    return useServerState(Varps.POTIONSTORE_RESTORE_POTION);
  }
  if (family === 3) {
    return useServerState(Varps.POTIONSTORE_DEFENCE_POTION);
  }
  if (family === 4) {
    return useServerState(Varps.POTIONSTORE_PRAYER_POTION);
  }
  if (family === 5) {
    return useServerState(Varps.POTIONSTORE_SUPER_ATTACK_POTION);
  }
  if (family === 6) {
    return useServerState(Varps.POTIONSTORE_FISHING_POTION);
  }
  if (family === 7) {
    return useServerState(Varps.POTIONSTORE_SUPER_STRENGTH_POTION);
  }
  if (family === 8) {
    return useServerState(Varps.POTIONSTORE_SUPER_DEFENCE_POTION);
  }
  if (family === 9) {
    return useServerState(Varps.POTIONSTORE_RANGING_POTION);
  }
  if (family === 10) {
    return useServerState(Varps.POTIONSTORE_ANTIPOISON);
  }
  if (family === 11) {
    return useServerState(Varps.POTIONSTORE_SUPER_ANTIPOISON_POTION);
  }
  if (family === 12) {
    return useServerState(Varps.POTIONSTORE_ZAMORAK_BREW_POTION);
  }
  if (family === 13) {
    return useServerState(Varps.POTIONSTORE_SARADOMINBREW_POTION);
  }
  if (family === 14) {
    return useServerState(Varps.POTIONSTORE_ANTIFIRE_POTION);
  }
  if (family === 15) {
    return useServerState(Varps.POTIONSTORE_ENERGY_POTION);
  }
  if (family === 16) {
    return useServerState(Varps.POTIONSTORE_SUPER_ENERGY_POTION);
  }
  if (family === 17) {
    return useServerState(Varps.POTIONSTORE_SUPER_RESTORE_POTION);
  }
  if (family === 18) {
    return useServerState(Varps.POTIONSTORE_AGILITY_POTION);
  }
  if (family === 19) {
    return useServerState(Varps.POTIONSTORE_MAGIC_POTION);
  }
  if (family === 20) {
    return useServerState(Varps.POTIONSTORE_HUNTER_POTION);
  }
  if (family === 21) {
    return useServerState(Varps.POTIONSTORE_COMBAT_POTION);
  }
  if (family === 22) {
    return useServerState(Varps.POTIONSTORE_ANTIDOTE_P);
  }
  if (family === 23) {
    return useServerState(Varps.POTIONSTORE_ANTIDOTE_PP);
  }
  if (family === 24) {
    return useServerState(Varps.POTIONSTORE_RELICYMSBALM_POTION);
  }
  if (family === 25) {
    return useServerState(Varps.POTIONSTORE_SERUM207_POTION);
  }
  if (family === 29) {
    return useServerState(Varps.POTIONSTORE_MAGICESSENCE_POTION);
  }
  if (family === 30) {
    return useServerState(Varps.POTIONSTORE_EXTENDED_ANTIFIRE_POTION);
  }
  if (family === 31) {
    return useServerState(Varps.POTIONSTORE_SANFEWSERUM_POTION);
  }
  if (family === 32) {
    return useServerState(Varps.POTIONSTORE_STAMINA_POTION);
  }
  if (family === 33) {
    return useServerState(Varps.POTIONSTORE_SUPER_COMBAT_POTION);
  }
  if (family === 38) {
    return useServerState(Varps.POTIONSTORE_GUTHIX_BALANCE_POTION);
  }
  if (family === 40) {
    return useServerState(Varps.POTIONSTORE_COMPOST_POTION);
  }
  if (family === 41) {
    return useServerState(Varps.POTIONSTORE_ANTI_VENOM);
  }
  if (family === 42) {
    return useServerState(Varps.POTIONSTORE_ANTI_VENOM_P);
  }
  if (family === 75) {
    return useServerState(Varps.POTIONSTORE_SUPER_ANTIFIRE_POTION);
  }
  if (family === 76) {
    return useServerState(Varps.POTIONSTORE_EXTENDED_SUPER_ANTIFIRE_POTION);
  }
  if (family === 77) {
    return useServerState(Varps.POTIONSTORE_BATTLEMAGE_POTION);
  }
  if (family === 78) {
    return useServerState(Varps.POTIONSTORE_BASTION_POTION);
  }
  if (family === 80) {
    return useServerState(Varps.POTIONSTORE_DIVINE_SUPER_COMBAT_POTION);
  }
  if (family === 81) {
    return useServerState(Varps.POTIONSTORE_DIVINE_SUPER_ATTACK_POTION);
  }
  if (family === 82) {
    return useServerState(Varps.POTIONSTORE_DIVINE_SUPER_STRENGTH_POTION);
  }
  if (family === 83) {
    return useServerState(Varps.POTIONSTORE_DIVINE_SUPER_DEFENCE_POTION);
  }
  if (family === 84) {
    return useServerState(Varps.POTIONSTORE_DIVINE_RANGING_POTION);
  }
  if (family === 85) {
    return useServerState(Varps.POTIONSTORE_DIVINE_MAGIC_POTION);
  }
  if (family === 86) {
    return useServerState(Varps.POTIONSTORE_BLIGHTED_2RESTORE_POTION);
  }
  if (family === 87) {
    return useServerState(Varps.POTIONSTORE_DIVINE_BATTLEMAGE_POTION);
  }
  if (family === 88) {
    return useServerState(Varps.POTIONSTORE_DIVINE_BASTION_POTION);
  }
  if (family === 91) {
    return useServerState(Varps.POTIONSTORE_ANCIENT_BREW);
  }
  if (family === 95) {
    return useServerState(Varps.POTIONSTORE_MENAPHITEREMEDY_POTION);
  }
  if (family === 96) {
    return useServerState(Varps.POTIONSTORE_FORGOTTENBREW_POTION);
  }
  if (family === 99) {
    return useServerState(Varps.POTIONSTORE_EXTENDED_ANTIVENOMPLUS_POTION);
  }
  if (family === 100) {
    return useServerState(Varps.POTIONSTORE_PRAYER_REGENERATION_POTION);
  }
  if (family === 101) {
    return useServerState(Varps.POTIONSTORE_GOADING_POTION);
  }
  if (family === 103) {
    return useServerState(Varps.POTIONSTORE_SURGE_POTION);
  }
  if (family === 105) {
    return useServerState(Varps.POTIONSTORE_SUPER_FISHING_POTION);
  }
  if (family === 106) {
    return useServerState(Varps.POTIONSTORE_EXTREME_ENERGY_POTION);
  }
  if (family === 107) {
    return useServerState(Varps.POTIONSTORE_SUPER_HUNTER_POTION);
  }
  if (family === 108) {
    return useServerState(Varps.POTIONSTORE_EXTENDED_STAMINA_POTION);
  }
  if (family === 109) {
    return useServerState(Varps.POTIONSTORE_ARMADYLBREW_POTION);
  }
  if (family === 110) {
    return useServerState(Varps.POTIONSTORE_ATTACK_MIX);
  }
  if (family === 111) {
    return useServerState(Varps.POTIONSTORE_ANTIPOISON_MIX);
  }
  if (family === 112) {
    return useServerState(Varps.POTIONSTORE_STRENGTH_MIX);
  }
  if (family === 113) {
    return useServerState(Varps.POTIONSTORE_COMBAT_MIX);
  }
  if (family === 114) {
    return useServerState(Varps.POTIONSTORE_RESTORE_MIX);
  }
  if (family === 115) {
    return useServerState(Varps.POTIONSTORE_ENERGY_MIX);
  }
  if (family === 116) {
    return useServerState(Varps.POTIONSTORE_DEFENCE_MIX);
  }
  if (family === 117) {
    return useServerState(Varps.POTIONSTORE_AGILITY_MIX);
  }
  if (family === 118) {
    return useServerState(Varps.POTIONSTORE_PRAYER_MIX);
  }
  if (family === 119) {
    return useServerState(Varps.POTIONSTORE_SUPERATTACK_MIX);
  }
  if (family === 120) {
    return useServerState(Varps.POTIONSTORE_ANTIPOISON_SUPER_MIX);
  }
  if (family === 121) {
    return useServerState(Varps.POTIONSTORE_FISHING_MIX);
  }
  if (family === 122) {
    return useServerState(Varps.POTIONSTORE_SUPER_ENERGY_MIX);
  }
  if (family === 123) {
    return useServerState(Varps.POTIONSTORE_SUPER_STRENGTH_MIX);
  }
  if (family === 124) {
    return useServerState(Varps.POTIONSTORE_MAGIC_ESSENCE_MIX);
  }
  if (family === 125) {
    return useServerState(Varps.POTIONSTORE_SUPER_RESTORE_MIX);
  }
  if (family === 126) {
    return useServerState(Varps.POTIONSTORE_SUPER_DEFENCE_MIX);
  }
  if (family === 127) {
    return useServerState(Varps.POTIONSTORE_ANTIFIRE_MIX);
  }
  if (family === 128) {
    return useServerState(Varps.POTIONSTORE_RANGING_MIX);
  }
  if (family === 129) {
    return useServerState(Varps.POTIONSTORE_MAGIC_MIX);
  }
  if (family === 130) {
    return useServerState(Varps.POTIONSTORE_HUNTING_MIX);
  }
  if (family === 131) {
    return useServerState(Varps.POTIONSTORE_ZAMORAK_MIX);
  }
  if (family === 132) {
    return useServerState(Varps.POTIONSTORE_EXTENDED_ANTIFIRE_MIX);
  }
  if (family === 133) {
    return useServerState(Varps.POTIONSTORE_STAMINA_MIX);
  }
  if (family === 134) {
    return useServerState(Varps.POTIONSTORE_RELICYMS_MIX);
  }
  if (family === 135) {
    return useServerState(Varps.POTIONSTORE_ANTIDOTE_P_MIX);
  }
  if (family === 136) {
    return useServerState(Varps.POTIONSTORE_SUPER_ANTIFIRE_MIX);
  }
  if (family === 137) {
    return useServerState(Varps.POTIONSTORE_EXTENDED_SUPER_ANTIFIRE_MIX);
  }
  if (family === 138) {
    return useServerState(Varps.POTIONSTORE_ANCIENT_MIX);
  }
  return -1;
}
