import { Telenexus } from './telenexus.component';
import { Layout, telenexusRef } from './telenexus.constants';

export const telenexusUi = (): UI => (
  <ui ref={telenexusRef} x="center" y="center" w={{ fill: Layout.ROOT_INSET }} h={{ fill: Layout.ROOT_INSET }}>
    <Telenexus />
  </ui>
);
