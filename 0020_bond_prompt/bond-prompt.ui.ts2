import { createRef } from 'ts2';
import { BondPrompt } from './bond-prompt.component';

const bondPromptRef = createRef('bond_prompt');

export const bondPromptUi = (): UI => (
  <ui ref={bondPromptRef}>
    <BondPrompt />
  </ui>
);
