import { createRef } from 'ts2';
import { Killbot } from './killbot.component';

const killbotRef = createRef('killbot');

export const killbotUi = (): UI => (
  <ui ref={killbotRef}>
    <Killbot />
  </ui>
);
