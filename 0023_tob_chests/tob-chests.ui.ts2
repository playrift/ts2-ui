import { createRef } from 'ts2';
import { TobChests } from './tob-chests.component';

const tobChestsRef = createRef('tob_chests');

export const tobChestsUi = (): UI => (
  <ui ref={tobChestsRef} w={240} h={220} x="center" y="center">
    <TobChests />
  </ui>
);
