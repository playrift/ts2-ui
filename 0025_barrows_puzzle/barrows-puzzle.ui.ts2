import { createRef } from 'ts2';
import { BarrowsPuzzle } from './barrows-puzzle.component';

const barrowsPuzzleRef = createRef('barrows_puzzle');

export const barrowsPuzzleUi = (): UI => (
  <ui ref={barrowsPuzzleRef}>
    <BarrowsPuzzle />
  </ui>
);
