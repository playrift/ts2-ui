import { createRef } from 'ts2';
import { PirateCombilock } from './pirate-combilock.component';
import { Layout } from './pirate-combilock.constants';

const pirateCombilockRef = createRef('pirate_combilock');

export const pirateCombilockUi = (): UI => (
  <ui ref={pirateCombilockRef} w={Layout.STAGE_W} h={Layout.STAGE_H} x="center" y="center">
    <PirateCombilock />
  </ui>
);
