import { createRef } from 'ts2';
import { Pillory } from './pillory.component';
import { Layout } from './pillory.constants';

const pilloryRef = createRef('pillory');

export const pilloryUi = (): UI => (
  <ui ref={pilloryRef} w={Layout.panel.w} h={Layout.panel.h} x="center" y="center">
    <Pillory />
  </ui>
);
