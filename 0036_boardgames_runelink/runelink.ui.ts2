import { createRef } from 'ts2';
import { Runelink } from './runelink.component';

const runelinkRef = createRef('boardgames_runelink');

export const runelinkUi = (): UI => (
  <ui ref={runelinkRef}>
    <Runelink />
  </ui>
);
