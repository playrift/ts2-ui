import { createRef } from 'ts2';
import { BoardgamesOptions } from '../0033_boardgames_draughts_options/boardgames-options.component';

const runelinkOptionsRef = createRef('boardgames_runelink_options');

export const runelinkOptionsUi = (): UI => (
  <ui ref={runelinkOptionsRef} x="center" y="center">
    <BoardgamesOptions title="Runelink: Options" subjects />
  </ui>
);
