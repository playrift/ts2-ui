import { useServerState } from 'ts2';

import { RunelinkOverlayVarps } from './runelink-overlay.constants';

export function runelinkRank(label: ComponentId): void {
  label.text = `Rank: ${useServerState(RunelinkOverlayVarps.RANK)}`;
}
