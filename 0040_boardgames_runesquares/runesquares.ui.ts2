import { createRef } from 'ts2';
import { Runesquares } from './runesquares.component';

const runesquaresRef = createRef('boardgames_runesquares');

export const runesquaresUi = (): UI => (
  <ui ref={runesquaresRef}>
    <Runesquares />
  </ui>
);
