import { createRef } from 'ts2';
import { StarTelescopeMeteor } from './star-telescope-meteor.component';

const starTelescopeMeteorRef = createRef('star_telescope_meteor');

export const starTelescopeMeteorUi = (): UI => (
  <ui ref={starTelescopeMeteorRef}>
    <StarTelescopeMeteor />
  </ui>
);
