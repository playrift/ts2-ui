import { createRef } from 'ts2';
import { Runeversi } from './runeversi.component';

const runeversiRef = createRef('boardgames_runeversi');

export const runeversiUi = (): UI => (
  <ui ref={runeversiRef}>
    <Runeversi />
  </ui>
);
