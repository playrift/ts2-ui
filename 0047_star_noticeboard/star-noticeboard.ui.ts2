import { createRef } from 'ts2';
import { StarNoticeboard } from './star-noticeboard.component';

const starNoticeboardRef = createRef('star_noticeboard');

export const starNoticeboardUi = (): UI => (
  <ui ref={starNoticeboardRef} x="center" y="center">
    <StarNoticeboard />
  </ui>
);
