import { useServerState } from 'ts2';
import { closeInterface } from '../lib/lib.helpers';
import { CuisineCell } from './cuisine-cell.component';
import { cuisineCheckInv } from './aluft-cooking.helpers';
import { GnomeVarbits, Recipes } from './aluft-cooking.constants';

export function CuisineGrid(): Component {
  const type: Int = useServerState(GnomeVarbits.COOKING_TYPE);

  if (type > 3) {
    closeInterface();
    return null;
  }

  return (
    <layer w="fill" h="fill">
      {Recipes.filter((row) => row.cuisine === type).map((row, index) => (
        <CuisineCell
          index={index}
          name={row.name}
          cuisineId={row.cuisineId}
          product={row.product}
          locked={row.lockedProduct}
          canMake={cuisineCheckInv(row)}
        />
      ))}
    </layer>
  );
}
