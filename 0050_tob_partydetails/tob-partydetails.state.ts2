import { sessionState } from 'ts2';

import { AutoRefresh } from './tob-partydetails.constants';

export const boardBuilt = sessionState(0);

export const tipIndex = sessionState(-1);

export const autorefreshStart = sessionState(-1);

export const dialRemaining = sessionState(AutoRefresh.CYCLES);

export const arrowsPlaced = sessionState(0);

export const partyTitled = sessionState(0);
