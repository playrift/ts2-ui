import { createRef } from 'ts2';
import { BroochCloseup } from './brooch-closeup.component';

const broochCloseupRef = createRef('brooch_closeup');

export const broochCloseupUi = (): UI => (
  <ui ref={broochCloseupRef}>
    <BroochCloseup />
  </ui>
);
