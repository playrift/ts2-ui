import { createRef } from 'ts2';

import { PohBoard } from './poh-board.component';

const pohBoardRef = createRef('poh_board');

export const pohBoardUi = (): UI => (
  <ui ref={pohBoardRef} x="center" y="center">
    <PohBoard />
  </ui>
);
