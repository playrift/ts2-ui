import { createRef } from 'ts2';
import { BurghMap } from './burgh-map.component';

const burghMapRef = createRef('burgh_map');

export const burghMapUi = (): UI => (
  <ui ref={burghMapRef}>
    <BurghMap />
  </ui>
);
