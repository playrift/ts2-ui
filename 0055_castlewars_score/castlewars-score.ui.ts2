import { createRef } from 'ts2';
import { CastlewarsScore } from './castlewars-score.component';

const castlewarsScoreRef = createRef('castlewars_score');

export const castlewarsScoreUi = (): UI => (
  <ui ref={castlewarsScoreRef}>
    <CastlewarsScore />
  </ui>
);
