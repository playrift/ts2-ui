import { createRef } from 'ts2';

const castlewarsShopside = createRef('castlewars_shopside');

export function CastlewarsShopside(): Component {
  return (
    <layer x={0} y={0} w={190} h={261}>
      <layer x={{ center: 2 }} y={{ center: 3 }} w={162} h={250} />
    </layer>
  );
}

export const castlewarsShopsideUi = (): UI => (
  <ui ref={castlewarsShopside}>
    <CastlewarsShopside />
  </ui>
);
