import { createRef } from 'ts2';
import { CanoeStationsMap } from './canoe-stations-map.component';

const canoeStationsMapRef = createRef('canoe_stations_map');

export const canoeStationsMapUi = (): UI => (
  <ui ref={canoeStationsMapRef}>
    <CanoeStationsMap />
  </ui>
);
