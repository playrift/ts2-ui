import { createRef } from 'ts2';
import { ChatBoth } from './chat-both.component';

const chatBothRef = createRef('chat_both');

export const chatBothUi = (): UI => (
  <ui ref={chatBothRef}>
    <ChatBoth />
  </ui>
);
