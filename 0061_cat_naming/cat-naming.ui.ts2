import { createRef } from 'ts2';
import { CatNaming } from './cat-naming.component';

const catNamingRef = createRef('cat_naming');

export const catNamingUi = (): UI => (
  <ui ref={catNamingRef}>
    <CatNaming />
  </ui>
);
