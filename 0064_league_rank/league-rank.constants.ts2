import { Enums } from '../lib/game/enums';
import { Fonts } from '../lib/game/fonts';
import { Sounds as GameSounds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';
import { Varbits } from '../lib/game/varbits';
import { Varps } from '../lib/game/varps';

export declare const RankVarbits: {
  readonly LEAGUE: typeof Varbits.LEAGUE_TYPE;
};

export declare const RankVarps: {
  readonly POINTS: typeof Varps.LEAGUE_POINTS_COMPLETED;
};

export enum League {
  ONE = 1,
  TWO = 2,
  THREE = 3,
  FOUR = 4,
  FIVE = 5,
  SIX = 6,
}

export declare const RankNames: typeof Enums.ENUM_5201;

export declare const RankSprites: {
  readonly DIVIDER: typeof Sprites.STEELBORDER_DIVIDER_0;
  readonly FLASH_ARROW: typeof Sprites.CLOSE_ARROWS_0;
};

export declare const RankFonts: {
  readonly MEASURE: typeof Fonts.P11_FULL;
};

export declare const RankSounds: {
  readonly OPEN: typeof GameSounds.INTERFACE_SELECT1;
};

export enum RankColors {
  BAR_OUTLINE = 0x474745,
  BAR_EDGE = 0x0e0e0c,
  PASSED = 0xbbbbbb,
  CURRENT = 0x0dc10d,
  LOCKED = 0x808080,
  ROW_BACK = 0x000000,
  TITLE_FALLBACK = 0xffffff,
}

export enum RankTransparency {
  EVEN = 220,
  ODD = 200,
}

export const RankLayout = {
  BAR_W: 282,
  BAR_SPAN: 278,
  BAR_OUTLINE_H: 29,
  BAR_TRACK_H: 27,
  MARKER_W: 10,
  MARKER_H: 31,
  EDGE_OUTER_H: 33,
  EDGE_INNER_H: 31,
  ROW_H: 32,
  ICON_X: 5,
  ICON_W: 36,
  ICON_H: 32,
  LABEL_X: 46,
  LABEL_INSET: 51,
  ARROW_X: 56,
  ARROW_W: 26,
  ARROW_H: 26,
  MEASURE_W: 500,
};

export const RankRows = [0, 1, 2, 3, 4, 5, 6];

export const Flash = {
  STEPS: 25,
  MAX: 255,
};
