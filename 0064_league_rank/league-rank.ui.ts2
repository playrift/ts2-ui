import { createRef } from 'ts2';
import { LeagueRank } from './league-rank.component';

const leagueRankRef = createRef('league_rank');

export const leagueRankUi = (): UI => (
  <ui ref={leagueRankRef}>
    <LeagueRank />
    <layer x="center" y="center" w="fill" h="fill" />
  </ui>
);
