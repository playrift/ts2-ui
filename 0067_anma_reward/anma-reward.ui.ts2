import { createRef } from 'ts2';
import { AnmaReward } from './anma-reward.component';
import { Layout } from './anma-reward.constants';

const anmaRewardRef = createRef('anma_reward');

export const anmaRewardUi = (): UI => (
  <ui ref={anmaRewardRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <AnmaReward />
  </ui>
);
