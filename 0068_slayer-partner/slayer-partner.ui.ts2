import { createRef } from 'ts2';
import { SlayerPartner } from './slayer-partner.component';

const slayerPartnerRef = createRef('slayer-partner');

export const slayerPartnerUi = (): UI => (
  <ui ref={slayerPartnerRef} w={300} h={270} x="center" y="center">
    <SlayerPartner />
  </ui>
);
