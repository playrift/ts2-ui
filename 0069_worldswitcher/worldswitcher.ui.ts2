import { WorldSwitcher } from './worldswitcher.component';
import { worldSwitcherRef } from './worldswitcher.constants';

export const worldSwitcherUi = (): UI => (
  <ui ref={worldSwitcherRef} w="fill" h="fill" x="center" y="center">
    <WorldSwitcher />
  </ui>
);
