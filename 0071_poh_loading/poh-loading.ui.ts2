import { createRef } from 'ts2';
import { PohLoading } from './poh-loading.component';

const pohLoadingRef = createRef('poh_loading');

export const pohLoadingUi = (): UI => (
  <ui ref={pohLoadingRef}>
    <PohLoading />
  </ui>
);
