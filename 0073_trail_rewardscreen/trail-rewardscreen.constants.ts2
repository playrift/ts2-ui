import { Inventories } from '../lib/game/inventories';
import { Sprites } from '../lib/game/sprites';

export declare const TrailReward: {
  readonly INV: typeof Inventories.TRAIL_REWARDINV;
};

export declare const TrailArt: {
  readonly CLOSE: typeof Sprites.CLOSE_BUTTONS_4;
  readonly CLOSE_HOVER: typeof Sprites.CLOSE_BUTTONS_5;
};

export enum TrailModels {
  CASKET = 3395,
  ORNAMENT = 1226,
}

export enum Panel {
  WIDTH = 512,
  HEIGHT = 334,
}

export enum Grid {
  BOX_WIDTH = 112,
  BOX_HEIGHT = 192,
  COLUMNS = 3,
  SLOT_WIDTH = 36,
  SLOT_HEIGHT = 32,
  ROW_PITCH = SLOT_HEIGHT + (BOX_WIDTH - SLOT_WIDTH * COLUMNS) / 2,
}

export enum Colors {
  SLOT_SHADOW = 0x333333,
}
