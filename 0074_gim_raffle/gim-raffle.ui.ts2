import { createRef } from 'ts2';
import { GimRaffle } from './gim-raffle.component';

const gimRaffleRef = createRef('gim_raffle');

export const gimRaffleUi = (): UI => (
  <ui ref={gimRaffleRef} x="center" y="center">
    <GimRaffle />
  </ui>
);
