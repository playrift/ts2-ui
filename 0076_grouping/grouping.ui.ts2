import { Grouping } from './grouping.component';
import { grouping } from './grouping.refs';
import { Layout } from './grouping.constants';

export const groupingUi = (): UI => (
  <ui ref={grouping} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <Grouping />
  </ui>
);
