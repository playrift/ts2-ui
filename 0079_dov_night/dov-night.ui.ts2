import { createRef } from 'ts2';
import { DovNight } from './dov-night.component';

const dovNightRef = createRef('dov_night');

export const dovNightUi = (): UI => (
  <ui ref={dovNightRef}>
    <DovNight />
  </ui>
);
