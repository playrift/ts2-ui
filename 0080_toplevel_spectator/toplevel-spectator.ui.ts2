import { ToplevelSpectator } from './toplevel-spectator.component';
import { controlRef } from './toplevel-spectator.constants';

export const toplevelSpectatorUi = (): UI => (
  <ui ref={controlRef} x="center" y="center" w="fill" h="fill">
    <ToplevelSpectator />
  </ui>
);
