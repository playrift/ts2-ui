export const enum CsvFonts {
  INSTRUCTIONS = 494,
  FIELD = 495,
}

export const enum CsvColors {
  ORANGE = 0xff981f,
  FIELD_BACK = 0x372e22,
  FIELD_TEXT = 0xffffff,
  SELECT = 0x000000,
  SELECT_BG = 0xffffff,
  FADER = 0x000000,
}

export const enum CsvLayout {
  CLAMP_INSET_X = 12,
  CLAMP_INSET_Y = 41,
  CLAMP_BOTTOM = 6,
  WINDOW_W = 400,
  WINDOW_H = 260,
  BODY_Y = 40,
  BODY_INSET_X = 20,
  BODY_INSET_Y = 80,
  INSTRUCTIONS_INSET = 8,
  INSTRUCTIONS_H = 54,
  TAB_INSET_Y = 54,
  FIELD_INSET = 10,
  FIELD_WRAP = 370,
  BUTTON_W = 80,
  BUTTON_H = 26,
  BUTTON_BOTTOM = 10,
  BUTTON_DX = 50,
}

export const enum CsvFade {
  CLEAR = 255,
  DIM = 150,
  IN_CYCLES = 30,
  OUT_CYCLES = 10,
}

export const CsvCopy = {
  ITEMS_INSTRUCTIONS:
    'Add to the ignore list by typing item names below. Include exact spaces within names and separate items with commas ( , ). Use stars ( * ) as a wildcard to match many items, e.g. *bone* matches Bones and Long Bone. Text can be pasted from the clipboard into the box.',
  SOURCES_INSTRUCTIONS:
    'Add to the ignore list by typing source names below. Include exact spaces within names and separate items with commas ( , ). Use stars ( * ) as a wildcard to match many sources, e.g. *goblin* matches Goblin and Cave goblin. Text can be pasted from the clipboard into the box.',
  CANCEL: '<col=ff981f>Cancel</col>',
  SAVE: '<col=0dc10d>Save</col>',
  TITLE_PREFIX: 'Text Input - ',
} as const;
