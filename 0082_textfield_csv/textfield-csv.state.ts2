import { sessionState } from 'ts2';

export const textfieldCsvOpens = sessionState(0);

export const textfieldCsvRowKey = sessionState(-1);

export const textfieldCsvInitialText = sessionState('');

export const textfieldCsvInstructions = sessionState('');

export const textfieldCsvClosed = sessionState(0);

export const textfieldCsvFadeDirection = sessionState(0);
export const textfieldCsvFadeStartedAt = sessionState(0);
