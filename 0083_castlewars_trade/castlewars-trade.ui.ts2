import { createRef } from 'ts2';
import { CastlewarsTrade } from './castlewars-trade.component';
import { Panel } from './castlewars-trade.constants';

const castlewarsTradeRef = createRef('castlewars_trade');

export const castlewarsTradeUi = (): UI => (
  <ui ref={castlewarsTradeRef} w={Panel.WIDTH} h={Panel.HEIGHT} x={Panel.X} y={Panel.Y}>
    <CastlewarsTrade />
  </ui>
);
