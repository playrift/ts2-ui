import { createRef } from 'ts2';
import { EquipmentStats } from './equipment.component';
import { EquipmentLayout } from './equipment.constants';

const equipmentRef = createRef('equipment');

export const equipmentUi = (): UI => (
  <ui ref={equipmentRef} w={EquipmentLayout.ROOT_W} h={EquipmentLayout.ROOT_H}>
    <EquipmentStats />
  </ui>
);
