import { sessionState } from 'ts2';

export const riskHidden = sessionState(1);

export const teleblockTipUp = sessionState(0);

export const broadcastText = sessionState('');

export const broadcastStartedAt = sessionState(-1);

export const lobbyCountdownFrom = sessionState(-1);

export const deadmanDeadline = sessionState(-1);

export const deadmanShownSeconds = sessionState(-1);

export const fogColour = sessionState(0xff7733);
export const fogTransparency = sessionState(255);

export const wildernessLevelSeen = sessionState(-1);

export const lobbySteps = sessionState(0);

export const bannerBoundsWidth = sessionState(220);

export const bannerAge = sessionState(-1);
