import { createRef } from 'ts2';

export const chatchannelSetup = createRef('chatchannel_setup');
export const friendList = createRef('chatchannel_setup:list');
export const tooltipHost = createRef('chatchannel_setup:tooltip');
