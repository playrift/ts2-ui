import { createRef } from 'ts2';
import { DarknessDark } from './darkness-dark.component';

const darknessDarkRef = createRef('darkness_dark');

export const darknessDarkUi = (): UI => (
  <ui ref={darknessDarkRef}>
    <DarknessDark />
  </ui>
);
