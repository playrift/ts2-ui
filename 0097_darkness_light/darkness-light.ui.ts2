import { createRef } from 'ts2';
import { DarknessLight } from './darkness-light.component';

const darknessLightRef = createRef('darkness_light');

export const darknessLightUi = (): UI => (
  <ui ref={darknessLightRef}>
    <DarknessLight />
  </ui>
);
