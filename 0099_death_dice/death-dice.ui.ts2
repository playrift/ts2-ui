import { createRef } from 'ts2';
import { DeathDice } from './death-dice.component';

const deathDiceRef = createRef('death_dice');

export const deathDiceUi = (): UI => (
  <ui ref={deathDiceRef}>
    <DeathDice />
  </ui>
);
