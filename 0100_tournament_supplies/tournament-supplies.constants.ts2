import { createRef } from 'ts2';

import { Fonts } from '../lib/game/fonts';
import { Inventories } from '../lib/game/inventories';
import { Sprites } from '../lib/game/sprites';
import { Varps } from '../lib/game/varps';

export const tournamentSuppliesRef = createRef('tournament_supplies');

export const TITLE = 'Tournament Supplies';

export declare const SuppliesVarps: {
  readonly WORLD_FLAGS: typeof Varps.MAP_FLAGS_CACHED;
  readonly LOADOUT: typeof Varps.IF1;
};

export declare const SuppliesInventories: {
  readonly PREVIEW: typeof Inventories.TRAIL_PUZZLEHINTINV;
};

export declare const SuppliesFonts: {
  readonly SMALL: typeof Fonts.P11_FULL;
  readonly BODY: typeof Fonts.P12_FULL;
  readonly BOLD: typeof Fonts.B12_FULL;
};

export declare const SuppliesSprites: {
  readonly BACKING: typeof Sprites.TRADEBACKING_DARK;
  readonly GLASS: typeof Sprites.BANKBUTTONS_2;
  readonly SLOT: typeof Sprites.MISCGRAPHICS_0;
  readonly RAIL_V: typeof Sprites.MISCGRAPHICS_2;
  readonly RAIL_H: typeof Sprites.MISCGRAPHICS_3;
};

export enum SuppliesColour {
  ORANGE = 0xff981f,
  WHITE = 0xffffff,
  GREY = 0x9f9f9f,
  ROW_SELECTED = 0xffb83f,
  ROW = 0xff891f,
  SHADOW = 0x333333,
}

export const Layout = {
  ROOT_W: 480,
  ROOT_H: 320,
  SWITCH_X: 7,
  SWITCH_Y: 7,
  SWITCH_H: 20,
  SWITCH_PAD: 10,
  MEASURE_W: 512,
  PANE_BOTTOM: 10,
  PANE_INSET_W: 20,
  PANE_INSET_H: 50,
  LIST_INSET_W: 16,
  LIST_INSET_H: 40,
  SCROLLBAR_W: 16,
  LIST_H: 230,
  SEARCH_W: 150,
  SEARCH_H: 36,
  GLASS_X: 2,
  GLASS_W: 29,
  GLASS_H: 22,
  SEARCH_TEXT_INSET: 33,
  SEARCH_TEXT_RIGHT: 4,
  CELL_W: 36,
  CELL_H: 32,
  COLUMNS: 10,
  ROW_STEP: 42,
  COLUMN_GAP: 9,
  EMPTY_H: 50,
  CAPTION_X: 165,
  CAPTION_W: 140,
  CAPTION_H: 35,
  PREVIEW_W: 160,
  PREVIEW_H: 246,
  PREVIEW_PITCH_X: 40,
  PREVIEW_PITCH_Y: 35,
  PREVIEW_INSET: 2,
  PREVIEW_COLUMNS: 4,
  PREVIEW_LINE_H: 20,
  CROSS_X: 165,
  CROSS_W: 140,
  CROSS_H: 200,
  SLOT: 36,
  SLOT_ITEM_X: 2,
  PLACEHOLDER: 32,
  LIST_FRAME_INSET_W: 310,
  LIST_FRAME_INSET_H: 40,
  ROWS_X: 2,
  ROWS_INSET_W: 20,
  ROWS_INSET_H: 4,
  ROWS_H: 226,
  ROW_H: 25,
  APPLY_RIGHT: 20,
  APPLY_INSET_W: 350,
  APPLY_H: 35,
};

export const WORN_SLOTS = 14;
export const CARRIED_SLOTS = 28;

export enum RowTrans {
  SELECTED = 200,
  EVEN = 255,
  ODD = 220,
  HOVER = 200,
}

export enum Flash {
  PRESSED = 100,
  REST = 0,
}

export const APPLY_BUSY_CYCLES = 40;

export const MAX_LOADOUTS = 24;

export const Copy = {
  ITEMS: 'Items',
  LOADOUTS: 'Load-outs',
  SEARCH: 'Search',
  CLEAR: 'Clear',
  SEARCH_TERM: '<lt>Search term<gt>',
  NO_MATCH: 'No matching items found.',
  PREVIEW_HINT: 'Select a load-out<br>to preview it.',
  PREVIEW: 'Preview',
  APPLY: 'Apply',
  BUSY: '...',
  NONE: '---',
  EXAMINE: 'Examine',
};

export const SEARCH_SHOWN = 9;

export const MESLAYER_SEARCH = 11;

export const TAKE_OPS = ['Take', 'Take-5', 'Take-10', 'Take-X', '', '', '', '', '', 'Examine'] as const;
export const STACK_OPS = ['Take', '', '', 'Take-X', '', '', '', '', '', 'Examine'] as const;
export const EXAMINE_OPS = ['', '', '', '', '', '', '', '', '', 'Examine'] as const;
