import { sessionState, useState } from 'ts2';

export const loadoutNames = sessionState('');

export function tournamentSuppliesLoadouts(names: String): void {
  const [, setNames] = useState(loadoutNames);
  setNames(names);
}
