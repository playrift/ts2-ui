import { createRef } from 'ts2';
import { BrutFoundDoc } from './brut-found-doc.component';

const brutFoundDocRef = createRef('brut_found_doc');

export const brutFoundDocUi = (): UI => (
  <ui ref={brutFoundDocRef}>
    <BrutFoundDoc />
  </ui>
);
