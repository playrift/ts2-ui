import { createRef } from 'ts2';
import { PatternNext } from './pattern-next.component';

const patternNextRef = createRef('pattern_next');

export const patternNextUi = (): UI => (
  <ui ref={patternNextRef}>
    <PatternNext />
  </ui>
);
