import { createRef } from 'ts2';
import { AgilityArenaRewards } from './agilityarena-rewards.component';
import { Layout } from './agilityarena-rewards.constants';

const agilityArenaRewardsRef = createRef('agilityarena_rewards');

export const agilityArenaRewardsUi = (): UI => (
  <ui ref={agilityArenaRewardsRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <AgilityArenaRewards />
  </ui>
);
