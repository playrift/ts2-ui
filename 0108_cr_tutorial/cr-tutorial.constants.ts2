import { Fonts } from '../lib/game/fonts';
import { Sprites } from '../lib/game/sprites';

export declare const TutorialFonts: {
  readonly BODY: typeof Fonts.P12_FULL;
};

export declare const TutorialArt: {
  readonly ADVISER_ICON: typeof Sprites.SIDE_ICONS_37;
  readonly BACKDROP: typeof Sprites.TRADEBACKING;
};

export enum TutorialColors {
  BODY = 0xff981f,
  LABEL = 0xffffff,
}

export enum TutorialLayout {
  PANEL_WIDTH = 400,
  PANEL_HEIGHT = 250,

  TEXT_Y = 35,
  TEXT_W_INSET = 12,
  TEXT_H_INSET = 41,

  FIRST_Y = 5,
  PAD = 10,
  LINE = 15,

  MEASURE_WIDTH = 368,

  BUTTON_Y = 205,
  BUTTON_WIDTH = 75,
  BUTTON_HEIGHT = 32,

  ICON_WIDTH = 33,
  ICON_HEIGHT = 36,
}

export enum TutorialText {
  HEADING = 'Welcome back to Old School RuneScape!',
  BODY = "To help you find new adventures, you can use the new <col=ffffff>Activity Adviser</col> feature. This provides personalised activity suggestions if you're ever unsure what to do next!",
  HINT_DESKTOP = 'Click the button by the <col=ffffff>Minimap</col> to find new adventures!',
  HINT_MOBILE = 'Tap the button by the <col=ffffff>Minimap</col> to find new adventures!',
  BUTTON_LABEL = 'Close',
}

export enum TutorialFrameText {
  TITLE = 'Activity Adviser',
}

export enum TutorialFrameFlags {
  NO_CLOSE_BUTTON = 1,
}
