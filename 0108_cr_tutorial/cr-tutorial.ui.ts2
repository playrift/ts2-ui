import { createRef } from 'ts2';
import { CrTutorial } from './cr-tutorial.component';

const crTutorialRef = createRef('cr_tutorial');

export const crTutorialUi = (): UI => (
  <ui ref={crTutorialRef}>
    <CrTutorial />
  </ui>
);
