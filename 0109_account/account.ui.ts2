import { createRef } from 'ts2';
import { Account } from './account.component';

const accountRef = createRef('account');

export const accountUi = (): UI => (
  <ui ref={accountRef} x="center" y="center" w="fill" h="fill">
    <Account />
  </ui>
);
