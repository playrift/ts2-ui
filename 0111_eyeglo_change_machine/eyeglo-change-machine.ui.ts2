import { createRef } from 'ts2';

import { EygloChangeMachine } from './eyeglo-change-machine.component';

const eygloChangeMachineRef = createRef('eyeglo_change_machine');

export const eygloChangeMachineUi = (): UI => (
  <ui ref={eygloChangeMachineRef}>
    <EygloChangeMachine />
  </ui>
);
