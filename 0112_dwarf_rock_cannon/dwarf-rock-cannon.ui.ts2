import { createRef } from 'ts2';
import { DwarfRockCannon } from './dwarf-rock-cannon.component';

const dwarfRockCannonRef = createRef('dwarf_rock_cannon');

export const dwarfRockCannonUi = (): UI => (
  <ui ref={dwarfRockCannonRef}>
    <DwarfRockCannon />
  </ui>
);
