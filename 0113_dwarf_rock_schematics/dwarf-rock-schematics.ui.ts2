import { createRef } from 'ts2';
import { DwarfRockSchematics } from './dwarf-rock-schematics.component';

const dwarfRockSchematicsRef = createRef('dwarf_rock_schematics');

export const dwarfRockSchematicsUi = (): UI => (
  <ui ref={dwarfRockSchematicsRef}>
    <DwarfRockSchematics />
  </ui>
);
