import { createRef } from 'ts2';
import { DwarfRockSchematicsControl } from './dwarf-rock-schematics-control.component';

const dwarfRockSchematicsControlRef = createRef('dwarf_rock_schematics_control');

export const dwarfRockSchematicsControlUi = (): UI => (
  <ui ref={dwarfRockSchematicsControlRef}>
    <DwarfRockSchematicsControl />
  </ui>
);
