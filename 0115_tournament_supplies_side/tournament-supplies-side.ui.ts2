import { createRef } from 'ts2';

const tournamentSuppliesSide = createRef('tournament_supplies_side');

export function TournamentSuppliesSide(): Component {
  return <layer x={16} y={8} w={162} h={250} />;
}

export const tournamentSuppliesSideUi = (): UI => (
  <ui ref={tournamentSuppliesSide}>
    <TournamentSuppliesSide />
  </ui>
);
