import { createRef } from 'ts2';
import { TournamentSidepanel } from './tournament-sidepanel.component';
import { TournamentLayout } from './tournament-sidepanel.constants';

const tournamentSidepanelRef = createRef('tournament_sidepanel');

export const tournamentSidepanelUi = (): UI => (
  <ui ref={tournamentSidepanelRef} w={TournamentLayout.ROOT_W} h={TournamentLayout.ROOT_H} x="center" y="center">
    <TournamentSidepanel />
  </ui>
);
