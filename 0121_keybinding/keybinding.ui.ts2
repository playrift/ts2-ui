import { Keybinding } from './keybinding.component';
import { Layout, keybindingRef } from './keybinding.constants';

export const keybindingUi = (): UI => (
  <ui ref={keybindingRef} x={Layout.ROOT_X} y={Layout.ROOT_Y} w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <Keybinding />
  </ui>
);
