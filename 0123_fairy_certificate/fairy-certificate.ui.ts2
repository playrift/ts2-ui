import { createRef } from 'ts2';
import { FairyCertificate } from './fairy-certificate.component';

const fairyCertificateRef = createRef('fairy_certificate');

export const fairyCertificateUi = (): UI => (
  <ui ref={fairyCertificateRef}>
    <FairyCertificate />
  </ui>
);
