import { createRef } from 'ts2';
import { FairySymptoms } from './fairy-symptoms.component';

const fairySymptomsRef = createRef('fairy_symptoms');

export const fairySymptomsUi = (): UI => (
  <ui ref={fairySymptomsRef}>
    <FairySymptoms />
  </ui>
);
