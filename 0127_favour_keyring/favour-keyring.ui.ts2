import { createRef } from 'ts2';
import { FavourKeyring } from './favour-keyring.component';

const favourKeyringRef = createRef('favour_keyring');

export const favourKeyringUi = (): UI => (
  <ui ref={favourKeyringRef}>
    <FavourKeyring />
  </ui>
);
