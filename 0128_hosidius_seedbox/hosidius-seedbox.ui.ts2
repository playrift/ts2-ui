import { createRef } from 'ts2';
import { HosidiusSeedbox } from './hosidius-seedbox.component';

const hosidiusSeedboxRef = createRef('hosidius_seedbox');

export const hosidiusSeedboxUi = (): UI => (
  <ui ref={hosidiusSeedboxRef} w={512} h={334} x="center" y="center">
    <HosidiusSeedbox />
  </ui>
);
