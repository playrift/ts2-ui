import { createRef } from 'ts2';
import { NzoneLists } from './nzone-lists.component';

const nzoneListsRef = createRef('nzone_lists');

export const nzoneListsUi = (): UI => (
  <ui ref={nzoneListsRef} x="center" y="center">
    <NzoneLists />
  </ui>
);
