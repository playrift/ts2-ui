import { createRef } from 'ts2';
import { DeadmanTournamentViewer } from './deadman-tournament-viewer.component';

const deadmanTournamentViewerRef = createRef('deadman_tournament_viewer');

export const deadmanTournamentViewerUi = (): UI => (
  <ui ref={deadmanTournamentViewerRef} x="center" y="center">
    <DeadmanTournamentViewer />
  </ui>
);
