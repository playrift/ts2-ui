import { Fonts } from '../lib/game/fonts';
import { Varps } from '../lib/game/varps';

export declare const WaitingFonts: {
  readonly LABEL: typeof Fonts.B12_FULL;
};

export declare const WaitingVarps: {
  readonly TIMER: typeof Varps.CASTLEWARS_TIMER;
};

export const Label = {
  W: 300,
  H: 30,
};

export enum WaitingColor {
  LABEL = 0xffffff,
}

export enum WaitingOverlay {
  DODGE = 118504,
}

export const IDLE_TEXT = 'Waiting for players to join the other team.';
export const COUNTDOWN_PREFIX = 'Time until next game starts: ';
