import { createRef } from 'ts2';
import { GardenList } from './garden-list.component';

const gardenListRef = createRef('garden_list');

export const gardenListUi = (): UI => (
  <ui ref={gardenListRef}>
    <GardenList />
  </ui>
);
