import { createRef } from 'ts2';
import { GardenQuiz } from './garden-quiz.component';

const gardenQuizRef = createRef('garden_quiz');

export const gardenQuizUi = (): UI => (
  <ui ref={gardenQuizRef}>
    <GardenQuiz />
  </ui>
);
