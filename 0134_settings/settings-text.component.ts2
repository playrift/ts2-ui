import { textLineCount } from '../lib/lib.engine';
import { Colour, Row, SettingsFonts } from './settings.constants';
import type { SettingsRow } from './settings-rows.data';
import { rowTitle } from './settings-layout';

export function SettingsTextBlock({
  row,
  top,
  stripe,
  mobile,
  compact,
  width,
}: {
  row: SettingsRow;
  top: Int;
  stripe: Int;
  mobile: Boolean;
  compact: Boolean;
  width: Int;
}): Component {
  if ((row.hideWhenCompact ?? false) && compact) {
    return null;
  }
  const title: String = rowTitle(row, mobile);
  if (title.length <= 0) {
    return null;
  }
  const text: Int = textLineCount(title, width - Row.blockMeasureInset, SettingsFonts.BODY) * Row.lineHeight;

  return (
    <>
      <rect
        x={Row.blockX}
        y={top}
        w={{ fill: Row.blockInsetW }}
        h={text + Row.blockPad}
        fill
        color={Colour.BLACK}
        transparency={stripe}
      />
      <text
        x={Row.blockTextX}
        y={top + Row.blockTextDy}
        w={{ fill: Row.blockTextInsetW }}
        h={text}
        text={title}
        font={SettingsFonts.BODY}
        shadow
        color={Colour.GREY}
        align="left"
        valign="middle"
        lineHeight={Row.lineHeight}
      />
    </>
  );
}
