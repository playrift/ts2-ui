import { createRef } from 'ts2';
import { AideCompass } from './aide-compass.component';

const aideCompassRef = createRef('aide_compass');

export const aideCompassUi = (): UI => (
  <ui ref={aideCompassRef}>
    <AideCompass />
  </ui>
);
