import { createRef } from 'ts2';
import { Unmorph } from './unmorph.component';

const unmorphRef = createRef('unmorph');

export const unmorphUi = (): UI => (
  <ui ref={unmorphRef}>
    <Unmorph />
  </ui>
);
