import { createRef } from 'ts2';
import { XpdropsSetup } from './xpdrops-setup.component';

const xpdropsSetupRef = createRef('xpdrops_setup');

export const xpdropsSetupUi = (): UI => (
  <ui ref={xpdropsSetupRef} x="center" y="center">
    <XpdropsSetup />
  </ui>
);
