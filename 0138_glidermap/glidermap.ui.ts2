import { createRef } from 'ts2';
import { GliderMap } from './glidermap.component';

const gliderMapRef = createRef('glidermap');

export const gliderMapUi = (): UI => (
  <ui ref={gliderMapRef}>
    <GliderMap />
  </ui>
);
