import { useServerState } from 'ts2';

import { GnomeballVarbits } from './gnomeball.constants';

export function gnomeballScore(label: ComponentId): void {
  label.text = `${useServerState(GnomeballVarbits.SCORE)}`;
}
