import { createRef } from 'ts2';
import { Gnomeball_ } from './gnomeball.component';

const gnomeballRef = createRef('gnomeball');

export const gnomeballUi = (): UI => (
  <ui ref={gnomeballRef}>
    <Gnomeball_ />
  </ui>
);
