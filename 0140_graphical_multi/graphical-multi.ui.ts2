import { createRef } from 'ts2';
import { GraphicalMulti } from './graphical-multi.component';

const graphicalMultiRef = createRef('graphical_multi');

export const graphicalMultiUi = (): UI => (
  <ui ref={graphicalMultiRef}>
    <GraphicalMulti />
  </ui>
);
