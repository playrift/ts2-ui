import { createRef } from 'ts2';
import { PetInsurance } from './pet-insurance.component';
import { Layout } from './pet-insurance.constants';

const petInsuranceRef = createRef('pet_insurance');

export const petInsuranceUi = (): UI => (
  <ui ref={petInsuranceRef} x={Layout.ROOT_X} y={Layout.ROOT_Y} w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <PetInsurance />
  </ui>
);
