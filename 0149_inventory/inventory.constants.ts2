import { Inventories } from '../lib/game/inventories';
import { Varbits } from '../lib/game/varbits';

export enum InventoryLayout {
  ROOT_W = 190,
  ROOT_H = 261,
  ORIGIN_X = 16,
  ORIGIN_Y = 8,
  COLUMN_PITCH = 42,
  ROW_PITCH = 36,
  COLUMNS = 4,
  SLOT_W = 36,
  SLOT_H = 32,
}

export enum SlotStyle {
  SHADOW = 0x333333,
  OUTLINE = 1,
  OUTLINE_TARGETED = 2,
  DRAG_DEAD_ZONE = 5,
  DRAG_DEAD_TIME = 6,
}

export enum Fade {
  OPAQUE = 0,
  PRESSED = 100,
  EMPTY = 255,
}

export enum OpPriority {
  NONE = -1,
  MIN = 0,
  MAX = 10,
}

export enum InventoryItems {
  BLANK = 6512,
}

export const SHIFT_KEY = 81;

export const USE = 'Use';
export const EXAMINE = 'Examine';
export const EXAMINE_SLOT = 10;
export const OP_BASE_PREFIX = '<col=ff9040>';
export const OP_BASE_SUFFIX = '</col>';

export declare const InventoryInventories: {
  readonly CARRIED: typeof Inventories.INV;
};

export declare const InventoryVarbits: {
  readonly DESKTOP_SHIFT_DROP: typeof Varbits.DESKTOP_SHIFTCLICKDROP_ENABLED;
  readonly MOBILE_TAP_DROP: typeof Varbits.SETTINGS_MOBILE_TAPTODROP_ENABLED;
  readonly MOBILE_TILE_HIGHLIGHT: typeof Varbits.SETTINGS_MOBILE_TILE_HIGHLIGHT_ENABLED;
  readonly NPC_TAGGING: typeof Varbits.OPTION_NPC_INDICATORS_TAGGING_ENABLED;
};
