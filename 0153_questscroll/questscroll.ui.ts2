import { createRef } from 'ts2';
import { QuestScroll } from './questscroll.component';

const questScrollRef = createRef('questscroll');

export const questScrollUi = (): UI => (
  <ui ref={questScrollRef}>
    <QuestScroll />
  </ui>
);
