import { createRef } from 'ts2';
import { DeadmanSpectator } from './deadman-spectator.component';

const deadmanSpectatorRef = createRef('deadman_spectator');

export const deadmanSpectatorUi = (): UI => (
  <ui ref={deadmanSpectatorRef}>
    <DeadmanSpectator />
  </ui>
);
