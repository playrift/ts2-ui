import { createRef } from 'ts2';
import { BarrowsReward } from './barrows-reward.component';
import { Panel } from './barrows-reward.constants';

const barrowsRewardRef = createRef('barrows_reward');

export const barrowsRewardUi = (): UI => (
  <ui ref={barrowsRewardRef} w={Panel.WIDTH} h={Panel.HEIGHT} x="center" y="center">
    <BarrowsReward />
  </ui>
);
