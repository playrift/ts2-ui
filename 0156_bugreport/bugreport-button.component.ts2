import { useState } from 'ts2';

import { StoneButtonOutFilled } from '../lib/stone-button-out-filled.component';
import { StoneButtonPressedFilled } from '../lib/stone-button-pressed-filled.component';
import { BugreportColour, BugreportFonts } from './bugreport.constants';

export function BugreportButton({ label }: { label: String }): Component {
  const [hovered, setHovered] = useState(0);
  return (
    <layer w="fill" h="fill" onMouseEnter={() => setHovered(1)} onMouseLeave={() => setHovered(0)}>
      {hovered === 1 ? <StoneButtonPressedFilled /> : <StoneButtonOutFilled />}
      <text
        x="center"
        y="center"
        w="fill"
        h="fill"
        text={label}
        font={BugreportFonts.BODY}
        shadow
        align="center"
        valign="middle"
        color={hovered === 1 ? BugreportColour.AMBER_LIGHT : BugreportColour.AMBER}
      />
    </layer>
  );
}
