import { createRef } from 'ts2';

export const bugreportRef = createRef('bugreport');

export const INTRO_PAGE = createRef();
export const FORM_PAGE = createRef();
export const REPRO_PAGE = createRef();

export enum Layout {
  ROOT_W = 350,
  ROOT_H = 320,
  BODY_BOTTOM = 10,
  BODY_INSET_W = 20,
  BODY_INSET_H = 50,
  INTRO_H = 90,
  SUPPORT_Y = 78,
  SUPPORT_H = 20,
  NEWS_Y = 100,
  NEWS_INSET_H = 135,
  FORM_PROMPT_H = 41,
  REPRO_PROMPT_H = 17,
  FORM_BOX_Y = 41,
  FORM_BOX_INSET_H = 90,
  REPRO_BOX_Y = 21,
  REPRO_BOX_INSET_H = 70,
  PANE_X = 2,
  PANE_INSET_W = 20,
  PANE_INSET_H = 4,
  BAR_W = 16,
  BUTTON_W = 120,
  BUTTON_H = 30,
  BUTTON_SIDE = 25,
  NEWS_HEADING_H = 40,
  LINE_H = 12,
  LINE_PAD = 5,
}

export enum BugreportFonts {
  SMALL = 494,
  BODY = 495,
  BOLD = 496,
}

export enum BugreportColour {
  AMBER = 0xff981f,
  AMBER_LIGHT = 0xffa82f,
  WHITE = 0xffffff,
}

export enum BugreportSounds {
  SELECT = 2266,
  WRONG = 2277,
}

export const STONE_TILE = 1040;

export const BOX_BACKING_TRANSPARENCY = 150;

export enum BugreportScrollbar {
  TRACK = 792,
  CAP_TOP = 789,
  THUMB = 790,
  CAP_BOTTOM = 791,
  ARROW_UP = 773,
  ARROW_DOWN = 788,
}

export enum BugreportInput {
  DESCRIPTION = 0,
  REPRODUCTION = 1,
}

export enum BugreportGate {
  THIS_PANEL = 156,
  NO_SUB = -1,
}

export const KEYBOARD_LIMIT = 500;

export const PARAGRAPH_MODE = 5;

export const REPORT_CAP = 10;
export const REPORT_MIN_LENGTH = 15;

export const TITLE = 'Bug report form';

export const INTRO_TEXT =
  'This form is for reporting <col=ffffff>game bugs</col> to the testing staff. Please ensure your bug can be verified in the <col=ffffff>Official Old School RuneScape Client</col> before reporting it.<br><br>For issues on <col=ffffff>account recovery</col>, <col=ffffff>billing</col>, or <col=ffffff>offenses</col>, the Support team will be better placed to help you.';

export const FORM_TEXT =
  'Please describe the bug. It may be helpful to give details of <col=ffffff>what you were doing</col>, <col=ffffff>what happened</col> when you did it, and <col=ffffff>what you expected</col> to happen instead.';

export const REPRO_TEXT = 'Please describe how to make the bug happen again.';

export const SUPPORT_LABEL = 'Support Centre';
export const SUPPORT_URL = 'https://support.runescape.com/';
