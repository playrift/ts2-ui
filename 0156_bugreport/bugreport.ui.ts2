import { Bugreport } from './bugreport.component';
import { bugreportRef, Layout } from './bugreport.constants';

export const bugreportUi = (): UI => (
  <ui ref={bugreportRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <Bugreport />
  </ui>
);
