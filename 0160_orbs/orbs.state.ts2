import { sessionState } from 'ts2';

export const runFlashLit = sessionState(0);

export const mapTipText = sessionState('');

export const mapTipRight = sessionState(-1);
export const mapTipY = sessionState(-1);
export const mapTipH = sessionState(-1);
