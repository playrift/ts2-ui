import { createRef } from 'ts2';
import { SnowFlakes } from './snow-flakes.component';

const snowFlakesRef = createRef('snow_flakes');

export const snowFlakesUi = (): UI => (
  <ui ref={snowFlakesRef}>
    <SnowFlakes />
  </ui>
);
