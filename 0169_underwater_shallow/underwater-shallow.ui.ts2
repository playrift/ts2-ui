import { createRef } from 'ts2';
import { UnderwaterShallow } from './underwater-shallow.component';

const underwaterShallowRef = createRef('underwater_shallow');

export const underwaterShallowUi = (): UI => (
  <ui ref={underwaterShallowRef}>
    <UnderwaterShallow />
  </ui>
);
