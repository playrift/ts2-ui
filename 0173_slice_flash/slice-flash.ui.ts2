import { createRef } from 'ts2';
import { SliceFlash } from './slice-flash.component';

const sliceFlash = createRef('slice_flash');

export const sliceFlashUi = (): UI => (
  <ui ref={sliceFlash}>
    <SliceFlash />
  </ui>
);
