import { createRef } from 'ts2';
import { FadeOverlay } from './fade-overlay.component';

const fadeOverlayRef = createRef('fade_overlay');

export const fadeOverlayUi = (): UI => (
  <ui ref={fadeOverlayRef}>
    <FadeOverlay />
  </ui>
);
