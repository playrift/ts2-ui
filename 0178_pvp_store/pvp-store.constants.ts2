import { Fonts } from '../lib/game/fonts';
import { Sprites } from '../lib/game/sprites';
import { Varps } from '../lib/game/varps';

export declare const StoreFonts: {
  readonly HEADING: typeof Fonts.P12_FULL;
  readonly PRICE: typeof Fonts.P11_FULL;
};

export declare const StoreScrollbar: {
  readonly TRACK: typeof Sprites.SCROLLBAR_DRAGGER_V2_3;
  readonly CAP_TOP: typeof Sprites.SCROLLBAR_DRAGGER_V2_0;
  readonly THUMB: typeof Sprites.SCROLLBAR_DRAGGER_V2_1;
  readonly CAP_BOTTOM: typeof Sprites.SCROLLBAR_DRAGGER_V2_2;
  readonly ARROW_UP: typeof Sprites.SCROLLBAR_V2_0;
  readonly ARROW_DOWN: typeof Sprites.SCROLLBAR_V2_1;
};

export declare const StoreVarps: {
  readonly ARCHAIC_POINTS: typeof Varps.IF2;
  readonly PRESTIGE_POINTS: typeof Varps.IF3;
};

export enum BountyHat {
  HUNTER = 12855,
  ROGUE = 12856,
}

export const SWAP_ROGUE_HAT = false;

export const TITLE_PREFIX = 'Bounty Hunter Shop - Archaic points: <col=ffffff>';
export const TITLE_SUFFIX = '</col>';

export const SEPARATOR = ',';

export const PRESTIGE_HEADING = 'Prestige rewards:';

export enum Layout {
  ROOT_W = 472,
  ROOT_H = 316,
  PANE_X = 10,
  PANE_BOTTOM = 10,
  PANE_INSET_W = 36,
  PANE_INSET_H = 50,
  PANE_W = 436,
  PANE_H = 266,
  BAR_W = 16,
  BAR_RIGHT = 10,
  UNLOCKS_W = 350,
}

export enum Grid {
  ROW_H = 42,
  PER_ROW = 9,
  STRIDE = 50,
  ITEM_W = 36,
  ITEM_H = 32,
  SHADOW = 0x333333,
  OUTLINE = 1,
}

export enum Prestige {
  TOP = 20,
  PRICE_W = 36,
  PRICE_H = 12,
}

export enum StoreColors {
  HEADING = 0xff981f,
  UNAFFORDABLE = 0xff0000,
  AFFORDABLE = 0xffffff,
}

export enum Fade {
  PRESSED = 100,
  REST = 0,
}
