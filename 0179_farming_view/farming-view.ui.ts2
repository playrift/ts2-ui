import { createRef } from 'ts2';

import { FarmingView, FarmingViewTooltip } from './farming-view.component';

const farmingViewRef = createRef('farming_view');

export const farmingViewUi = (): UI => (
  <ui ref={farmingViewRef}>
    <FarmingView />
    <FarmingViewTooltip />
  </ui>
);
