import { createRef } from 'ts2';
import { IiTracker } from './ii-tracker.component';

const iiTrackerRef = createRef('ii_tracker');

export const iiTrackerUi = (): UI => (
  <ui ref={iiTrackerRef}>
    <IiTracker />
  </ui>
);
