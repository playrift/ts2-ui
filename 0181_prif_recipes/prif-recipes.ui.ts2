import { createRef } from 'ts2';
import { PrifRecipes } from './prif-recipes.component';

const prifRecipesRef = createRef('prif_recipes');

export const prifRecipesUi = (): UI => (
  <ui ref={prifRecipesRef}>
    <PrifRecipes />
  </ui>
);
