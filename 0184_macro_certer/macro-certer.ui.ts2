import { createRef } from 'ts2';
import { MacroCerter } from './macro-certer.component';

const macroCerterRef = createRef('macro_certer');

export const macroCerterUi = (): UI => (
  <ui ref={macroCerterRef}>
    <MacroCerter />
  </ui>
);
