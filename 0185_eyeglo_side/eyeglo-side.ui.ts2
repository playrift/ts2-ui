import { createRef } from 'ts2';

const eyegloSide = createRef('eyeglo_side');

export function EyegloSide(): Component {
  return <layer x={16} y={8} w={162} h={250} />;
}

export const eyegloSideUi = (): UI => (
  <ui ref={eyegloSide}>
    <EyegloSide />
  </ui>
);
