import { sessionState } from 'ts2';

export const menuCall = sessionState(0);

export const menuTitle = sessionState('');

export const menuOptions = sessionState('');

export const menuMode = sessionState(0);

export const enum MenuMode {
  PLAIN = 0,
  KEYS = 1,
  INDEXED = 2,
}
