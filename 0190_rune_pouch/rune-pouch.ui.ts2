import { RunePouch } from './rune-pouch.component';
import { Layout, universeRef } from './rune-pouch.constants';

export const runePouchUi = (): UI => (
  <ui ref={universeRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <RunePouch />
  </ui>
);
