import { createRef, sessionState } from 'ts2';

export const depositMenuOpen = sessionState(0);

export const depositMain = createRef('bank_depositbox:main');
export const depositBlocker = createRef('bank_depositbox:blocker');
export const depositSettingsMenu = createRef('bank_depositbox:settings_menu');
export const depositMenuButton = createRef('bank_depositbox:menu_button');
export const depositTitle = createRef('bank_depositbox:title');
