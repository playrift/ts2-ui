import { createRef } from 'ts2';
import { MagicTrainingAlchem_ } from './magictraining-alchem.component';

const magictrainingAlchemRef = createRef('magictraining_alchem');

export const magictrainingAlchemUi = (): UI => (
  <ui ref={magictrainingAlchemRef}>
    <MagicTrainingAlchem_ />
  </ui>
);
