import { Fonts } from '../lib/game/fonts';
import { Sprites } from '../lib/game/sprites';

export declare const FfaFonts: {
  readonly HINT: typeof Fonts.QUILL_OBLIQUE_LARGE;
};

export declare const FfaArt: {
  readonly BACKING: typeof Sprites.TRADEBACKING_DARK;
};

export const HINT = 'Step over the line to fight!';

export const Plate = {
  MEASURE_WIDTH: 512,
  PADDING: 15,
  H: 40,
  Y: 3,
  TEXT_Y: 3,
  TEXT_H: 34,
  BACKING_WASH: 100,
};

export enum FfaColor {
  HINT = 0xffffa0,
}

export const FfaZone = {
  MIN_X: 3264,
  MIN_Z: 4736,
  MIN_PLANE: 0,
  MAX_X: 3391,
  MAX_Z: 4759,
  MAX_PLANE: 3,
};

export enum FfaOverlay {
  DODGE = 52968,
}
