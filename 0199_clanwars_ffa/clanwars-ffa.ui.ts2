import { createRef } from 'ts2';
import { ClanwarsFfa } from './clanwars-ffa.component';

const clanwarsFfaRef = createRef('clanwars_ffa');

export const clanwarsFfaUi = (): UI => (
  <ui ref={clanwarsFfaRef}>
    <ClanwarsFfa />
  </ui>
);
