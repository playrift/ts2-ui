import { createRef } from 'ts2';

import { GeViewonly } from './ge-viewonly.component';

const geViewonlyRef = createRef('ge_viewonly');

export const geViewonlyUi = (): UI => (
  <ui ref={geViewonlyRef}>
    <GeViewonly />
  </ui>
);
