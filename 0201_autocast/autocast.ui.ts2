import { createRef } from 'ts2';
import { Autocast } from './autocast.component';

const autocastRef = createRef('autocast');

export const autocastUi = (): UI => (
  <ui ref={autocastRef}>
    <Autocast />
  </ui>
);
