import { createRef } from 'ts2';
import { TrailClueText } from './trail-cluetext.component';

const trailClueTextRef = createRef('trail_cluetext');

export const trailClueTextUi = (): UI => (
  <ui ref={trailClueTextRef}>
    <TrailClueText />
  </ui>
);
