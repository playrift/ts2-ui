import { createRef } from 'ts2';

import { MakeoverMage } from './makeover-mage.component';
import { Layout } from './makeover-mage.constants';

const makeoverMageRef = createRef('makeover_mage');

export const makeoverMageUi = (): UI => (
  <ui ref={makeoverMageRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <MakeoverMage />
  </ui>
);
