import { createRef } from 'ts2';
import { NzoneRewards } from './nzone-rewards.component';
import { NzoneLayout } from './nzone-rewards.constants';

const nzoneRewardsRef = createRef('nzone_rewards');

export const nzoneRewardsUi = (): UI => (
  <ui ref={nzoneRewardsRef} w={NzoneLayout.ROOT_W} h={NzoneLayout.ROOT_H} x={NzoneLayout.ROOT_X} y={NzoneLayout.ROOT_Y}>
    <NzoneRewards />
  </ui>
);
