import { createRef } from 'ts2';
import { NzoneLobby } from './nzone-lobby.component';

const nzoneLobbyRef = createRef('nzone_lobby');

export const nzoneLobbyUi = (): UI => (
  <ui ref={nzoneLobbyRef} x="center" y="center">
    <NzoneLobby />
  </ui>
);
