import { createRef } from 'ts2';
import { MazeTimer } from './mazetimer.component';

const mazeTimerRef = createRef('mazetimer');

export const mazeTimerUi = (): UI => (
  <ui ref={mazeTimerRef}>
    <MazeTimer />
  </ui>
);
