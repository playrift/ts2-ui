import { createRef } from 'ts2';
import { PohPetlist } from './poh-petlist.component';
import { PetlistLayout } from './poh-petlist.constants';

const pohPetlistRef = createRef('poh_petlist');

export const pohPetlistUi = (): UI => (
  <ui
    ref={pohPetlistRef}
    w={PetlistLayout.ROOT_W}
    h={PetlistLayout.ROOT_H}
    x={PetlistLayout.ROOT_X}
    y={PetlistLayout.ROOT_Y}
  >
    <PohPetlist />
  </ui>
);
