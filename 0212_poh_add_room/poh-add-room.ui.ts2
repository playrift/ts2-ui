import { createRef } from 'ts2';

import { PohAddRoom } from './poh-add-room.component';
import { PohLayout } from './poh-add-room.constants';

const pohAddRoomRef = createRef('poh_add_room');

export const pohAddRoomUi = (): UI => (
  <ui ref={pohAddRoomRef} x={PohLayout.ROOT_X} y={PohLayout.ROOT_Y} w={PohLayout.ROOT_W} h={PohLayout.ROOT_H}>
    <PohAddRoom />
  </ui>
);
