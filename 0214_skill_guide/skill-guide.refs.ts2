import { createRef } from 'ts2';

export const guideRoot = createRef('skill_guide');

export const guideWindow = createRef('skill_guide:universe');

export const guideClose = createRef('skill_guide:close');
