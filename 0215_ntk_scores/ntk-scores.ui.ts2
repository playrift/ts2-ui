import { createRef } from 'ts2';
import { NtkScores } from './ntk-scores.component';

const ntkScoresRef = createRef('ntk_scores');

export const ntkScoresUi = (): UI => (
  <ui ref={ntkScoresRef}>
    <NtkScores />
  </ui>
);
