import { createRef } from 'ts2';
import { ChatRight } from './chat-right.component';

const chatRightRef = createRef('chat_right');

export const chatRightUi = (): UI => (
  <ui ref={chatRightRef}>
    <ChatRight />
  </ui>
);
