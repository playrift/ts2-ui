import { createRef } from 'ts2';

import { MagicSpellbook } from './magic-spellbook.component';
import { Layout } from './magic-spellbook.constants';

const magicSpellbookRef = createRef('magic_spellbook');

export const magicSpellbookUi = (): UI => (
  <ui ref={magicSpellbookRef} x="center" y="center" w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <MagicSpellbook />
  </ui>
);
