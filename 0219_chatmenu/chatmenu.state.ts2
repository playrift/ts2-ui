import { sessionState } from 'ts2';

export const menuTitle = sessionState('');
export const menuOptions = sessionState('');
export const menuCount = sessionState(0);

export const menuWaiting = sessionState(0);

export const menuArmedAt1 = sessionState(0);
export const menuArmedAt2 = sessionState(0);
export const menuArmedAt3 = sessionState(0);
export const menuArmedAt4 = sessionState(0);
export const menuArmedAt5 = sessionState(0);
