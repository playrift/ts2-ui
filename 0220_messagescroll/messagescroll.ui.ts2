import { createRef } from 'ts2';
import { MessageScroll } from './messagescroll.component';

const messageScrollRef = createRef('messagescroll');

export const messageScrollUi = (): UI => (
  <ui ref={messageScrollRef}>
    <MessageScroll />
  </ui>
);
