import { createRef } from 'ts2';
import { PengEmote } from './peng-emote.component';

const pengEmoteRef = createRef('peng_emote');

export const pengEmoteUi = (): UI => (
  <ui ref={pengEmoteRef}>
    <PengEmote />
  </ui>
);
