import { createRef } from 'ts2';
import { MmMessage } from './mm-message.component';

const mmMessageRef = createRef('mm_message');

export const mmMessageUi = (): UI => (
  <ui ref={mmMessageRef}>
    <MmMessage />
  </ui>
);
