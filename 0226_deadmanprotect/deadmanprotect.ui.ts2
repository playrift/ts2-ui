import { createRef } from 'ts2';
import { DeadmanProtect } from './deadmanprotect.component';
import { Layout } from './deadmanprotect.constants';

const deadmanProtectRef = createRef('deadmanprotect');

export const deadmanProtectUi = (): UI => (
  <ui ref={deadmanProtectRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <DeadmanProtect />
  </ui>
);
