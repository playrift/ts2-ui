import { createRef } from 'ts2';
import { NeededItemsList } from './mourning-deathalter-list.component';

const neededItemsListRef = createRef('mourning_deathalter_list');

export const neededItemsListUi = (): UI => (
  <ui ref={neededItemsListRef} w={512} h={334}>
    <NeededItemsList />
  </ui>
);
