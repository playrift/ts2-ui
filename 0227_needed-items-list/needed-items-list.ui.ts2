import { createRef } from 'ts2';
import { NeededItemsList } from './needed-items-list.component';

const neededItemsListRef = createRef('needed-items-list');

export const neededItemsListUi = (): UI => (
  <ui ref={neededItemsListRef} w={512} h={334}>
    <NeededItemsList />
  </ui>
);
