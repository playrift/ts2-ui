import { createRef } from 'ts2';
import { DeadmanDelay } from './deadman-delay.component';

const deadmanDelayRef = createRef('deadman_delay');

export const deadmanDelayUi = (): UI => (
  <ui ref={deadmanDelayRef}>
    <DeadmanDelay />
  </ui>
);
