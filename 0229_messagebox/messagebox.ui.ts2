import { createRef } from 'ts2';
import { MessageBox } from './messagebox.component';

const messageBoxRef = createRef('messagebox');

export const messageBoxUi = (): UI => (
  <ui ref={messageBoxRef}>
    <MessageBox />
  </ui>
);
