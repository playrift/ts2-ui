import { createRef } from 'ts2';
import { DeadmanSafebox } from './deadman-safebox.component';
import { Layout } from './deadman-safebox.constants';

const deadmanSafeboxRef = createRef('deadman_safebox');

export const deadmanSafeboxUi = (): UI => (
  <ui ref={deadmanSafeboxRef} w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <DeadmanSafebox />
  </ui>
);
