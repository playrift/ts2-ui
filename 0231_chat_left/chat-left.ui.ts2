import { createRef } from 'ts2';
import { ChatLeft } from './chat-left.component';

const chatLeftRef = createRef('chat_left');

export const chatLeftUi = (): UI => (
  <ui ref={chatLeftRef}>
    <ChatLeft />
  </ui>
);
