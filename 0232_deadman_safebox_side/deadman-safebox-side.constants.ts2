import { Inventories } from '../lib/game/inventories';

export declare const Safebox: {
  readonly INV: typeof Inventories.INV;
};

export enum Grid {
  W = 36,
  H = 32,
  COLUMNS = 4,
  ORIGIN_X = 16,
  ORIGIN_Y = 8,
  STRIDE_X = 42,
  STRIDE_Y = 36,
  OUTLINE = 1,
  DRAG_DEAD = 5,
}

export enum SafeboxColor {
  SLOT_SHADOW = 0x333333,
}

export enum Transparency {
  FILLED = 0,
  BLANK = 255,
}

export const BLANK_ITEM = 6512;

export enum Root {
  W = 190,
  H = 261,
}
