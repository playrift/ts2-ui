import { createRef } from 'ts2';
import { DeadmanSafeboxGrid } from './deadman-safebox-side.component';
import { Root } from './deadman-safebox-side.constants';

const deadmanSafeboxSideRef = createRef('deadman_safebox_side');

export const deadmanSafeboxSideUi = (): UI => (
  <ui ref={deadmanSafeboxSideRef} w={Root.W} h={Root.H}>
    <layer w={Root.W} h={Root.H}>
      <DeadmanSafeboxGrid />
    </layer>
  </ui>
);
