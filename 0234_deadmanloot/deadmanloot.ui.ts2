import { createRef } from 'ts2';
import { DeadmanLoot } from './deadmanloot.component';
import { Layout } from './deadmanloot.constants';

const deadmanLootRef = createRef('deadmanloot');

export const deadmanLootUi = (): UI => (
  <ui ref={deadmanLootRef} w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <DeadmanLoot />
  </ui>
);
