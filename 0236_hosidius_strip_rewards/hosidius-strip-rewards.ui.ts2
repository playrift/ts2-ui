import { createRef } from 'ts2';
import { HosidiusStripRewards } from './hosidius-strip-rewards.component';
import { TitheLayout } from './hosidius-strip-rewards.constants';

const hosidiusStripRewardsRef = createRef('hosidius_strip_rewards');

export const hosidiusStripRewardsUi = (): UI => (
  <ui ref={hosidiusStripRewardsRef} w={TitheLayout.ROOT_W} h={TitheLayout.ROOT_H} x="center" y="center">
    <HosidiusStripRewards />
  </ui>
);
