import { Fonts } from '../lib/game/fonts';
import { Inventories } from '../lib/game/inventories';
import { Sounds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';
import { Varbits } from '../lib/game/varbits';

export declare const PriceFonts: {
  readonly LABEL: typeof Fonts.P12_FULL;
  readonly SMALL: typeof Fonts.P11_FULL;
};

export declare const PriceSprites: {
  readonly RADIO_COLUMN: typeof Sprites.OPTIONS_RADIO_BUTTONS_0;
  readonly RADIO_ROWS: typeof Sprites.OPTIONS_RADIO_BUTTONS_2;
  readonly ROW_BACKING: typeof Sprites.TRADEBACKING_LIGHT;
};

export declare const PriceScrollbar: {
  readonly TRACK: typeof Sprites.SCROLLBAR_DRAGGER_V2_3;
  readonly CAP_TOP: typeof Sprites.SCROLLBAR_DRAGGER_V2_0;
  readonly THUMB: typeof Sprites.SCROLLBAR_DRAGGER_V2_1;
  readonly CAP_BOTTOM: typeof Sprites.SCROLLBAR_DRAGGER_V2_2;
  readonly ARROW_UP: typeof Sprites.SCROLLBAR_V2_0;
  readonly ARROW_DOWN: typeof Sprites.SCROLLBAR_V2_1;
};

export declare const PriceSounds: {
  readonly TOGGLE: typeof Sounds.INTERFACE_SELECT1;
};

export declare const PriceVarbits: {
  readonly MODE: typeof Varbits.GE_PRICELIST_MODE;
};

export declare const PriceInventories: {
  readonly SHOP: typeof Inventories.GENERALSHOP1;
};

export const Copy = {
  TITLE: 'Grand Exchange: General Store',
  TOGGLE: 'Column view',
  ONE_COIN: '1 coin',
  ONE_COIN_WHITE: '<col=ffffff>1 coin</col>',
};

export const SEPARATOR = ',';

export enum Layout {
  ROOT_X = 6,
  ROOT_Y = 7,
  ROOT_W = 500,
  ROOT_H = 320,
  PANE_X = 10,
  PANE_Y = 40,
  PANE_W = 464,
  PANE_H = 250,
  BAR_X = 474,
  BAR_W = 16,
  TOGGLE_X = 10,
  TOGGLE_Y = 293,
  TOGGLE_H = 17,
  RADIO = 17,
  TOGGLE_PAD = 20,
  MEASURE_W = 512,
}

export enum Columns {
  CELL_W = 46,
  CELL_H = 42,
  INSET = 39,
  ROW_H = 77,
  STRIDE = 85,
  PER_ROW = 5,
  TOP = 20,
  ICON_DX = 5,
  ICON_DY = 5,
  PRICE_W = 85,
  PRICE_H = 22,
  PRICE_DX = -19,
  PRICE_DY = 44,
}

export enum Rows {
  HALF = 232,
  LABEL_W = 196,
  ROW_H = 36,
  LABEL_H = 30,
  ICON_DY = 2,
  LABEL_DX = 36,
  LABEL_DY = 1,
  BACKING_W = 464,
  BAND_OPAQUE = 0,
  BAND_FADED = 125,
  BAND_PERIOD = 4,
  BAND_HALF = 2,
}

export enum Icon {
  W = 36,
  H = 32,
  SHADOW = 0x333333,
  OUTLINE = 1,
}

export enum CellFrame {
  INNER = 0x5a5245,
  OUTER = 0x383023,
}

export enum PriceColors {
  LABEL = 0xff981f,
  LABEL_HOVER = 0xffffff,
}

export const TOGGLE_OP = 1;
