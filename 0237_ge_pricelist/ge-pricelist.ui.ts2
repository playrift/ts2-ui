import { createRef } from 'ts2';

import { GePricelist } from './ge-pricelist.component';
import { Layout } from './ge-pricelist.constants';

const gePricelistRef = createRef('ge_pricelist');

export const gePricelistUi = (): UI => (
  <ui ref={gePricelistRef} x={Layout.ROOT_X} y={Layout.ROOT_Y} w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <GePricelist />
  </ui>
);
