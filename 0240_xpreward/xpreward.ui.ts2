import { createRef } from 'ts2';
import { XpReward } from './xpreward.component';
import { Layout } from './xpreward.constants';

const xpRewardRef = createRef('xpreward');

export const xpRewardUi = (): UI => (
  <ui ref={xpRewardRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <XpReward />
  </ui>
);
