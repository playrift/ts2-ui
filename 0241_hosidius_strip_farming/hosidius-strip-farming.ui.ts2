import { createRef } from 'ts2';
import { HosidiusStripFarming_ } from './hosidius-strip-farming.component';

const hosidiusStripFarmingRef = createRef('hosidius_strip_farming');

export const hosidiusStripFarmingUi = (): UI => (
  <ui ref={hosidiusStripFarmingRef}>
    <HosidiusStripFarming_ />
  </ui>
);
