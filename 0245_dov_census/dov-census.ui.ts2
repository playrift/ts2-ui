import { createRef } from 'ts2';

import { DovCensus } from './dov-census.component';

const dovCensusRef = createRef('dov_census');

export const dovCensusUi = (): UI => (
  <ui ref={dovCensusRef} x="center" y="center">
    <DovCensus />
  </ui>
);
