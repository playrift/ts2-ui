import { createRef } from 'ts2';
import { AreaTask } from './area-task.component';

const areaTaskRef = createRef('area_task');

export const areaTaskUi = (): UI => (
  <ui ref={areaTaskRef}>
    <AreaTask />
  </ui>
);
