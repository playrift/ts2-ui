import { createRef } from 'ts2';
import { GuideFlourDisplay } from './guide-flour-display.component';
import { Layout } from './guide-flour-display.constants';

const guideFlourDisplayRef = createRef('100guide_flour_display');

export const guideFlourDisplayUi = (): UI => (
  <ui ref={guideFlourDisplayRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <GuideFlourDisplay />
  </ui>
);
