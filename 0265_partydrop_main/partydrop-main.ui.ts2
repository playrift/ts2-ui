import { createRef } from 'ts2';
import { PartydropMain } from './partydrop-main.component';
import { Layout } from './partydrop-main.constants';

const partydropMainRef = createRef('partydrop_main');

export const partydropMainUi = (): UI => (
  <ui ref={partydropMainRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <PartydropMain />
  </ui>
);
