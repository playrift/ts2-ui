import { Inventories } from '../lib/game/inventories';
import { Varbits } from '../lib/game/varbits';

export declare const PartyDrop: {
  readonly INV: typeof Inventories.INV;
};

export declare const PartyDropVarbits: {
  readonly QUANTITY: typeof Varbits.FARMING_TOOLS_SELECTEDQUANTITY;
};

export enum Quantity {
  ONE = 0,
  FIVE = 1,
  ALL = 2,
  X = 3,
}

export enum Grid {
  W = 36,
  H = 32,
  COLUMNS = 4,
  ORIGIN_X = 16,
  ORIGIN_Y = 8,
  STRIDE_X = 42,
  STRIDE_Y = 36,
  OUTLINE = 1,
  DRAG_DEAD = 5,
}

export enum PartyDropColor {
  SLOT_SHADOW = 0x333333,
}

export enum Transparency {
  FILLED = 0,
  BLANK = 255,
  FLASH = 120,
}

export const BLANK_ITEM = 6512;

export enum Root {
  W = 190,
  H = 261,
}
