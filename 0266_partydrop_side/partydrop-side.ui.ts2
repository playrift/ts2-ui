import { createRef } from 'ts2';
import { PartydropSide } from './partydrop-side.component';
import { Root } from './partydrop-side.constants';

const partydropSideRef = createRef('partydrop_side');

export const partydropSideUi = (): UI => (
  <ui ref={partydropSideRef} w={Root.W} h={Root.H} x="center" y="center">
    <PartydropSide />
  </ui>
);
