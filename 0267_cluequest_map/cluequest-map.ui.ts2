import { createRef } from 'ts2';
import { CluequestMap } from './cluequest-map.component';

const cluequestMapRef = createRef('cluequest_map');

export const cluequestMapUi = (): UI => (
  <ui ref={cluequestMapRef}>
    <CluequestMap />
  </ui>
);
