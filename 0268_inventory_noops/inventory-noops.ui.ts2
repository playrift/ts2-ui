import { createRef } from 'ts2';
import { InventoryNoops } from './inventory-noops.component';
import { InventoryLayout } from './inventory-noops.constants';

const inventoryNoopsRef = createRef('inventory_noops');

export const inventoryNoopsUi = (): UI => (
  <ui ref={inventoryNoopsRef} x="center" y="center" w={InventoryLayout.ROOT_W} h={InventoryLayout.ROOT_H}>
    <InventoryNoops />
  </ui>
);
