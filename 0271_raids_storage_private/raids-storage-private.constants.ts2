import { Fonts } from '../lib/game/fonts';
import { Inventories } from '../lib/game/inventories';
import { Sounds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';
import { Varbits } from '../lib/game/varbits';

export declare const PrivateInventory: {
  readonly STORAGE: typeof Inventories.RAIDS_PRIVATESTORAGE;
};

export declare const PrivateVarbits: {
  readonly IN_DUNGEON: typeof Varbits.RAIDS_CLIENT_INDUNGEON;
};

export declare const StorageFonts: {
  readonly SMALL: typeof Fonts.P11_FULL;
  readonly BODY: typeof Fonts.P12_FULL;
};

export declare const StorageSounds: {
  readonly SELECT: typeof Sounds.INTERFACE_SELECT1;
};

export declare const StorageSprites: {
  readonly DIVIDER: typeof Sprites.MISCGRAPHICS_3;
  readonly BAR_TRACK: typeof Sprites.SCROLLBAR_DRAGGER_V2_3;
  readonly BAR_CAP_TOP: typeof Sprites.SCROLLBAR_DRAGGER_V2_0;
  readonly BAR_THUMB: typeof Sprites.SCROLLBAR_DRAGGER_V2_1;
  readonly BAR_CAP_BOTTOM: typeof Sprites.SCROLLBAR_DRAGGER_V2_2;
  readonly BAR_ARROW_UP: typeof Sprites.SCROLLBAR_V2_0;
  readonly BAR_ARROW_DOWN: typeof Sprites.SCROLLBAR_V2_1;
};

export enum StorageItems {
  BLANK = 6512,
}

export enum StorageColors {
  LABEL = 0xff981f,
  WHITE = 0xffffff,
  SLOT_SHADOW = 0x333333,
}

export enum Transparency {
  CLEAR = 0,
  FLASH = 125,
  EMPTY = 255,
}

export const PRESS_CYCLES = 20;

export enum Layout {
  ROOT_W = 380,
  ROOT_H = 300,
  COUNT_X = 3,
  COUNT_Y = 6,
  COUNT_W = 32,
  COUNT_H = 24,
  RULE_X = 13,
  RULE_Y = 17,
  RULE_W = 12,
  RULE_WIDTH = 1,
  TAB_X = 38,
  TAB_Y = 7,
  TAB_W = 80,
  TAB_H = 21,
  PANE_X = 10,
  PANE_Y = 40,
  PANE_W_INSET = 36,
  PANE_H_INSET = 115,
  BAR_X = 10,
  BAR_W = 16,
  DIVIDER_Y = 210,
  DIVIDER_H = 36,
  DIVIDER_W_INSET = 2,
  BUTTON_X = 30,
  BUTTON_Y = 45,
  BUTTON_W = 150,
  BUTTON_H = 22,
  WARNING_Y = 10,
  WARNING_W_INSET = 20,
  WARNING_H = 35,
}

export enum Grid {
  W = 36,
  H = 32,
  COLUMNS = 8,
  STRIDE_X = 44,
  STRIDE_Y = 40,
  DRAG_DEAD = 5,
}

export enum DragScroll {
  STEP = 5,
  STEP_NEAR = 8,
  STEP_PAST = 10,
  TOP_BAND = 15,
  TOP_URGENT = 5,
  BOTTOM_BAND = 20,
  BOTTOM_NEAR = 5,
  BOTTOM_PAST = -5,
}
