import { createRef } from 'ts2';
import { RaidsStoragePrivate } from './raids-storage-private.component';
import { Layout } from './raids-storage-private.constants';

const raidsStoragePrivateRef = createRef('raids_storage_private');

export const raidsStoragePrivateUi = (): UI => (
  <ui ref={raidsStoragePrivateRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <RaidsStoragePrivate />
  </ui>
);
