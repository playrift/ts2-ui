import { Enums } from '../lib/game/enums';
import { Fonts } from '../lib/game/fonts';
import { Varbits } from '../lib/game/varbits';

export declare const TrekVarbits: {
  readonly SELECTED: typeof Varbits.TEMPLETREK_REWARD_SELECTED;
};

export declare const RewardItems: typeof Enums.ENUM_1382;

export declare const RewardNames: typeof Enums.ENUM_1383;

export declare const TrekFonts: {
  readonly TITLE: typeof Fonts.B12_FULL;
  readonly BODY: typeof Fonts.P12_FULL;
};

export enum TrekColors {
  TEXT = 0xff981f,
  LABEL_HOVER = 0xfcd393,
  DIVIDER = 0x333300,
}

export const Layout = {
  ROOT_INSET_W: 20,
  ROOT_INSET_H: 40,
  BODY_Y: 35,
  BODY_INSET_W: 10,
  BODY_INSET_H: 40,
  DIVIDER_RIGHT: 121,
  DIVIDER_WIDTH: 1,
  CHOOSER_INSET_W: 121,
  DETAIL_W: 120,
  TITLE_Y: 5,
  TITLE_H: 20,
  TITLE_INSET_W: 10,
  DESC_Y: 29,
  DESC_INSET_W: 10,
  DESC_INSET_H: 62,
  DESC_LINE_HEIGHT: 18,
  CLAIM_BOTTOM: 5,
  CLAIM_H: 25,
  CLAIM_INSET_W: 10,
};

export const Grid = {
  REWARDS: 9,
  COLUMNS: 3,
  ICON_W: 36,
  ICON_H: 32,
  LABEL_W: 56,
  LABEL_H: 62,
  LABEL_OVERHANG: 10,
  ROW0_Y: 20,
  ROW_PITCH: 80,
};

export enum Outline {
  SELECTED = 2,
  REST = 0,
}

export enum Fade {
  ICON_HOVER = 100,
  ICON_REST = 0,
}
