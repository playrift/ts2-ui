import { createRef } from 'ts2';
import { TrekRewards } from './trek-rewards.component';

const trekRewardsRef = createRef('trek_rewards');

export const trekRewardsUi = (): UI => (
  <ui ref={trekRewardsRef}>
    <TrekRewards />
  </ui>
);
