import { createRef } from 'ts2';
import { TogSidepanel } from './tog-sidepanel.component';

const togSidepanelRef = createRef('tog_sidepanel');

export const togSidepanelUi = (): UI => (
  <ui ref={togSidepanelRef}>
    <TogSidepanel />
  </ui>
);
