import { createRef } from 'ts2';
import { MacroEvilTwin } from './macro-evil-twin.component';
import { TwinLayout } from './macro-evil-twin.constants';

const macroEvilTwinRef = createRef('macro_evil_twin');

export const macroEvilTwinUi = (): UI => (
  <ui ref={macroEvilTwinRef} x={TwinLayout.ROOT.X} y={TwinLayout.ROOT.Y} w={TwinLayout.ROOT.W} h={TwinLayout.ROOT.H}>
    <MacroEvilTwin />
  </ui>
);
