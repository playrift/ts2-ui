export { LoadingRingItems, SineDegrees, CosineDegrees } from './tables.data';

import { Sprites } from '../lib/game/sprites';

export declare const CloseSprites: {
  readonly OUT: typeof Sprites.CLOSE_BUTTONS_4;
  readonly OVER: typeof Sprites.CLOSE_BUTTONS_5;
};

export declare const UpgradeSprites: {
  readonly OUT: typeof Sprites.UPGRADE_MEMBERSHIP_BUTTON;
  readonly OVER: typeof Sprites.UPGRADE_MEMBERSHIP_BUTTON_HIGHLIGHT;
};

export enum CrmColors {
  DIM = 0xffffff,
}

export enum Layout {
  PANEL_X = 7,
  PANEL_Y = 7,
  PANEL_WIDTH = 506,
  PANEL_HEIGHT = 129,
  BACKDROP_WIDTH = 506,
  BACKDROP_HEIGHT = 129,
  UPGRADE_BOTTOM = 20,
  UPGRADE_WIDTH = 153,
  UPGRADE_HEIGHT = 32,
  CLOSE_X = 473,
  CLOSE_Y = 2,
  CLOSE_HIT = 32,
  CLOSE_WIDTH = 26,
  CLOSE_HEIGHT = 23,
}

export enum Dimmer {
  IDLE = 255,
  HOVER = 230,
}

export enum Ops {
  UPGRADE_SOUND_MASK = 2,
}

export enum LoadingIcon {
  ITEMS = 13,
  DEGREES_PER_ITEM = 27,
  DEGREES_SPARE = 9,
  RADIUS = 3,
  TRIG_UNIT = 100000,
  ZOOM_MIN = 1000,
  ZOOM_MAX = 3000,
  ZOOM_STEP = 153,
  ZOOM_PER_CYCLE = 30,
  PITCH = 512,
  YAW = 1024,
  ROLL = 0,
  STACK = 1,
  SIZE_DIVISOR = 4,
}
