import { createRef } from 'ts2';
import { RdCombolock } from './rd-combolock.component';

const rdCombolockRef = createRef('rd_combolock');

export const rdCombolockUi = (): UI => (
  <ui ref={rdCombolockRef}>
    <RdCombolock />
  </ui>
);
