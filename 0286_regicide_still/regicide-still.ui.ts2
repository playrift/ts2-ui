import { createRef } from 'ts2';
import { RegicideStill } from './regicide-still.component';

const regicideStillRef = createRef('regicide_still');

export const regicideStillUi = (): UI => (
  <ui ref={regicideStillRef}>
    <RegicideStill />
  </ui>
);
