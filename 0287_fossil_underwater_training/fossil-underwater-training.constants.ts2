import { Fonts } from '../lib/game/fonts';
import { Inventories } from '../lib/game/inventories';
import { Sounds as GameSounds } from '../lib/game/sounds';
import { Varps } from '../lib/game/varps';

export declare const TrainingVarps: {
  readonly AGILITY_XP: typeof Varps.IF1;
  readonly THIEVING_XP: typeof Varps.IF2;
  readonly BOTH_AGILITY_XP: typeof Varps.IF3;
  readonly BOTH_THIEVING_XP: typeof Varps.IF4;
};

export declare const TrainingInventory: {
  readonly CARRIED: typeof Inventories.INV;
};

export declare const TrainingFont: {
  readonly CAPTION: typeof Fonts.B12_FULL;
  readonly DETAIL: typeof Fonts.P11_FULL;
  readonly COUNTER: typeof Fonts.P12_FULL;
};

export declare const TrainingSound: {
  readonly CLICK: typeof GameSounds.INTERFACE_SELECT1;
};

export enum TrainingItem {
  GLISTENING_TEAR = 22207,
}

export enum TrainingModel {
  AGILITY = 3328,
  THIEVING = 2500,
  AGILITY_ZOOM = 524,
  AGILITY_PITCH = 512,
  THIEVING_ZOOM = 120,
  THIEVING_PITCH = 520,
  BOTH_AGILITY_ZOOM = 700,
  BOTH_THIEVING_ZOOM = 200,
}

export enum TrainingColor {
  TEXT = 0xff981f,
  ICON_SHADOW = 0x333333,
}

export enum Panel {
  WIDTH = 350,
  HEIGHT = 235,
}

export enum Band {
  TOP = 40,
  WIDTH_INSET = 20,
  HEIGHT_INSET = 85,
  BUTTON_WIDTH = 108,
  MODEL_WIDTH = 108,
  MODEL_HEIGHT = 100,
}

export enum Caption {
  TOP = 5,
  HEIGHT = 20,
  DETAIL_BOTTOM = 5,
  DETAIL_HEIGHT = 25,
}

export enum Counter {
  BOTTOM = 10,
  WIDTH_INSET = 20,
  HEIGHT = 35,
  ICON_WIDTH = 36,
  ICON_HEIGHT = 32,
  GAP = 3,
}

export const EXCHANGE_CAP = 700;

export const WAIT_CYCLES = 20;

export enum Exchange {
  AGILITY = 1,
  THIEVING = 2,
  BOTH = 3,
}
