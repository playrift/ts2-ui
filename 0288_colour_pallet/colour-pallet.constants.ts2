import { createRef } from 'ts2';

export const colourPalletRef = createRef('colour_pallet');

export const CLAMP = createRef();
export const FADER = createRef();

export enum Layout {
  CLAMP_BOTTOM = 6,
  CLAMP_INSET_X = 12,
  CLAMP_INSET_Y = 41,
  WINDOW_W = 400,
  WINDOW_H = 260,
  BOX_Y = 40,
  BOX_INSET_W = 20,
  BOX_INSET_H = 80,
  SWATCHES_X = 3,
  SWATCHES_Y = 1,
  SWATCHES_INSET_W = 123,
  SWATCHES_H = 89,
  INPUT_X = 260,
  INPUT_INSET_W = 260,
  GRID_X = 2,
  GRID_BOTTOM = 2,
  GRID_INSET_W = 4,
  GRID_INSET_H = 94,
  BAR_W = 16,
  BUTTON_DX = 50,
  BUTTON_BOTTOM = 10,
  BUTTON_W = 80,
  BUTTON_H = 26,
  BIG_SWATCH = 45,
  CURRENT_SWATCH_X = 45,
  NEW_SWATCH_X = 164,
  BIG_SWATCH_Y = 22,
  CAPTION_W = 100,
  CAPTION_H = 18,
  CURRENT_CAPTION_X = 18,
  NEW_CAPTION_X = 137,
  HEX_W = 80,
  CURRENT_HEX_X = 27,
  NEW_HEX_X = 147,
  HEX_BOTTOM = 2,
  CUSTOM_CAPTION_X = 10,
  FIELD_LABEL_H = 12,
  CELL = 26,
  CELL_GAP = 1,
}

export enum PalletFonts {
  SMALL = 494,
  BODY = 495,
  BOLD = 496,
}

export enum PalletColour {
  AMBER = 0xff981f,
  BLACK = 0x000000,
  WHITE = 0xffffff,
  OUTLINE_DARK = 0x0e0e0c,
  OUTLINE_LIGHT = 0x474745,
}

export const FIELD_BACKING = 297;

export const SELECT_SOUND = 2266;

export enum PalletScrollbar {
  TRACK = 792,
  CAP_TOP = 789,
  THUMB = 790,
  CAP_BOTTOM = 791,
  ARROW_UP = 773,
  ARROW_DOWN = 788,
}

export enum Fade {
  CLEAR = 255,
  DIM = 150,
  CYCLES = 30,
}

export const COLOUR_KIND = 9;
export const NO_COLOUR = 2147483647;
export const MAX_COLOUR = 16777215;
export const PICK_FLASH_TRANSPARENCY = 200;
export const PICK_FLASH_CYCLES = 20;
export const FIELD_VEIL_HOVER = 220;
export const CELL_VEIL = 250;
export const CELL_VEIL_HOVER = 220;

export const CustomFields = [
  {
    label: 'Hexadecimal',
    x: 20,
    y: 33,
    w: 80,
    h: 18,
    prompt: 'Enter a hex code: (0-9, a-f, format: #RRGGBB)',
    mode: 22,
  },
  { label: 'Red', x: 7, y: 67, w: 32, h: 18, prompt: 'Enter a value for red (0-255)', mode: 23 },
  { label: 'Green', x: 44, y: 67, w: 32, h: 18, prompt: 'Enter a value for green (0-255)', mode: 24 },
  { label: 'Blue', x: 81, y: 67, w: 32, h: 18, prompt: 'Enter a value for blue (0-255)', mode: 25 },
] as const;

export const HEX_MODE = 22;

export const CANCEL_LABEL = '<col=ff981f>Cancel</col>';
export const SAVE_LABEL = '<col=0dc10d>Save</col>';
