import { createRef } from 'ts2';
import { PopupOverlay } from './popupoverlay.component';

const popupOverlayRef = createRef('popupoverlay');

export const popupOverlayUi = (): UI => (
  <ui ref={popupOverlayRef} x="center" y="center">
    <PopupOverlay />
  </ui>
);
