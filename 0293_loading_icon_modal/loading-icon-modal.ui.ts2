import { createRef } from 'ts2';
import { LoadingIconModal } from './loading-icon-modal.component';

const loadingIconModal = createRef('loading_icon_modal');

export const loadingIconModalUi = (): UI => (
  <ui ref={loadingIconModal}>
    <LoadingIconModal />
  </ui>
);
