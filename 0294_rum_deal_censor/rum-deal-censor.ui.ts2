import { createRef } from 'ts2';
import { RumDealCensor } from './rum-deal-censor.component';

const rumDealCensorRef = createRef('rum_deal_censor');

export const rumDealCensorUi = (): UI => (
  <ui ref={rumDealCensorRef}>
    <RumDealCensor />
  </ui>
);
