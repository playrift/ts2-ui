import { createRef } from 'ts2';
import { RumDealTitle } from './rum-deal-title.component';

const rumDealTitleRef = createRef('rum_deal_title');

export const rumDealTitleUi = (): UI => (
  <ui ref={rumDealTitleRef}>
    <RumDealTitle />
  </ui>
);
