import { createRef } from 'ts2';
import { SandwichTray } from './sandwich-tray.component';

const sandwichTrayRef = createRef('sandwich_tray');

export const sandwichTrayUi = (): UI => (
  <ui ref={sandwichTrayRef}>
    <SandwichTray />
  </ui>
);
