import { createRef } from 'ts2';
import { SeerCombolock } from './seer-combolock.component';

const seerCombolockRef = createRef('seer_combolock');

export const seerCombolockUi = (): UI => (
  <ui ref={seerCombolockRef}>
    <SeerCombolock />
  </ui>
);
