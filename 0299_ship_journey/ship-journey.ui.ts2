import { createRef } from 'ts2';
import { ShipJourney } from './ship-journey.component';

const shipJourneyRef = createRef('ship_journey');

export const shipJourneyUi = (): UI => (
  <ui ref={shipJourneyRef}>
    <ShipJourney />
  </ui>
);
