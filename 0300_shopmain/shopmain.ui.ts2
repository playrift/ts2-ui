import { createRef } from 'ts2';
import { Shopmain } from './shopmain.component';
import { Layout } from './shopmain.constants';

const shopmainRef = createRef('shopmain');

export const shopmainUi = (): UI => (
  <ui ref={shopmainRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y={Layout.ROOT_Y}>
    <Shopmain />
  </ui>
);
