import { createRef } from 'ts2';

const shopside = createRef('shopside');

export function Shopside(): Component {
  return <layer x={16} y={8} w={162} h={250} />;
}

export const shopsideUi = (): UI => (
  <ui ref={shopside}>
    <Shopside />
  </ui>
);
