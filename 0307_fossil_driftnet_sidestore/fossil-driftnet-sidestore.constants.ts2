import { Inventories } from '../lib/game/inventories';

export declare const DriftnetStore: {
  readonly INV: typeof Inventories.INV;
};

export const DRIFT_NET = 21652;

export enum Grid {
  W = 36,
  H = 32,
  COLUMNS = 4,
  ORIGIN_X = 16,
  ORIGIN_Y = 8,
  STRIDE_X = 42,
  STRIDE_Y = 36,
  OUTLINE = 1,
}

export enum DriftnetColor {
  SLOT_SHADOW = 0x333333,
}

export enum Transparency {
  NET = 0,
  NET_FLASH = 100,
  OTHER = 130,
  OTHER_FLASH = 170,
}

export enum Root {
  W = 190,
  H = 261,
}
