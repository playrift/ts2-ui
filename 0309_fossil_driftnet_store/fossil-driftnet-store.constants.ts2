import { Animations } from '../lib/game/animations';
import { Varbits } from '../lib/game/varbits';

export declare const FossilVarbits: {
  readonly STORE: typeof Varbits.FOSSIL_DRIFTNET_STORE;
};

export declare const FossilAnim: {
  readonly CHAT_IDLE: typeof Animations.CHATIDLENEU1;
};

export enum FossilNpc {
  ANNETTE = 7510,
}

export enum FossilItem {
  DRIFT_NET = 21652,
}

export enum FossilModel {
  NET_RACK = 2626,
  RACK_ZOOM = 400,
  RACK_PITCH = 299,
  RACK_YAW = 1795,
  HEAD_ZOOM = 1200,
  HEAD_PITCH = 97,
  HEAD_YAW = 1924,
}

export enum Panel {
  WIDTH = 203,
  HEIGHT = 160,
}

export enum Content {
  BOTTOM = 10,
  WIDTH_INSET = 20,
  HEIGHT_INSET = 50,
  RACK_X = 46,
  HEAD_X = 38,
  MODEL_Y = 33,
  MODEL_SIZE = 32,
  SLOT_RIGHT = 23,
  SLOT_BOTTOM = 23,
  SLOT_WIDTH = 36,
  SLOT_HEIGHT = 32,
}

export enum Colors {
  SLOT_SHADOW = 0x333333,
}

export enum Transparency {
  DIM = 100,
  CLEAR = 0,
}

export const FADE_CYCLES = 10;
