import { createRef } from 'ts2';
import { FossilDriftnetStore } from './fossil-driftnet-store.component';
import { Panel } from './fossil-driftnet-store.constants';

const fossilDriftnetStoreRef = createRef('fossil_driftnet_store');

export const fossilDriftnetStoreUi = (): UI => (
  <ui ref={fossilDriftnetStoreRef} w={Panel.WIDTH} h={Panel.HEIGHT} x="center" y="center">
    <FossilDriftnetStore />
  </ui>
);
