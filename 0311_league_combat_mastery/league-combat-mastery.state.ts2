import { sessionState } from 'ts2';

export const detailMastery = sessionState(-1);

export const detailPassive = sessionState(-1);

export const masteryLoading = sessionState(0);

export const confirmOpen = sessionState(0);

export const extraOpen = sessionState(0);

export const masteryHover = sessionState(-1);

export const masteryTipIndex = sessionState(-1);
