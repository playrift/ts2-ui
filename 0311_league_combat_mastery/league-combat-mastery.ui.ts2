import { createRef } from 'ts2';
import { LeagueCombatMastery } from './league-combat-mastery.component';

const leagueCombatMasteryRef = createRef('league_combat_mastery');

export const leagueCombatMasteryUi = (): UI => (
  <ui ref={leagueCombatMasteryRef} x="center" y="center" w="fill" h="fill">
    <LeagueCombatMastery />
  </ui>
);
