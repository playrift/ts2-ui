import { createRef } from 'ts2';

import { Smithing } from './smithing.component';

const smithingRef = createRef('smithing');

export const smithingUi = (): UI => (
  <ui ref={smithingRef} x={6} y={7} w={500} h={320}>
    <Smithing />
  </ui>
);
