import { createRef } from 'ts2';
import { Smokeoverlay } from './smokeoverlay.component';

const smokeoverlayRef = createRef('smokeoverlay');

export const smokeoverlayUi = (): UI => (
  <ui ref={smokeoverlayRef}>
    <Smokeoverlay />
  </ui>
);
