import { createRef } from 'ts2';
import { CwsWarning } from './cws-warning.component';

const cwsWarning25 = createRef('cws_warning_25');

export const cwsWarning25Ui = (): UI => (
  <ui ref={cwsWarning25}>
    <CwsWarning
      message={25}
      body="This way leads into the Wilderness and is only<br>one-way! Are you sure you want to go down here?"
      yes="Yes, I'm fearless"
      no="Nope, that doesn't seem like a sensible<br>thing to do"
    />
  </ui>
);
