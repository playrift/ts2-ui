import { createRef } from 'ts2';
import { LightPuzzle } from './light-puzzle.component';

const lightPuzzleRef = createRef('light_puzzle');

export const lightPuzzleUi = (): UI => (
  <ui ref={lightPuzzleRef} w={322} x="center" y="center">
    <LightPuzzle />
  </ui>
);
