import { Fonts } from '../lib/game/fonts';
import { Inventories } from '../lib/game/inventories';
import { Sprites } from '../lib/game/sprites';

export enum Panel {
  WIDTH = 512,
  HEIGHT = 302,
  TOP = 18,
}

export enum Title {
  WIDTH = 232,
  HEIGHT = 18,
  DY = -126,
}

export const TITLE_TEXT = 'Tai Bwo Wannai Parcel Service';

export enum Ornament {
  X = 228,
  Y = 255,
  SIZE = 32,
  MODEL = 8949,
  ZOOM = 404,
  PITCH = 523,
}

export enum GridBox {
  X = 105,
  Y = 75,
  WIDTH = 294,
  HEIGHT = 155,
}

export enum CloseBox {
  X = 464,
  Y = 10,
  WIDTH = 26,
  HEIGHT = 23,
}

export enum Grid {
  SLOT_W = 36,
  SLOT_H = 32,
  COLUMNS = 7,
  STRIDE_X = 41,
  STRIDE_Y = 39,
  TOP = 4,
}

export enum ParcelColors {
  TITLE = 0xff981f,
  SLOT_SHADOW = 0x333333,
}

export enum SlotFade {
  HELD = 0,
  EMPTY = 255,
  PRESSED = 100,
}

export enum ParcelItems {
  BLANK = 6512,
}

export declare const ParcelInventories: {
  readonly INV: typeof Inventories.INV;
};

export declare const ParcelFonts: {
  readonly TITLE: typeof Fonts.Q8_FULL;
};

export declare const ParcelSprites: {
  readonly CLOSE: typeof Sprites.CLOSE_BUTTONS_6;
  readonly CLOSE_HOVER: typeof Sprites.CLOSE_BUTTONS_7;
};
