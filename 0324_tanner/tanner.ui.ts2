import { createRef } from 'ts2';
import { Tanner } from './tanner.component';

const tannerRef = createRef('tanner');

export const tannerUi = (): UI => (
  <ui ref={tannerRef}>
    <Tanner />
  </ui>
);
