import { createRef } from 'ts2';
import { Target } from './target.component';

const targetRef = createRef('target');

export const targetUi = (): UI => (
  <ui ref={targetRef}>
    <Target />
  </ui>
);
