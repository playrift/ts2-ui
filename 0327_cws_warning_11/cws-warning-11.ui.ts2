import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning11 = createRef('cws_warning_11');

export const cwsWarning11Ui = (): UI => (
  <ui ref={cwsWarning11}>
    <CwsWarning
      message={11}
      body="This area is so cold that you will experience<br>a constant, slow drain of your stats while<br>you remain within it."
      answers={false}
      again="Don't show me this again"
      bodyX={106}
      bodyY={102}
    />
  </ui>
);
