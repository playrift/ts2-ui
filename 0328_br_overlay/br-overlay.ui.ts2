import { createRef } from 'ts2';
import { BrOverlay } from './br-overlay.component';

const brOverlayRef = createRef('br_overlay');

export const brOverlayUi = (): UI => (
  <ui ref={brOverlayRef}>
    <BrOverlay />
  </ui>
);
