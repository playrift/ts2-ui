import { createRef } from 'ts2';
import { TempleTrekMap } from './templetrek-map.component';

const templetrekMapRef = createRef('templetrek_map');

export const templetrekMapUi = (): UI => (
  <ui ref={templetrekMapRef}>
    <TempleTrekMap />
  </ui>
);
