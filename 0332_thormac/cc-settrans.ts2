import { hideTooltip } from '../0128_hosidius_seedbox/hosidius-seedbox.state';

export function ccSetTrans(target: ComponentId, transparency: Int, tooltip: ComponentId = -1): void {
  target.transparency = transparency;
  if (tooltip !== -1) {
    hideTooltip();
  }
}
