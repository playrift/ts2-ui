import { sessionState, useState } from 'ts2';

export const brSettingsPressed = sessionState(0);

export function drawSettings(pressed: Int): void {
  const [, setPressed] = useState(brSettingsPressed);
  setPressed(pressed);
}
