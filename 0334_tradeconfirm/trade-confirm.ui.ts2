import { createRef } from 'ts2';
import { TradeConfirm } from './trade-confirm.component';

const tradeConfirmRef = createRef('tradeconfirm');

export const tradeConfirmUi = (): UI => (
  <ui ref={tradeConfirmRef}>
    <TradeConfirm />
  </ui>
);
