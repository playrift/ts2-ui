import { createRef } from 'ts2';

const tradeside = createRef('tradeside');

export function Tradeside(): Component {
  return <layer x={16} y={8} w={162} h={250} />;
}

export const tradesideUi = (): UI => (
  <ui ref={tradeside}>
    <Tradeside />
  </ui>
);
