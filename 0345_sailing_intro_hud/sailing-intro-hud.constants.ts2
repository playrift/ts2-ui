import { Fonts } from '../lib/game/fonts';
import { Sprites } from '../lib/game/sprites';

export declare const HintFonts: {
  readonly HINT: typeof Fonts.B12_FULL;
};

export declare const HintSprites: {
  readonly BACKING: typeof Sprites.TRADEBACKING_DARK;
};

export enum HintColor {
  WASH = 0x000000,
  BORDER = 0x5b3400,
  TEXT = 0xff981f,
}

export enum HintTransparency {
  BACKING = 80,
  WASH = 220,
}

export enum HintOverlay {
  DODGE_COMPACT = 52960,
  DODGE = 52968,
}

export enum HintWindow {
  FIXED = 1,
  NARROW_W = 600,
  SHORT_H = 400,
}

export const Layout = {
  HINT_W: 274,
  HINT_H: 96,
  HINT_MARGIN: 4,
  HINT_MARGIN_MOBILE: 10,

  PLATE_INSET: 2,

  LINE_INSET_W: 16,
  LINE_H: 48,
  LINE_HEIGHT: 17,
  LINE_1_Y: -20,
  LINE_2_Y: 21,
};
