import { createRef } from 'ts2';
import { SailingIntroHud } from './sailing-intro-hud.component';

const sailingIntroHudRef = createRef('sailing_intro_hud');

export const sailingIntroHudUi = (): UI => (
  <ui ref={sailingIntroHudRef}>
    <SailingIntroHud />
  </ui>
);
