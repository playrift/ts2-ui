import { createRef } from 'ts2';
import { TrawlerOverlay } from './trawler-overlay.component';

const trawlerOverlayRef = createRef('trawler_overlay');

export const trawlerOverlayUi = (): UI => (
  <ui ref={trawlerOverlayRef}>
    <TrawlerOverlay />
  </ui>
);
