import { createRef } from 'ts2';
import { TrawlerReward } from './trawler-reward.component';
import { Panel } from './trawler-reward.constants';

const trawlerRewardRef = createRef('trawler_reward');

export const trawlerRewardUi = (): UI => (
  <ui ref={trawlerRewardRef} w={Panel.WIDTH} h={Panel.HEIGHT} x="center" y="center">
    <TrawlerReward />
  </ui>
);
