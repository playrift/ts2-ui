import { createRef } from 'ts2';
import { TrawlerStart } from './trawler-start.component';

const trawlerStartRef = createRef('trawler_start');

export const trawlerStartUi = (): UI => (
  <ui ref={trawlerStartRef}>
    <TrawlerStart />
  </ui>
);
