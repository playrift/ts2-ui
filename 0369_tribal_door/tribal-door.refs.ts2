import { createRef } from 'ts2';

export const dialA = createRef('tribala');
export const dialB = createRef('tribalb');
export const dialC = createRef('tribalc');
export const dialD = createRef('tribald');
