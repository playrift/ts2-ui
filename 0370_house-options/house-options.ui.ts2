import { createRef } from 'ts2';
import { HouseOptions } from './house-options.component';

const houseOptionsRef = createRef('house-options');

export const houseOptionsUi = (): UI => (
  <ui ref={houseOptionsRef}>
    <HouseOptions />
  </ui>
);
