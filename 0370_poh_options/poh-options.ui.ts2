import { createRef } from 'ts2';
import { HouseOptions } from './poh-options.component';

const houseOptionsRef = createRef('poh_options');

export const houseOptionsUi = (): UI => (
  <ui ref={houseOptionsRef}>
    <HouseOptions />
  </ui>
);
