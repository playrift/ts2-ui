import { createRef } from 'ts2';
import { VikingMural } from './viking-mural.component';

const vikingMuralRef = createRef('viking_mural');

export const vikingMuralUi = (): UI => (
  <ui ref={vikingMuralRef}>
    <VikingMural />
  </ui>
);
