import { useServerState } from 'ts2';

import { VillageVarbits } from './village-community-score.constants';

export function showFavourPercent(target: ComponentId): void {
  target.text = `${stringOf(useServerState(VillageVarbits.FAVOUR_PERCENTAGE))} %`;
}
