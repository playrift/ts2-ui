import { createRef } from 'ts2';
import { WelcomeScreen } from './welcome-screen.component';

const welcomeScreenRef = createRef('welcome_screen');

export const welcomeScreenUi = (): UI => (
  <ui ref={welcomeScreenRef} x="center" y="center">
    <WelcomeScreen />
  </ui>
);
