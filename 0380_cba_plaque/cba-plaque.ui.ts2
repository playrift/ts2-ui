import { createRef } from 'ts2';
import { CbaPlaque } from './cba-plaque.component';

const cbaPlaqueRef = createRef('cba_plaque');

export const cbaPlaqueUi = (): UI => (
  <ui ref={cbaPlaqueRef}>
    <CbaPlaque />
  </ui>
);
