import { sessionState } from 'ts2';

export const fairyringsApplied = sessionState('');

export const fairyringsRevision = sessionState(0);

export const fairyringsSearchTimer = sessionState(0);

export const fairyringsExtent = sessionState(0);

export const fairyringsPaneHeight = sessionState(0);
