import { createRef } from 'ts2';
import { FairyringsLog } from './fairyrings-log.component';

const fairyringsLogRef = createRef('fairyrings_log');

export const fairyringsLogUi = (): UI => (
  <ui ref={fairyringsLogRef} w={190} h={261}>
    <FairyringsLog />
  </ui>
);
