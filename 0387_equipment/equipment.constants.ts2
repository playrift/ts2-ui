import { Enums as GameEnums } from '../lib/game/enums';
import { Inventories as GameInventories } from '../lib/game/inventories';

export const enum ActionIcons {
  EQUIPMENT_STATS = 675,
  GUIDE_PRICES = 1090,
  ITEMS_KEPT_ON_DEATH = 912,
  CALL_FOLLOWER = 1343,
}

export const enum SlotBackgroundSprites {
  NARROW = 172,
  WIDE = 173,
}

export const enum SlotSprites {
  PLATE = 170,
}

export declare const SlotIcons: typeof GameEnums.ENUM_904;

export const enum SlotColors {
  ITEM_SHADOW = 0x333333,
}

export declare const Inventories: {
  readonly WORN: typeof GameInventories.WORN;
};

export const enum EquipSlot {
  HEAD = 0,
  CAPE = 1,
  AMULET = 2,
  WEAPON = 3,
  BODY = 4,
  SHIELD = 5,
  LEGS = 7,
  HANDS = 9,
  FEET = 10,
  RING = 12,
  AMMO = 13,
}
