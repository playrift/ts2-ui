import { createRef } from 'ts2';
import { Equipment } from './equipment.component';

const equipmentRef = createRef('equipment');

export const equipmentUi = (): UI => (
  <ui ref={equipmentRef} w={190} h={261}>
    <Equipment />
  </ui>
);
