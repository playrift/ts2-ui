import { createRef } from 'ts2';
import { Equipment } from './wornitems.component';

const wornitemsRef = createRef('wornitems');

export const wornitemsUi = (): UI => (
  <ui ref={wornitemsRef} w={190} h={261}>
    <Equipment />
  </ui>
);
