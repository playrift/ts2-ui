import { createRef } from 'ts2';
import { BrReward } from './br-reward.component';
import { Layout } from './br-reward.constants';

const brRewardRef = createRef('br_reward');

export const brRewardUi = (): UI => (
  <ui ref={brRewardRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <BrReward />
  </ui>
);
