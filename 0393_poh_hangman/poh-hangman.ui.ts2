import { createRef } from 'ts2';
import { PohHangman } from './poh-hangman.component';

const pohHangmanRef = createRef('poh_hangman');

export const pohHangmanUi = (): UI => (
  <ui ref={pohHangmanRef}>
    <PohHangman />
  </ui>
);
