import { createRef } from 'ts2';
import { BrTournamentViewer } from './br-tournament-viewer.component';

const brTournamentViewerRef = createRef('br_tournament_viewer');

export const brTournamentViewerUi = (): UI => (
  <ui ref={brTournamentViewerRef} x="center" y="center">
    <BrTournamentViewer />
  </ui>
);
