import { createRef } from 'ts2';
import { WintStatus } from './wint-status.component';

const wintStatusRef = createRef('wint_status');

export const wintStatusUi = (): UI => (
  <ui ref={wintStatusRef}>
    <WintStatus />
  </ui>
);
