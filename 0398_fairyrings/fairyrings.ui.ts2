import { createRef } from 'ts2';
import { FairyRings } from './fairyrings.component';

const fairyRingsRef = createRef('fairyrings');

export const fairyRingsUi = (): UI => (
  <ui ref={fairyRingsRef}>
    <FairyRings />
  </ui>
);
