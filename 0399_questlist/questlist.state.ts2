import { sessionState } from 'ts2';

export const healthbarShown = sessionState(0);

export const healthbarCurrent = sessionState(0);

export const healthbarMax = sessionState(0);

export const healthbarAnim = sessionState(0);

export const healthbarBoundAt = sessionState(0);

export const healthbarOverlayPx = sessionState(0);

export const healthbarFillPx = sessionState(0);

export const healthbarBoxWidth = sessionState(0);

export const healthbarFrameLit = sessionState(0);
