import { Questlist } from './questlist.component';
import { questlistRef } from './questlist.constants';

export const questlistUi = (): UI => (
  <ui ref={questlistRef} x="center" y="center">
    <Questlist />
  </ui>
);
