import { createRef } from 'ts2';

import { TeletabsCraft } from './teletabs-craft-if.component';

const teletabsCraftIfRef = createRef('teletabs_craft_if');

export const teletabsCraftIfUi = (): UI => (
  <ui ref={teletabsCraftIfRef} x="center" y="center">
    <TeletabsCraft />
  </ui>
);
