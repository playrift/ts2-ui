import { createRef } from 'ts2';
import { PohScryingPool } from './poh-scrying-pool.component';

const pohScryingPoolRef = createRef('poh_scrying_pool');

export const pohScryingPoolUi = (): UI => (
  <ui ref={pohScryingPoolRef}>
    <PohScryingPool />
  </ui>
);
