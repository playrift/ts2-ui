import { createRef } from 'ts2';
import { PestStatusOverlay } from './pest-status-overlay.component';

const pestStatusOverlayRef = createRef('pest_status_overlay');

export const pestStatusOverlayUi = (): UI => (
  <ui ref={pestStatusOverlayRef}>
    <PestStatusOverlay />
  </ui>
);
