import { createRef } from 'ts2';
import { McannonInterface } from './mcannon-interface.component';

const mcannonInterfaceRef = createRef('mcannon_interface');

export const mcannonInterfaceUi = (): UI => (
  <ui ref={mcannonInterfaceRef}>
    <McannonInterface />
  </ui>
);
