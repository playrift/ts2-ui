import { createRef } from 'ts2';
import { WarguildDefence } from './warguild-defence.component';
import { Layout } from './warguild-defence.constants';

const warguildDefenceRef = createRef('warguild_defence');

export const warguildDefenceUi = (): UI => (
  <ui ref={warguildDefenceRef} x={Layout.ROOT_X} y={Layout.ROOT_Y} w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <WarguildDefence />
  </ui>
);
