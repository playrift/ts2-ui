import { createRef } from 'ts2';
import { WarguildDefenceMini } from './warguild-defence-mini.component';

const warguildDefenceMiniRef = createRef('warguild_defence_mini');

export const warguildDefenceMiniUi = (): UI => (
  <ui ref={warguildDefenceMiniRef} x={1} y={-1} w={183} h={261}>
    <WarguildDefenceMini />
  </ui>
);
