import { createRef } from 'ts2';
import { WarguildDummy } from './warguild-dummy.component';
import { Layout } from './warguild-dummy.constants';

const warguildDummyRef = createRef('warguild_dummy');

export const warguildDummyUi = (): UI => (
  <ui ref={warguildDummyRef} w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <WarguildDummy />
  </ui>
);
