import { createRef } from 'ts2';
import { NightmareTotems } from './nightmare-totems.component';

const nightmareTotemsRef = createRef('nightmare_totems');

export const nightmareTotemsUi = (): UI => (
  <ui ref={nightmareTotemsRef}>
    <NightmareTotems />
  </ui>
);
