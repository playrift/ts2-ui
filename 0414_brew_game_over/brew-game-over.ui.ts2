import { createRef } from 'ts2';
import { BrewGameOver } from './brew-game-over.component';

const brewGameOverRef = createRef('brew_game_over');

export const brewGameOverUi = (): UI => (
  <ui ref={brewGameOverRef}>
    <BrewGameOver />
  </ui>
);
