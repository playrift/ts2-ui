import { createRef } from 'ts2';
import { Canoeing } from './canoeing.component';

const canoeingRef = createRef('canoeing');

export const canoeingUi = (): UI => (
  <ui ref={canoeingRef}>
    <Canoeing />
  </ui>
);
