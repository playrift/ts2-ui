import { createRef } from 'ts2';
import { DeadmanTutorial } from './deadman-tutorial.component';

const deadmanTutorialRef = createRef('deadman_tutorial');

export const deadmanTutorialUi = (): UI => (
  <ui ref={deadmanTutorialRef}>
    <DeadmanTutorial />
  </ui>
);
