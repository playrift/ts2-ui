import { createRef } from 'ts2';
import { Beehive } from './beehive.component';

const beehiveRef = createRef('beehive');

export const beehiveUi = (): UI => (
  <ui ref={beehiveRef}>
    <Beehive />
  </ui>
);
