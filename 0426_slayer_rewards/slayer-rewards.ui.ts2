import { createRef } from 'ts2';
import { Panel } from './slayer-rewards.constants';
import { SlayerRewards } from './slayer-rewards.component';

const slayerRewardsRef = createRef('slayer_rewards');

export const slayerRewardsUi = (): UI => (
  <ui ref={slayerRewardsRef} x="center" y="center" w={Panel.W} h={Panel.H}>
    <SlayerRewards />
  </ui>
);
