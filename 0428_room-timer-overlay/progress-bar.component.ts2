import { CS2, useRef, useServerState } from 'ts2';
import { Colors, Varbits } from './room-timer-overlay.constants';
import { progressWidth } from './room-timer-overlay.helpers';

export function ProgressBar(): Component {
  const count = useServerState(Varbits.NTK_PLAYER_TIMER_COUNT);
  const anchor: Int = CS2.clientclock();
  const fill = useRef();

  return (
    <layer x={14} y={46} w={182} h={7}>
      <rect w="fill" h="fill" color={Colors.BAR_EMPTY} fill />
      <rect
        ref={fill}
        w={progressWidth(count, 0)}
        h="fill"
        color={Colors.BAR_FULL}
        fill
        onTimer={() => {
          fill.width = progressWidth(count, CS2.clientclock() - anchor);
        }}
      />
    </layer>
  );
}
