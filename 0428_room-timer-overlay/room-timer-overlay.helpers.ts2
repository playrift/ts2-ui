import { CS2 } from 'ts2';

export function progressWidth(count: Int, elapsedCycles: Int): Int {
  const value: Int = count + elapsedCycles / 30;
  const raw: Int = CS2.interpolate(0, value, 0, 500, 182);
  return Math.clamp(0, 182, raw);
}
