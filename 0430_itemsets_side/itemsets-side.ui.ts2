import { createRef } from 'ts2';
import { ItemsetsSide } from './itemsets-side.component';
import { Root } from './itemsets-side.constants';

const itemsetsSideRef = createRef('itemsets_side');

export const itemsetsSideUi = (): UI => (
  <ui ref={itemsetsSideRef} w={Root.W} h={Root.H}>
    <ItemsetsSide />
  </ui>
);
