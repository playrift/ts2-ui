import { createRef } from 'ts2';
import { SoulWarsLobby } from './soul-wars-lobby.component';

const soulWarsLobbyRef = createRef('soul_wars_lobby');

export const soulWarsLobbyUi = (): UI => (
  <ui ref={soulWarsLobbyRef}>
    <SoulWarsLobby />
  </ui>
);
