import { createRef } from 'ts2';
import { SoulWarsStats } from './soul-wars-stats.component';

const soulWarsStatsRef = createRef('soul_wars_stats');

export const soulWarsStatsUi = (): UI => (
  <ui ref={soulWarsStatsRef} x="center" y="center">
    <SoulWarsStats />
  </ui>
);
