import { createRef } from 'ts2';
import { TemporossHud } from './tempoross-hud.component';

const temporossHudRef = createRef('tempoross_hud');

export const temporossHudUi = (): UI => (
  <ui ref={temporossHudRef}>
    <TemporossHud />
  </ui>
);
