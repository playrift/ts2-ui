import { createRef } from 'ts2';
import { SleddingHud } from './sledding-hud.component';

const sleddingHudRef = createRef('sledding_hud');

export const sleddingHudUi = (): UI => (
  <ui ref={sleddingHudRef}>
    <SleddingHud />
  </ui>
);
