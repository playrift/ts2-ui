import { createRef } from 'ts2';
import { EyegloBattleMap } from './eyeglo-battlemap.component';

const eyegloBattleMapRef = createRef('eyeglo_battlemap');

export const eyegloBattleMapUi = (): UI => (
  <ui ref={eyegloBattleMapRef}>
    <EyegloBattleMap />
  </ui>
);
