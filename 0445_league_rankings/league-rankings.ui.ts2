import { createRef } from 'ts2';
import { LeagueRankings } from './league-rankings.component';

const leagueRankingsRef = createRef('league_rankings');

export const leagueRankingsUi = (): UI => (
  <ui ref={leagueRankingsRef}>
    <LeagueRankings />
  </ui>
);
