import { Layout, Leagues, LeagueParams, RankingColors, RankingFonts } from './league-rankings.constants';
import { RankingsDivider } from './rankings-divider.component';

export function LeagueSection({ top, league }: { top: Int; league: Int }): Component {
  return (
    <>
      <RankingsDivider y={top + Layout.DIVIDER_Y} />
      <text
        x={0}
        y={top + Layout.TITLE_Y}
        w="fill"
        h={Layout.TEXT_H}
        align="center"
        valign="middle"
        shadow
        color={RankingColors.TEXT}
        font={RankingFonts.TITLE}
        text={Leagues.get(league).text(LeagueParams.NAME)}
      />
      <RankingsDivider y={top + Layout.DIVIDER_2_Y} />
    </>
  );
}
