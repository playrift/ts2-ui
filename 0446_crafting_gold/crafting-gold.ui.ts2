import { createRef } from 'ts2';

import { CraftingGold } from './crafting-gold.component';
import { Panel } from './crafting-gold.constants';

const craftingGoldRef = createRef('crafting_gold');

export const craftingGoldUi = (): UI => (
  <ui ref={craftingGoldRef} x="center" y="center" w={Panel.W} h={Panel.H}>
    <CraftingGold />
  </ui>
);
