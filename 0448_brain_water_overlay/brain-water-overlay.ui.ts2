import { createRef } from 'ts2';
import { BrainWaterOverlay } from './brain-water-overlay.component';

const brainWaterOverlayRef = createRef('brain_water_overlay');

export const brainWaterOverlayUi = (): UI => (
  <ui ref={brainWaterOverlayRef}>
    <BrainWaterOverlay />
  </ui>
);
