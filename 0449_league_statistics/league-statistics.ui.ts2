import { createRef } from 'ts2';
import { LeagueStatistics } from './league-statistics.component';

const leagueStatisticsRef = createRef('league_statistics');

export const leagueStatisticsUi = (): UI => (
  <ui ref={leagueStatisticsRef} x="center" y="center">
    <LeagueStatistics />
  </ui>
);
