import { createRef } from 'ts2';
import { TobInfoboard } from './tob_infoboard.component';

const tobInfoboardRef = createRef('tob_infoboard');

export const tobInfoboardUi = (): UI => (
  <ui ref={tobInfoboardRef} x="center" y="center">
    <TobInfoboard />
  </ui>
);
