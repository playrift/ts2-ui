import { createRef } from 'ts2';
import { Slug2RessemblingTornPaper } from './slug2-ressembling-torn-paper.component';

const slug2RessemblingTornPaperRef = createRef('slug2_ressembling_torn_paper');

export const slug2RessemblingTornPaperUi = (): UI => (
  <ui ref={slug2RessemblingTornPaperRef}>
    <Slug2RessemblingTornPaper />
  </ui>
);
