import { createRef } from 'ts2';
import { Slug2TornPaper } from './slug2-ressembling-torn-paper-controll.component';

const slug2TornPaperRef = createRef('slug2_ressembling_torn_paper_controll');

export const slug2TornPaperUi = (): UI => (
  <ui ref={slug2TornPaperRef}>
    <Slug2TornPaper />
  </ui>
);
