import { Fonts } from '../lib/game/fonts';
import { Inventories } from '../lib/game/inventories';
import { Sprites } from '../lib/game/sprites';
import { Varbits } from '../lib/game/varbits';
import { Varps } from '../lib/game/varps';

export declare const SlugVarps: {
  readonly FRAGMENT_COMPLETE: typeof Varps.SLUG2_FRAGMENT_COMPLETE;
};

export declare const SlugVarbits: {
  readonly PIECE1_SHOW: typeof Varbits.SLUG2_PIECE1_SHOW;
  readonly PIECE1_SELECT: typeof Varbits.SLUG2_PIECE1_SELECT;
  readonly PIECE2_SHOW: typeof Varbits.SLUG2_PIECE2_SHOW;
  readonly PIECE2_SELECT: typeof Varbits.SLUG2_PIECE2_SELECT;
  readonly PIECE3_SHOW: typeof Varbits.SLUG2_PIECE3_SHOW;
  readonly PIECE3_SELECT: typeof Varbits.SLUG2_PIECE3_SELECT;
};

export declare const SlugInventory: {
  readonly INV: typeof Inventories.INV;
};

export declare const SlugFonts: {
  readonly HEADING: typeof Fonts.B12_FULL;
  readonly LABEL: typeof Fonts.P12_FULL;
};

export declare const SlugSprites: {
  readonly RADIO_OFF: typeof Sprites.OPTIONS_RADIO_BUTTONS_5;
  readonly RADIO_ON: typeof Sprites.OPTIONS_RADIO_BUTTONS_6;
};

export enum SlugItems {
  PIECE_1 = 9687,
  PIECE_2 = 9688,
  PIECE_3 = 9689,
}

export enum SlugModels {
  ROTATE = 18391,
  ARROW = 18389,
  FLIP = 18390,
}

export enum SlugColors {
  LABEL = 0xff981f,
}

export enum Layout {
  PANEL_WIDTH = 190,
  PANEL_HEIGHT = 261,

  BUTTON_SIZE = 32,
  BUTTON_ZOOM = 1000,
  BUTTON_PITCH = 500,
  FLIP_ZOOM = 1384,
  YAW_UP = 0,
  YAW_RIGHT = 500,
  YAW_DOWN = 1015,
  YAW_LEFT = 1530,

  BOX_X = 21,
  BOX_BOTTOM = 5,
  BOX_WIDTH = 153,
  BOX_HEIGHT = 116,
  INNER_INSET = 16,

  ROW_HEIGHT = 25,
  ROW_1_Y = 25,
  ROW_2_Y = 50,
  ROW_3_Y = 75,

  RADIO_SIZE = 17,
  SHOW_X = 13,
  SELECT_X = 50,
  SHOW_TOUCH_X = 5,
  SELECT_TOUCH_X = 42,
  TOUCH_WIDTH = 32,

  LABEL_INSET = 84,

  COMPLETE_LINE_HEIGHT = 20,
}
