import { createRef } from 'ts2';
import { Slug2TornPaper } from './slug2-torn-paper.component';

const slug2TornPaperRef = createRef('slug2_torn_paper');

export const slug2TornPaperUi = (): UI => (
  <ui ref={slug2TornPaperRef}>
    <Slug2TornPaper />
  </ui>
);
