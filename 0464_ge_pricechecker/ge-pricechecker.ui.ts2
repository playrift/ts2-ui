import { createRef } from 'ts2';
import { GePriceChecker } from './ge-pricechecker.component';
import { Layout } from './ge-pricechecker.constants';

const gePriceCheckerRef = createRef('ge_pricechecker');

export const gePriceCheckerUi = (): UI => (
  <ui ref={gePriceCheckerRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x={Layout.ROOT_X} y={Layout.ROOT_Y}>
    <GePriceChecker />
  </ui>
);
