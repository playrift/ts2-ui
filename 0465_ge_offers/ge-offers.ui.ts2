import { createRef } from 'ts2';

import { GeOffers } from './ge-offers.component';

const geOffersRef = createRef('ge_offers');

export const geOffersUi = (): UI => (
  <ui ref={geOffersRef}>
    <GeOffers />
  </ui>
);
