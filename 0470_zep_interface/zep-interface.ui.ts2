import { createRef } from 'ts2';
import { ZepInterface } from './zep-interface.component';

const zepInterfaceRef = createRef('zep_interface');

export const zepInterfaceUi = (): UI => (
  <ui ref={zepInterfaceRef}>
    <ZepInterface />
  </ui>
);
