import { Enums } from '../lib/game/enums';
import { Fonts } from '../lib/game/fonts';
import { Sounds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';
import { Varbits } from '../lib/game/varbits';

export declare const BrSettingsVars: {
  readonly PREFERRED_BUILD: typeof Varbits.BR_PREFERRED_BUILD;
};

export enum BrBuild {
  MAX_MED = 0,
  ZERKER = 1,
  PURE = 2,
}

export declare const BuildNames: typeof Enums.ENUM_4414;

export declare const SkillIcons: typeof Enums.ENUM_255;

export enum LmsSkill {
  ATTACK = 0,
  DEFENCE = 1,
  STRENGTH = 2,
  HITPOINTS = 3,
  RANGED = 4,
  MAGIC = 6,
}

export declare const BrSettingsArt: {
  readonly RADIO_ON: typeof Sprites.OPTIONS_RADIO_BUTTONS_2;
  readonly RADIO_OFF: typeof Sprites.OPTIONS_RADIO_BUTTONS_0;
  readonly BACKING: typeof Sprites.TRADEBACKING_LIGHT;
};

export declare const BrSettingsFonts: {
  readonly BODY: typeof Fonts.P12_FULL;
  readonly STATS: typeof Fonts.B12_FULL;
};

export declare const BrSettingsSounds: {
  readonly SELECT: typeof Sounds.INTERFACE_SELECT1;
};

export enum Palette {
  GOLD = 0xff981f,
  WHITE = 0xffffff,
}

export enum Fade {
  ROW_SHADED = 240,
  ROW_CLEAR = 255,
  ROW_HOVER = 220,
  DIVIDER = 240,
}

export const Layout = {
  ROOT_W: 410,
  ROOT_H: 242,
  CONTENT_INSET: 20,
  FRAME_INSET: 50,
  CONTENT_BOTTOM: 10,

  ROW_H: 25,
  HEADER_H: 25,
  ROWS: 3,

  BOX_H: 104,
  BOX_W: 274,
  GRID_INSET: 4,

  RADIO: 17,
  RADIO_DY: 4,
  LABEL_INSET: 22,

  COLUMN_X: 120,
  COLUMN_W: 25,

  PARA_LINE: 14,
  PARA_PAD: 2,
};

export const Copy = {
  INTRO:
    "Last Man Standing may be played on <col=ffb83f>different builds</col>, with different <col=ffb83f>stats and items</col> available for each. A build will be chosen at the <col=ffb83f>start of each game</col>, influenced by the participants' preferences.<br>Choose your favourite:",
  FOOTER: "Don't forget to visit the <col=ffb83f>supply chest</col> to customise your load-outs for each build.",
};
