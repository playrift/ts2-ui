import { createRef } from 'ts2';
import { BrSettings } from './br-settings.component';
import { Layout } from './br-settings.constants';

const brSettingsRef = createRef('br_settings');

export const brSettingsUi = (): UI => (
  <ui ref={brSettingsRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <BrSettings />
  </ui>
);
