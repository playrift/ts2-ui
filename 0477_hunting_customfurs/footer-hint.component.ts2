import { textDevice } from '../lib/lib.helpers';
import { Colors, Fonts } from './hunting-customfurs.constants';

export function FooterHint(): Component {
  return (
    <text
      w="fill"
      h="fill"
      text={`${textDevice('Right-click', 'Long press')} an item in the shop to have it made for you.`}
      font={Fonts.BODY}
      color={Colors.HINT}
      shadow
      align="center"
      valign="top"
    />
  );
}
