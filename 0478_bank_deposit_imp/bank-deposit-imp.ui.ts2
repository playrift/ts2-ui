import { createRef } from 'ts2';
import { BankDepositImp } from './bank-deposit-imp.component';
import { Panel } from './bank-deposit-imp.constants';

const bankDepositImpRef = createRef('bank_deposit_imp');

export const bankDepositImpUi = (): UI => (
  <ui ref={bankDepositImpRef} w={Panel.WIDTH} h={Panel.HEIGHT} x="center" y="center">
    <BankDepositImp />
  </ui>
);
