import { createRef } from 'ts2';
import { ToaHud } from './toa-hud.component';

const toaHudRef = createRef('toa_hud');

export const toaHudUi = (): UI => (
  <ui ref={toaHudRef}>
    <ToaHud />
  </ui>
);
