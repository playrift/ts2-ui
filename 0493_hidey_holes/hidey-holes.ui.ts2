import { createRef } from 'ts2';
import { HideyHoles } from './hidey-holes.component';

const hideyHolesRef = createRef('hidey_holes');

export const hideyHolesUi = (): UI => (
  <ui ref={hideyHolesRef} x="center" y="center">
    <HideyHoles />
  </ui>
);
