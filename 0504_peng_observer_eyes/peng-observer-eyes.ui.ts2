import { createRef } from 'ts2';
import { PengObserverEyes } from './peng-observer-eyes.component';

const pengObserverEyesRef = createRef('peng_observer_eyes');

export const pengObserverEyesUi = (): UI => (
  <ui ref={pengObserverEyesRef}>
    <PengObserverEyes />
  </ui>
);
