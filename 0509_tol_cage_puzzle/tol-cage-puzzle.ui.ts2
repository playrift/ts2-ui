import { createRef } from 'ts2';
import { TolCagePuzzle } from './tol-cage-puzzle.component';

const tolCagePuzzleRef = createRef('tol_cage_puzzle');

export const tolCagePuzzleUi = (): UI => (
  <ui ref={tolCagePuzzleRef}>
    <TolCagePuzzle />
  </ui>
);
