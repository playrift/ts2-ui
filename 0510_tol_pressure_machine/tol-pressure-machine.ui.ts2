import { createRef } from 'ts2';
import { TolPressureMachine } from './tol-pressure-machine.component';

const tolPressureMachineRef = createRef('tol_pressure_machine');

export const tolPressureMachineUi = (): UI => (
  <ui ref={tolPressureMachineRef}>
    <TolPressureMachine />
  </ui>
);
