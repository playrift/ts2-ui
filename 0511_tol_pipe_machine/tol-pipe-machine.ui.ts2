import { createRef } from 'ts2';
import { TolPipeMachine } from './tol-pipe-machine.component';

const tolPipeMachineRef = createRef('tol_pipe_machine');

export const tolPipeMachineUi = (): UI => (
  <ui ref={tolPipeMachineRef}>
    <TolPipeMachine />
  </ui>
);
