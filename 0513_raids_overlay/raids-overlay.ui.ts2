import { createRef } from 'ts2';
import { RaidsOverlay } from './raids-overlay.component';

const raidsOverlayRef = createRef('raids_overlay');

export const raidsOverlayUi = (): UI => (
  <ui ref={raidsOverlayRef}>
    <RaidsOverlay />
  </ui>
);
