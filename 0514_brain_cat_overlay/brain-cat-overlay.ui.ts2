import { createRef } from 'ts2';
import { BrainCatOverlay } from './brain-cat-overlay.component';

const brainCatOverlayRef = createRef('brain_cat_overlay');

export const brainCatOverlayUi = (): UI => (
  <ui ref={brainCatOverlayRef}>
    <BrainCatOverlay />
  </ui>
);
