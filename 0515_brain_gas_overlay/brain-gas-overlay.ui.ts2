import { createRef } from 'ts2';
import { BrainGasOverlay } from './brain-gas-overlay.component';

const brainGasOverlayRef = createRef('brain_gas_overlay');

export const brainGasOverlayUi = (): UI => (
  <ui ref={brainGasOverlayRef}>
    <BrainGasOverlay />
  </ui>
);
