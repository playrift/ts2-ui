import { createRef } from 'ts2';

import { MakeoverStage } from './makeover.component';

const makeoverRef = createRef('makeover');

export const makeoverUi = (): UI => (
  <ui ref={makeoverRef}>
    <MakeoverStage />
  </ui>
);
