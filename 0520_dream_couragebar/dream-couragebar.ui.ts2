import { createRef } from 'ts2';
import { DreamCouragebar } from './dream-couragebar.component';

const dreamCouragebarRef = createRef('dream_couragebar');

export const dreamCouragebarUi = (): UI => (
  <ui ref={dreamCouragebarRef}>
    <DreamCouragebar />
  </ui>
);
