import { createRef } from 'ts2';
import { DreamPlayerStats } from './dream-player-stats.component';

const dreamPlayerStats = createRef('dream_player_stats');

export const dreamPlayerStatsUi = (): UI => (
  <ui ref={dreamPlayerStats}>
    <DreamPlayerStats />
  </ui>
);
