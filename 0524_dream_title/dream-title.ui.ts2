import { createRef } from 'ts2';
import { DreamTitle } from './dream-title.component';

const dreamTitleRef = createRef('dream_title');

export const dreamTitleUi = (): UI => (
  <ui ref={dreamTitleRef}>
    <DreamTitle />
  </ui>
);
