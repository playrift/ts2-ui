import { createRef } from 'ts2';
import { AnimationPicker } from './animation-picker.component';

const animationPickerRef = createRef('animation_picker');

export const animationPickerUi = (): UI => (
  <ui ref={animationPickerRef}>
    <AnimationPicker />
  </ui>
);
