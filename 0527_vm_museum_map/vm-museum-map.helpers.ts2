export function compSetHide(hide: Int, component: ComponentId): void {
  if (hide === 1) {
    component.hide = true;
  } else {
    component.hide = false;
  }
}

export function textColourSwapper(component: ComponentId, colour: Int): void {
  component.color = colour;
}
