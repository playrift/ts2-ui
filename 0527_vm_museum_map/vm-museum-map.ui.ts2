import { createRef } from 'ts2';
import { VmMuseumMap } from './vm-museum-map.component';

const vmMuseumMapRef = createRef('vm_museum_map');

export const vmMuseumMapUi = (): UI => (
  <ui ref={vmMuseumMapRef}>
    <VmMuseumMap />
  </ui>
);
