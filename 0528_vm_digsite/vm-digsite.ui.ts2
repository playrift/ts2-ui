import { createRef } from 'ts2';
import { VmDigsite } from './vm-digsite.component';

const vmDigsiteRef = createRef('vm_digsite');

export const vmDigsiteUi = (): UI => (
  <ui ref={vmDigsiteRef}>
    <VmDigsite />
  </ui>
);
