import { createRef } from 'ts2';
import { LeagueSummary } from './league-summary.component';

const leagueSummaryRef = createRef('league_summary');

export const leagueSummaryUi = (): UI => (
  <ui ref={leagueSummaryRef} w={512} h={334}>
    <LeagueSummary />
  </ui>
);
