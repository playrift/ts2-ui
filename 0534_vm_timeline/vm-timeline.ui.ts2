import { createRef } from 'ts2';
import { VmTimeline } from './vm-timeline.component';

const vmTimelineRef = createRef('vm_timeline');

export const vmTimelineUi = (): UI => (
  <ui ref={vmTimelineRef}>
    <VmTimeline />
  </ui>
);
