import { createRef } from 'ts2';
import { GrimPiano } from './grim-piano.component';

const grimPianoRef = createRef('grim_piano');

export const grimPianoUi = (): UI => (
  <ui ref={grimPianoRef}>
    <GrimPiano />
  </ui>
);
