import { createRef } from 'ts2';
import { GrimRecipe } from './grim-recipe.component';

const grimRecipeRef = createRef('grim_recipe');

export const grimRecipeUi = (): UI => (
  <ui ref={grimRecipeRef}>
    <GrimRecipe />
  </ui>
);
