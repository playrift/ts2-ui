import { Inventories } from '../lib/game/inventories';
import { Sounds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';
import { Varbits } from '../lib/game/varbits';

export declare const RaidsRewards: {
  readonly INV: typeof Inventories.RAIDS_REWARDS;
};

export declare const RaidsVarbits: {
  readonly NEEDS_REWARD_BOOK: typeof Varbits.RAIDS_CLIENT_NEEDSREWARDBOOK;
};

export declare const RewardSounds: {
  readonly SELECT: typeof Sounds.INTERFACE_SELECT1;
};

export declare const ChestSprites: {
  readonly BUTTON: typeof Sprites.MISCGRAPHICS_0;
  readonly BANK_ICON: typeof Sprites.BANKBUTTONS_5;
  readonly INVENTORY_ICON: typeof Sprites.BANKBUTTONS_4;
  readonly DISCARD_ICON: typeof Sprites.BANKBUTTONS_6;
};

export enum RewardItems {
  BOOK = 20899,
}

export enum RewardModels {
  CHEST = 32506,
  CHEST_ZOOM = 1500,
  CHEST_YAW = 1536,
  CHEST_OFFSET_Y = 200,
  DECOR = 32756,
  DECOR_ZOOM = 500,
  DECOR_PITCH = 105,
  DECOR_YAW = 1891,
  DECOR_OFFSET_X = -5,
  DECOR_OFFSET_Y = 80,
}

export enum Panel {
  WIDTH = 260,
  HEIGHT = 220,
}

export enum Content {
  INSET_X = 20,
  INSET_Y = 50,
  BOTTOM = 10,
}

export enum Grid {
  BOX_X = 125,
  BOX_WIDTH = 85,
  BOX_Y_OFFSET = -20,
  BOX_HEIGHT_INSET = 40,
  SLOT_WIDTH = 36,
  SLOT_HEIGHT = 32,
  ROW_PITCH = 37,
  COLUMN_X = 46,
  FIRST_COLUMN_SLOTS = 3,
  SECOND_COLUMN_BIAS = 2,
}

export enum Colors {
  SLOT_SHADOW = 0x333333,
}

export enum Transparency {
  CLEAR = 0,
  FLASH = 120,
}

export const FADE_CYCLES = 10;

export enum ChestHighlight {
  HOVER = 100,
  IDLE = 0,
}

export enum ChestOpSound {
  BANK = 64,
  INVENTORY = 2,
  DISCARD = 2,
}

export enum SlotOp {
  TAKE = 1,
  EXAMINE = 10,
}
