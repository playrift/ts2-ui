import { onMobile } from '../lib/lib.helpers';

export function setTransparencyUnlessMobile(target: ComponentId, transparency: Int): void {
  if (!onMobile()) {
    target.transparency = transparency;
  }
}
