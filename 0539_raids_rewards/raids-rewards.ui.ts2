import { createRef } from 'ts2';
import { RaidsRewards } from './raids-rewards.component';
import { Panel } from './raids-rewards.constants';

const raidsRewardsRef = createRef('raids_rewards');

export const raidsRewardsUi = (): UI => (
  <ui ref={raidsRewardsRef} w={Panel.WIDTH} h={Panel.HEIGHT} x="center" y="center">
    <RaidsRewards />
  </ui>
);
