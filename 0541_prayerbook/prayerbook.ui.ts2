import { createRef } from 'ts2';
import { Prayerbook } from './prayerbook.component';
import { Layout } from './prayerbook.constants';

const prayerbookRef = createRef('prayerbook');

export const prayerbookUi = (): UI => (
  <ui ref={prayerbookRef} x="center" y="center" w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <Prayerbook />
  </ui>
);
