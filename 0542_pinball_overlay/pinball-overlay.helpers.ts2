import { useServerState } from 'ts2';

import { PinballOverlayVarbits } from './pinball-overlay.constants';

export function pinballScore(label: ComponentId): void {
  label.text = `Score: ${useServerState(PinballOverlayVarbits.SCORE)}`;
}
