import { createRef } from 'ts2';
import { PinballOverlay_ } from './pinball-overlay.component';

const pinballOverlayRef = createRef('pinball_overlay');

export const pinballOverlayUi = (): UI => (
  <ui ref={pinballOverlayRef}>
    <PinballOverlay_ />
  </ui>
);
