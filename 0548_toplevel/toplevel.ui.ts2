import { ToplevelFixed } from './toplevel.component';
import { universeRef } from './toplevel.constants';

export const toplevelUi = (): UI => (
  <ui ref={universeRef} x={0} y={0} w="fill" h="fill">
    <ToplevelFixed />
  </ui>
);
