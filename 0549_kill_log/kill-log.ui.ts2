import { createRef } from 'ts2';
import { KillLog } from './kill-log.component';

const killLogRef = createRef('kill_log');

export const killLogUi = (): UI => (
  <ui ref={killLogRef}>
    <KillLog />
  </ui>
);
