import { createRef } from 'ts2';
import { RaidsStorageShared } from './raids-storage-shared.component';
import { Layout } from './raids-storage-shared.constants';

const raidsStorageSharedRef = createRef('raids_storage_shared');

export const raidsStorageSharedUi = (): UI => (
  <ui ref={raidsStorageSharedRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <RaidsStorageShared />
  </ui>
);
