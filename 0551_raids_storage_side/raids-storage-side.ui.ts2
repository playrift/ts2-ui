import { createRef } from 'ts2';
import { RaidsStorageSide } from './raids-storage-side.component';

const raidsStorageSideRef = createRef('raids_storage_side');

export const raidsStorageSideUi = (): UI => (
  <ui ref={raidsStorageSideRef} x="center" y="center">
    <RaidsStorageSide />
  </ui>
);
