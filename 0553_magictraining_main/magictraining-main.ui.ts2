import { createRef } from 'ts2';
import { MagicTrainingMain_ } from './magictraining-main.component';

const magictrainingMainRef = createRef('magictraining_main');

export const magictrainingMainUi = (): UI => (
  <ui ref={magictrainingMainRef}>
    <MagicTrainingMain_ />
  </ui>
);
