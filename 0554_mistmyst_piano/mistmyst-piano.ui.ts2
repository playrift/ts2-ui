import { createRef } from 'ts2';
import { MistmystPiano } from './mistmyst-piano.component';

const mistmystPianoRef = createRef('mistmyst_piano');

export const mistmystPianoUi = (): UI => (
  <ui ref={mistmystPianoRef}>
    <MistmystPiano />
  </ui>
);
