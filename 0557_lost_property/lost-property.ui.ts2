import { createRef } from 'ts2';
import { LostProperty } from './lost-property.component';

const lostPropertyRef = createRef('lost_property');

export const lostPropertyUi = (): UI => (
  <ui ref={lostPropertyRef}>
    <LostProperty />
  </ui>
);
