import { createRef } from 'ts2';
import { PatternCards } from './pattern-cards.component';

const patternCardsRef = createRef('pattern_cards');

export const patternCardsUi = (): UI => (
  <ui ref={patternCardsRef}>
    <PatternCards />
  </ui>
);
