import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning21 = createRef('cws_warning_21');

export const cwsWarning21Ui = (): UI => (
  <ui ref={cwsWarning21}>
    <CwsWarning
      message={21}
      body="If you climb down this rope you will be attacked by<br>the Kalphite Queen, one of the most deadly creatures in RuneScape."
      yes="That's what I came here for"
      no="I think I'll stay out"
      yesOption="Go in, I don't mind dying!"
      noOption="No, I'm holding too much stuff!"
    />
  </ui>
);
