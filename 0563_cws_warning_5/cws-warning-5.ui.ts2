import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning5 = createRef('cws_warning_5');

export const cwsWarning5Ui = (): UI => (
  <ui ref={cwsWarning5}>
    <CwsWarning
      message={5}
      body="Entering building mode will destroy any items that are on the ground. Do you want to continue?"
      yes="Yes, I do not mind losing any items that are on the ground."
      no="No, I want to look around and pick things up first."
      yesH={27}
      rowW={312}
      rowH={22}
    />
  </ui>
);
