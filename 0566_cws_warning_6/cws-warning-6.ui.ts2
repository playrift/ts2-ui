import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning6 = createRef('cws_warning_6');

export const cwsWarning6Ui = (): UI => (
  <ui ref={cwsWarning6}>
    <CwsWarning
      message={6}
      body="Do you need to collect any dropped items?"
      yes="Yes - I need to collect some stuff<br>before I leave"
      no="No - I haven't left any valuables on the floor"
      yesH={27}
    />
  </ui>
);
