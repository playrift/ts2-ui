import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning14 = createRef('cws_warning_14');

export const cwsWarning14Ui = (): UI => (
  <ui ref={cwsWarning14}>
    <CwsWarning
      message={14}
      body="It's very dark in there. Are you sure you want to<br>go in without a light source?"
      yes="Yes I am sure."
      no="No, I am not properly equipped."
    />
  </ui>
);
