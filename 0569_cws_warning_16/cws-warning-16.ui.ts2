import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning16 = createRef('cws_warning_16');

export const cwsWarning16Ui = (): UI => (
  <ui ref={cwsWarning16}>
    <CwsWarning
      message={16}
      body="It's very dark in there.<br>Are you sure you want to go in without a light source?"
      yes="Yes I am sure."
      no="No, I am not properly equipped.."
    />
  </ui>
);
