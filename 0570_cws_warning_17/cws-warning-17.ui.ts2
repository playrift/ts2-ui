import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning17 = createRef('cws_warning_17');

export const cwsWarning17Ui = (): UI => (
  <ui ref={cwsWarning17}>
    <CwsWarning
      message={17}
      body="The cave is very dark and you don't have a light<br>source. Are you sure you want to go down?"
      yes="I'm NOT scared of the dark"
      no="I AM scared of the dark"
    />
  </ui>
);
