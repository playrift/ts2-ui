import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning13 = createRef('cws_warning_13');

export const cwsWarning13Ui = (): UI => (
  <ui ref={cwsWarning13}>
    <CwsWarning
      message={13}
      body="If your light source goes out down there you'll<br>be in trouble! Are you sure you want to go in<br>without a tinderbox?"
      yes="I'm NOT scared of the dark"
      no="I AM scared of the dark"
    />
  </ui>
);
