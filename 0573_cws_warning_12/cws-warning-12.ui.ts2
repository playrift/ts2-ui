import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning12 = createRef('cws_warning_12');

export const cwsWarning12Ui = (): UI => (
  <ui ref={cwsWarning12}>
    <CwsWarning
      message={12}
      body="You can run past, but keep in mind that a<br>dangerous area follows!"
      yes="I'm not scared"
      no="I think I'll stay here"
    />
  </ui>
);
