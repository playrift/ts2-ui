import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning1 = createRef('cws_warning_1');

export const cwsWarning1Ui = (): UI => (
  <ui ref={cwsWarning1}>
    <CwsWarning
      message={1}
      body="This area is very dangerous.<br>Are you sure you want to continue?"
      yes="Go in - I don't mind dying"
      no="No - I'm holding too much stuff"
    />
  </ui>
);
