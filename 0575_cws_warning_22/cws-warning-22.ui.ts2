import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning22 = createRef('cws_warning_22');

export const cwsWarning22Ui = (): UI => (
  <ui ref={cwsWarning22}>
    <CwsWarning
      message={22}
      body="If you pull this lever you will be attacked by the King Black Dragon, one of the most deadly creatures in RuneScape."
      yes="That's what I came here for"
      no="I think I'll stay out"
      yesOption="Go in, I don't mind dying!"
      noOption="No, I'm holding too much stuff!"
    />
  </ui>
);
