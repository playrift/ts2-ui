import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning8 = createRef('cws_warning_8');

export const cwsWarning8Ui = (): UI => (
  <ui ref={cwsWarning8}>
    <CwsWarning
      message={8}
      body="This way leads into the Wilderness and is only<br>one-way! Are you sure you want to go down here?"
      yes="Yes - I'm fearless"
      no="No - That doesn't seem like a sensible<br>thing to do"
      yesH={27}
      noH={28}
    />
  </ui>
);
