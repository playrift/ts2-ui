import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning18 = createRef('cws_warning_18');

export const cwsWarning18Ui = (): UI => (
  <ui ref={cwsWarning18}>
    <CwsWarning
      message={18}
      body="The tunnel is very dark. Are you sure you want to<br>go down without a light source?"
      yes="Yes I am sure."
      no="No, I am not properly equipped."
    />
  </ui>
);
