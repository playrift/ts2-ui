import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning15 = createRef('cws_warning_15');

export const cwsWarning15Ui = (): UI => (
  <ui ref={cwsWarning15}>
    <CwsWarning
      message={15}
      body="This fairy ring code leads to a dark area. Are you<br>sure you want to go there without a light source?"
      yes="Yes I am sure."
      no="No, I am not properly equipped."
    />
  </ui>
);
