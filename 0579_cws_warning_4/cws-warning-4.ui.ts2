import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning4 = createRef('cws_warning_4');

export const cwsWarning4Ui = (): UI => (
  <ui ref={cwsWarning4}>
    <CwsWarning
      message={4}
      body="Are you sure you want to climb down?"
      yes="Yes - I know that it may be<br>dangerous down there"
      no="No thanks - I don't want to die"
      yesH={27}
    />
  </ui>
);
