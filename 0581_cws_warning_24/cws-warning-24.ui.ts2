import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const cwsWarning24 = createRef('cws_warning_24');

export const cwsWarning24Ui = (): UI => (
  <ui ref={cwsWarning24}>
    <CwsWarning
      message={24}
      body="Danger! Death Plateau is not named that for<br>a joke! If you continue down this path you will<br>be assailed by trolls! If you are not prepared<br>for that you should turn back now!"
      answers={false}
      again="Don't show me this again"
      bodyX={107}
      bodyY={104}
    />
  </ui>
);
