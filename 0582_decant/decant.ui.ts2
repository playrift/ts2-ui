import { createRef } from 'ts2';
import { Decant } from './decant.component';

const decantRef = createRef('decant');

export const decantUi = (): UI => (
  <ui ref={decantRef}>
    <Decant />
  </ui>
);
