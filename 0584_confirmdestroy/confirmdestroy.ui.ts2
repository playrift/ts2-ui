import { createRef } from 'ts2';
import { ConfirmDestroy } from './confirmdestroy.component';

const confirmdestroyRef = createRef('confirmdestroy');

export const confirmdestroyUi = (): UI => (
  <ui ref={confirmdestroyRef}>
    <ConfirmDestroy />
  </ui>
);
