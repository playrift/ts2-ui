import { createRef } from 'ts2';
import { DisplayName } from './displayname.component';

const displayNameRef = createRef('displayname');

export const displayNameUi = (): UI => (
  <ui ref={displayNameRef} x="center" y="center">
    <DisplayName />
  </ui>
);
