import { createRef } from 'ts2';
import { DizanasQuiver } from './dizanas-quiver.component';

const dizanasQuiverRef = createRef('dizanas_quiver');

export const dizanasQuiverUi = (): UI => (
  <ui ref={dizanasQuiverRef}>
    <DizanasQuiver />
  </ui>
);
