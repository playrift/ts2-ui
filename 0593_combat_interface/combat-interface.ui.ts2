import { createRef } from 'ts2';
import { CombatInterface } from './combat-interface.component';

const combatInterfaceRef = createRef('combat_interface');

export const combatInterfaceUi = (): UI => (
  <ui ref={combatInterfaceRef} x={0} y={0} w={190} h={261}>
    <CombatInterface />
  </ui>
);
