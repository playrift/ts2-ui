import { sessionState } from 'ts2';

export const worldmapEmbedded = sessionState(0);

export const worldmapMapChoice = sessionState(-2);

export const worldmapListOpen = sessionState(0);

export const worldmapZoomChoice = sessionState(-1);

export const worldmapTipText = sessionState('');
export const worldmapTipX = sessionState(0);
export const worldmapTipY = sessionState(0);
export const worldmapTipW = sessionState(1);
export const worldmapTipH = sessionState(1);
