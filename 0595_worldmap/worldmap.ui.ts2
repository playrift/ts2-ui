import { createRef } from 'ts2';

import { Worldmap } from './worldmap.component';

const worldmapRef = createRef('worldmap');

export const worldmapUi = (): UI => (
  <ui ref={worldmapRef}>
    <Worldmap />
  </ui>
);
