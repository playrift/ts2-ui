import { createRef } from 'ts2';
import { FossilStorage } from './fossil-storage.component';
import { Frame } from './fossil-storage.constants';

const fossilStorageRef = createRef('fossil_storage');

export const fossilStorageUi = (): UI => (
  <ui ref={fossilStorageRef} w={{ fill: Frame.ROOT_INSET }} h={{ fill: Frame.ROOT_INSET }} x="center" y="center">
    <FossilStorage />
  </ui>
);
