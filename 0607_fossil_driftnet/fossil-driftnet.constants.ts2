import { Fonts } from '../lib/game/fonts';
import { Sounds as GameSounds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';

export declare const DriftnetFont: {
  readonly BUTTON: typeof Fonts.B12_FULL;
  readonly MESSAGE: typeof Fonts.P12_FULL;
};

export declare const DriftnetSprite: {
  readonly ICON_DISCARD: typeof Sprites.BANKBUTTONS_6;
  readonly ICON_BANK: typeof Sprites.BANKBUTTONS_5;
  readonly ICON_TAKE: typeof Sprites.BANKBUTTONS_0;
};

export declare const DriftnetSound: {
  readonly CLICK: typeof GameSounds.INTERFACE_SELECT1;
};

export enum DriftnetColor {
  TEXT = 0xff981f,
  DIMMER = 0x000000,
}

export enum Dimmer {
  TRANSPARENCY = 150,
}

export enum Panel {
  WIDTH_INSET = 100,
  HEIGHT_INSET = 80,
  SHADOW_OFFSET = 5,
}

export enum Content {
  BOTTOM = 9,
  WIDTH_INSET = 16,
  HEIGHT_INSET = 45,
  CATCH_TOP = 20,
  CATCH_WIDTH_INSET = 40,
  CATCH_HEIGHT_INSET = 65,
  TOOLBAR_HEIGHT = 36,
  BUTTON = 36,
  ICON_WIDTH = 29,
  ICON_HEIGHT = 22,
}

export enum Confirmation {
  MESSAGE_TOP = 30,
  MESSAGE_HEIGHT = 100,
  BUTTON_TOP = 130,
  BUTTON_WIDTH = 100,
  BUTTON_HEIGHT = 30,
  BUTTON_LEFT = -80,
  BUTTON_RIGHT = 80,
}

export enum Fade {
  DIM = 100,
  CLEAR = 0,
}

export const PRESS_CYCLES = 20;

export const BANK_FEE = 5;

export enum Confirm {
  NONE = 0,
  DISCARD = 1,
  BANK = 2,
}
