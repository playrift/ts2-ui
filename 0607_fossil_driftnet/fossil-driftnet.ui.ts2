import { createRef } from 'ts2';
import { FossilDriftnet } from './fossil-driftnet.component';

const fossilDriftnetRef = createRef('fossil_driftnet');

export const fossilDriftnetUi = (): UI => (
  <ui ref={fossilDriftnetRef}>
    <FossilDriftnet />
  </ui>
);
