import { createRef } from 'ts2';
import { Fonts } from '../lib/game/fonts';

export const fossilMushtreesRef = createRef('fossil_mushtrees');

export declare const MushtreeFonts: {
  readonly LABEL: typeof Fonts.P11_FULL;
};

export enum MushtreeModel {
  MUSHTREE = 33645,
}

export enum MushtreeCamera {
  ZOOM = 7824,
  PITCH = 42,
  YAW = 213,
}

export enum MushtreeColor {
  LABEL = 0xff981f,
}

export const Layout = {
  ROOT_INSET: 40,
  COLUMN_Y: 30,
  COLUMN_W_INSET: 12,
  COLUMN_H_INSET: 36,
  BUTTON_X: 8,
  BUTTON_Y0: 15,
  BUTTON_PITCH: 60,
  BUTTON_W: 200,
  BUTTON_H: 52,
  LABEL_X: 50,
  LABEL_W: 140,
  MODEL_X: 10,
  MODEL_Y: 28,
  MODEL_SIZE: 32,
};

export enum MushtreeGate {
  FOSSIL_MUSHTREES = 608,
  NO_SUB = -1,
}

export const PRESS_CYCLES = 40;

export const NO_CHAR = -1;
