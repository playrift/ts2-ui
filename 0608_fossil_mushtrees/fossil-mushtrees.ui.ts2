import { FossilMushtrees } from './fossil-mushtrees.component';
import { Layout, fossilMushtreesRef } from './fossil-mushtrees.constants';

export const fossilMushtreesUi = (): UI => (
  <ui ref={fossilMushtreesRef} x="center" y="center" w={{ fill: Layout.ROOT_INSET }} h={{ fill: Layout.ROOT_INSET }}>
    <FossilMushtrees />
  </ui>
);
