import { createRef } from 'ts2';

const fossilStorageInv = createRef('fossil_storage_inv');

export function FossilStorageInv(): Component {
  return <layer x={16} y={8} w={162} h={250} />;
}

export const fossilStorageInvUi = (): UI => (
  <ui ref={fossilStorageInv}>
    <FossilStorageInv />
  </ui>
);
