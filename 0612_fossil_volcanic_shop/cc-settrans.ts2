import { CS2 } from 'ts2';

export function ccSetTransparency(component: ComponentId, subId: Int, transparency: Int): void {
  if (CS2.cc_find(component, subId)) {
    CS2.cc_transparency_set(transparency);
  }
}
