import { createRef } from 'ts2';
import { FossilPool } from './fossil-pool.component';

const fossilPoolRef = createRef('fossil_pool');

export const fossilPoolUi = (): UI => (
  <ui ref={fossilPoolRef}>
    <FossilPool />
  </ui>
);
