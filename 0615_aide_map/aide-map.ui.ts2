import { createRef } from 'ts2';
import { AideMap } from './aide-map.component';

const aideMapRef = createRef('aide_map');

export const aideMapUi = (): UI => (
  <ui ref={aideMapRef}>
    <AideMap />
  </ui>
);
