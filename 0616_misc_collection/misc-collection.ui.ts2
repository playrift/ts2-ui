import { createRef } from 'ts2';
import { MiscCollection } from './misc-collection.component';

const miscCollectionRef = createRef('misc_collection');

export const miscCollectionUi = (): UI => (
  <ui ref={miscCollectionRef} w={410} h={310} x="center" y="center">
    <MiscCollection />
  </ui>
);
