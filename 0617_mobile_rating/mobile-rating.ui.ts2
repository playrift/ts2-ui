import { createRef } from 'ts2';
import { MobileRating } from './mobile-rating.component';

const mobileRatingRef = createRef('mobile_rating');

export const mobileRatingUi = (): UI => (
  <ui ref={mobileRatingRef}>
    <MobileRating />
  </ui>
);
