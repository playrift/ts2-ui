import { createRef } from 'ts2';
import { ArenaReview } from './arena-review.component';

const arenaReviewRef = createRef('1v1arena_review');

export const arenaReviewUi = (): UI => (
  <ui ref={arenaReviewRef} w={355} h={296} x="center" y="center">
    <ArenaReview />
  </ui>
);
