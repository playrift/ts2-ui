import { createRef } from 'ts2';
import { ArenaResults } from './arena-results.component';

const arenaResultsRef = createRef('1v1arena_results');

export const arenaResultsUi = (): UI => (
  <ui ref={arenaResultsRef} w={430} h={300} x="center" y="center">
    <ArenaResults />
  </ui>
);
