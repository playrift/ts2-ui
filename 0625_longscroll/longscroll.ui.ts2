import { createRef } from 'ts2';
import { MessageScroll } from './longscroll.component';

const messageScrollRef = createRef('longscroll');

export const messageScrollUi = (): UI => (
  <ui ref={messageScrollRef}>
    <MessageScroll />
  </ui>
);
