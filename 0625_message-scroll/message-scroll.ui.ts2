import { createRef } from 'ts2';
import { MessageScroll } from './message-scroll.component';

const messageScrollRef = createRef('message-scroll');

export const messageScrollUi = (): UI => (
  <ui ref={messageScrollRef}>
    <MessageScroll />
  </ui>
);
