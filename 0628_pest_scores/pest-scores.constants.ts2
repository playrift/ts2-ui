import { Animations } from '../lib/game/animations';
import { Sprites } from '../lib/game/sprites';

export declare const PestScoresFont: {
  readonly LABEL: typeof Sprites.BARBASSAULT_FONT;
};

export enum PestModel {
  SHIFTER = 14548,
  DEFILER = 14546,
  RAVAGER = 14547,
}

export declare const PestAnim: {
  readonly SHIFTER: typeof Animations.SHIFTER_READY_TWO;
  readonly DEFILER: typeof Animations.DEFILER_READY;
  readonly RAVAGER: typeof Animations.RAVAGER_READY;
};

export enum PestColor {
  LABEL = 0xff981f,
}

export const Layout = {
  ROOT_W_INSET: 260,
  ROOT_H_INSET: 20,
  CONTENT_Y: 40,
  CONTENT_W_INSET: 10,
  CONTENT_H_INSET: 45,
  MODEL_W: 32,
  MODEL_H: 32,
  ROW_H: 32,
  LABEL_W: 120,
  WIDE_W: 140,
};
