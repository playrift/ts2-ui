import { createRef } from 'ts2';
import { PestScores } from './pest-scores.component';

const pestScoresRef = createRef('pest_scores');

export const pestScoresUi = (): UI => (
  <ui ref={pestScoresRef} x="center" y="center">
    <PestScores />
  </ui>
);
