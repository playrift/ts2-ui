import { createRef } from 'ts2';

import { SideJournal } from './side-journal.component';

const sideJournalRef = createRef('side_journal');

export const sideJournalUi = (): UI => (
  <ui ref={sideJournalRef}>
    <SideJournal />
  </ui>
);
