import { createRef } from 'ts2';
import { SeedVaultDepositSide } from './seed-vault-deposit.component';

const seedVaultDepositRef = createRef('seed_vault_deposit');

export const seedVaultDepositUi = (): UI => (
  <ui ref={seedVaultDepositRef} x="center" y="center">
    <SeedVaultDepositSide />
  </ui>
);
