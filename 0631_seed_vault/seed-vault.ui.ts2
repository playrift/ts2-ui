import { createRef } from 'ts2';
import { SeedVaultPanel } from './seed-vault-panel.component';

const seedVaultRef = createRef('seed_vault');

export const seedVaultUi = (): UI => (
  <ui ref={seedVaultRef}>
    <SeedVaultPanel />
  </ui>
);
