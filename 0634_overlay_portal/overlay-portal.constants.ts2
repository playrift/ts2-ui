import { createRef } from 'ts2';

import { Animations } from '../lib/game/animations';

export const PORTAL_ANIM = Animations.POH_PORTAL_LOOP;

export enum WipeChild {
  MODEL = 0,
  WASH = 1,
  BACKDROP = 0,
}

export enum ComponentType {
  RECT = 3,
  MODEL = 6,
}

export enum SizeMode {
  MINUS = 1,
}

export enum PosMode {
  CENTER = 1,
}

export const Wipe = {
  MODEL_OFFSET_Y: 130,
  START_ZOOM: 4000,
  ZOOM_NUM: 6464,
  ZOOM_DEN: 10000,
  MIN_ZOOM: 1,
  MIN_ZOOM_OUT: 2,
  CLEAR: 255,
  OPAQUE: 0,
  STEP: 12,
  CYCLES: 20,
};

export enum Fade {
  OUT = 0,
  IN = 1,
}

export const NO_BACKDROP = -1;

export const backgroundRef = createRef('overlay_portal_background');
export const foregroundRef = createRef('overlay_portal_foreground');
