import { createRef } from 'ts2';
import { OverlayPortal } from './overlay-portal.component';

const overlayPortalRef = createRef('overlay_portal');

export const overlayPortalUi = (): UI => (
  <ui ref={overlayPortalRef} x="center" y="center">
    <OverlayPortal />
  </ui>
);
