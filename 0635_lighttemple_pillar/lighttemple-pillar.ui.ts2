import { createRef } from 'ts2';
import { LightTemplePillar } from './lighttemple-pillar.component';

const lightTemplePillarRef = createRef('lighttemple_pillar');

export const lightTemplePillarUi = (): UI => (
  <ui ref={lightTemplePillarRef}>
    <LightTemplePillar />
  </ui>
);
