import { createRef } from 'ts2';
import { SoteUpassBattle_ } from './sote-upass-battle.component';

const soteUpassBattleRef = createRef('sote_upass_battle');

export const soteUpassBattleUi = (): UI => (
  <ui ref={soteUpassBattleRef}>
    <SoteUpassBattle_ />
  </ui>
);
