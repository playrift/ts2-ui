import { createRef } from 'ts2';
import { GauntletMap } from './gauntlet-map.component';

const gauntletMapRef = createRef('gauntlet_map');

export const gauntletMapUi = (): UI => (
  <ui ref={gauntletMapRef}>
    <GauntletMap />
  </ui>
);
