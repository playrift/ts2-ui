import { createRef } from 'ts2';
import { GauntletRecipes } from './gauntlet-recipes.component';

const gauntletRecipesRef = createRef('gauntlet_recipes');

export const gauntletRecipesUi = (): UI => (
  <ui ref={gauntletRecipesRef}>
    <GauntletRecipes />
  </ui>
);
