import { createRef } from 'ts2';
import { PrifOverlay } from './prif-overlay.component';

const prifOverlayRef = createRef('prif_overlay');

export const prifOverlayUi = (): UI => (
  <ui ref={prifOverlayRef} x="center" y="center">
    <PrifOverlay />
  </ui>
);
