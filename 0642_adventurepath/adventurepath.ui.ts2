import { AdventurePath } from './adventurepath.component';
import { Layout, universeRef } from './adventurepath.constants';

export const adventurePathUi = (): UI => (
  <ui ref={universeRef} x={Layout.ROOT_X} y={0} w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <AdventurePath />
  </ui>
);
