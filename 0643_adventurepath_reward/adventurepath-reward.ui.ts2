import { createRef } from 'ts2';
import { Layout } from './adventurepath-reward.constants';
import { AdventurepathReward } from './adventurepath-reward.component';

const adventurepathRewardRef = createRef('adventurepath_reward');

export const adventurepathRewardUi = (): UI => (
  <ui ref={adventurepathRewardRef} x={Layout.ROOT_X} y={Layout.ROOT_Y} w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <AdventurepathReward />
  </ui>
);
