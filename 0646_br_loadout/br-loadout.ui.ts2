import { createRef } from 'ts2';
import { BrLoadout } from './br-loadout.component';

const brLoadoutRef = createRef('br_loadout');

export const brLoadoutUi = (): UI => (
  <ui ref={brLoadoutRef} x="center" y="center" w={500} h={332}>
    <BrLoadout />
  </ui>
);
