import { createRef } from 'ts2';
import { CanoeMap } from './canoe-map.component';

const canoeMapRef = createRef('canoe_map');

export const canoeMapUi = (): UI => (
  <ui ref={canoeMapRef} w={512} h={334}>
    <CanoeMap />
  </ui>
);
