import { createRef } from 'ts2';
import { TargetStreaks } from './target-streaks.component';

const targetStreaksRef = createRef('target_streaks');

export const targetStreaksUi = (): UI => (
  <ui ref={targetStreaksRef} x="center" y="center">
    <TargetStreaks />
  </ui>
);
