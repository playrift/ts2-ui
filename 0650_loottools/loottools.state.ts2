import { sessionState } from 'ts2';

export const lootTotalCount = sessionState(0);
export const lootTotalValue = sessionState(0);

export const lootRowsExtent = sessionState(0);

export const lootNamedRows = sessionState(0);

export const lootRowsExpanded = sessionState(0);

export const lootGroupViewSynced = sessionState(0);

export const groundListsRevision = sessionState(0);
