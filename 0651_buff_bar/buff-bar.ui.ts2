import { createRef } from 'ts2';
import { BuffBar } from './buff-bar.component';

const buffBarRef = createRef('buff_bar');

export const buffBarUi = (): UI => (
  <ui ref={buffBarRef}>
    <BuffBar />
  </ui>
);
