import { createRef } from 'ts2';
import { LeagueInfoPanel } from './league-info-panel.component';

const leagueInfoRef = createRef('league_info');

export const leagueInfoUi = (): UI => (
  <ui ref={leagueInfoRef}>
    <LeagueInfoPanel />
  </ui>
);
