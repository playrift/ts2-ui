import { createRef } from 'ts2';
import { LeagueRelics } from './league-relics.component';

const leagueRelicsRef = createRef('league_relics');

export const leagueRelicsUi = (): UI => (
  <ui ref={leagueRelicsRef} x="center" y="center" w="fill" h="fill">
    <LeagueRelics />
  </ui>
);
