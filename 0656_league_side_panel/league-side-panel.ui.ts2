import { createRef } from 'ts2';
import { LeagueSidePanel } from './league-side-panel.component';

const leagueSidePanelRef = createRef('league_side_panel');

export const leagueSidePanelUi = (): UI => (
  <ui ref={leagueSidePanelRef} x="center" y="center">
    <LeagueSidePanel />
  </ui>
);
