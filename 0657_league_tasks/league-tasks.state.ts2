import { sessionState } from 'ts2';

export const openFilter = sessionState(-1);

export const dropdownOpenTick = sessionState(0);

export const listExtent = sessionState(0);

export const listPaneHeight = sessionState(0);
