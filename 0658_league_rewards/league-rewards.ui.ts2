import { createRef } from 'ts2';
import { LeagueRewards } from './league-rewards.component';
import { RewardLayout } from './league-rewards.constants';

const leagueRewardsRef = createRef('league_rewards');

export const leagueRewardsUi = (): UI => (
  <ui ref={leagueRewardsRef} w={RewardLayout.ROOT_W} h={RewardLayout.ROOT_H}>
    <LeagueRewards />
  </ui>
);
