import { createRef } from 'ts2';
import { Newspaper } from './newspaper.component';

const newspaperRef = createRef('newspaper');

export const newspaperUi = (): UI => (
  <ui ref={newspaperRef} x="center" y="center">
    <Newspaper />
  </ui>
);
