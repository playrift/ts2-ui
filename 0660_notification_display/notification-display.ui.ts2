import { createRef } from 'ts2';
import { NotificationDisplay } from './notification-display.component';

const notificationDisplayRef = createRef('notification_display');

export const notificationDisplayUi = (): UI => (
  <ui ref={notificationDisplayRef}>
    <NotificationDisplay />
  </ui>
);
