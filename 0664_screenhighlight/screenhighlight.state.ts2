import { sessionState } from 'ts2';

export const PANEL_REST = -1;
export const PANEL_PLACED = 0;
export const PANEL_FALLBACK = 1;

export const highlightCaption = sessionState('Sample text<br>More sample text');

export const highlightAccent = sessionState(-1);

export const highlightCounter = sessionState('0/10');

export const highlightCloseShown = sessionState(1);

export const highlightFallback = sessionState(0);

export const highlightPanelPlace = sessionState(-1);
export const highlightPanelX = sessionState(180);
export const highlightPanelY = sessionState(66);
export const highlightPanelW = sessionState(242);
export const highlightPanelH = sessionState(165);

export const highlightMeasuredH = sessionState(-1);
export const highlightMeasuredW = sessionState(-1);

export const highlightNextLabel = sessionState('');
export const highlightNextMode = sessionState(-1);

export const highlightPrevLabel = sessionState('');
export const highlightPrevMode = sessionState(-1);

export const highlightNextResumes = sessionState(0);

export const highlightNextDisarmed = sessionState(0);
export const highlightPrevDisarmed = sessionState(0);

export const highlightNextHovered = sessionState(0);
export const highlightPrevHovered = sessionState(0);
