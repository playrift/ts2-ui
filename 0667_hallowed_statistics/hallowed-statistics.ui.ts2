import { createRef } from 'ts2';
import { HallowedStatistics } from './hallowed-statistics.component';

const hallowedStatisticsRef = createRef('hallowed_statistics');

export const hallowedStatisticsUi = (): UI => (
  <ui ref={hallowedStatisticsRef}>
    <HallowedStatistics />
  </ui>
);
