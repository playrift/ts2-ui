import { createRef } from 'ts2';
import { HallowedOverlayPanel } from './hallowed-overlay.component';

const hallowedOverlayRef = createRef('hallowed_overlay');

export const hallowedOverlayUi = (): UI => (
  <ui ref={hallowedOverlayRef}>
    <HallowedOverlayPanel />
  </ui>
);
