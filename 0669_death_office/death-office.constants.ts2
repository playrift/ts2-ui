import { Fonts } from '../lib/game/fonts';
import { Inventories } from '../lib/game/inventories';
import { Sounds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';
import { Varps } from '../lib/game/varps';

export declare const OfficeVarps: {
  readonly COFFER: typeof Varps.IF1;
  readonly SELECTED_SLOT: typeof Varps.IF2;
  readonly FEE: typeof Varps.IF3;
};

export declare const OfficeInventories: {
  readonly LOST: typeof Inventories.DEATH_PERMANENT;
};

export declare const OfficeFonts: {
  readonly SMALL: typeof Fonts.P11_FULL;
  readonly LARGE: typeof Fonts.P12_FULL;
};

export declare const OfficeArt: {
  readonly TAKE_ALL: typeof Sprites.BANKBUTTONS_4;
};

export declare const OfficeSounds: {
  readonly PRESS: typeof Sounds.INTERFACE_SELECT1;
};

export declare const OfficeBar: {
  readonly TRACK: typeof Sprites.SCROLLBAR_DRAGGER_V2_3;
  readonly CAP_TOP: typeof Sprites.SCROLLBAR_DRAGGER_V2_0;
  readonly THUMB: typeof Sprites.SCROLLBAR_DRAGGER_V2_1;
  readonly CAP_BOTTOM: typeof Sprites.SCROLLBAR_DRAGGER_V2_2;
  readonly ARROW_UP: typeof Sprites.SCROLLBAR_V2_0;
  readonly ARROW_DOWN: typeof Sprites.SCROLLBAR_V2_1;
};

export enum Panel {
  ROOT_W = 480,
  ROOT_H = 320,
  PANE_H = 226,
  PANE_W = 440,
  BAR_W = 460,
  BUTTON = 36,
}

export enum Slot {
  COUNT = 120,
  W = 36,
  H = 32,
  PITCH_X = 46,
  COLUMNS = 9,
  SPREAD = 14,
  STRIDE_X = W + SPREAD,
  STRIDE_Y = 42,
  INSET_X = 2,
  OUTLINE = 1,
  OUTLINE_SELECTED = 2,
}

export enum Bar {
  RIGHT_1 = 180,
  RIGHT_5 = 135,
  RIGHT_X = 90,
  RIGHT_ALL = 45,
  RIGHT_TAKE_ALL = 0,
  INFO_W_ITEM = 244,
  INFO_W_EMPTY = 424,
}

export enum OfficeColor {
  SLOT_SHADOW = 0x333333,
  INFO = 0xff981f,
  BUTTON_LABEL = 0xdfdfdf,
  BUTTON_LABEL_PRESSED = 0xffffff,
}

export enum Fade {
  SLOT_PRESSED = 100,
  REST = 0,
  ICON_REST = 50,
  ICON_PRESSED = 0,
}

export const PRESS_CYCLES = 20;
