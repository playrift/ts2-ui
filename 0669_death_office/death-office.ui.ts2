import { createRef } from 'ts2';
import { DeathOffice } from './death-office.component';
import { Panel } from './death-office.constants';

const deathOfficeRef = createRef('death_office');

export const deathOfficeUi = (): UI => (
  <ui ref={deathOfficeRef} w={Panel.ROOT_W} h={Panel.ROOT_H} x="center" y="center">
    <DeathOffice />
  </ui>
);
