import { Fonts } from '../lib/game/fonts';
import { Inventories } from '../lib/game/inventories';
import { Sounds as SoundIds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';
import { Varps } from '../lib/game/varps';

export declare const CofferInventories: {
  readonly INV: typeof Inventories.INV;
};

export declare const CofferVarps: {
  readonly BALANCE: typeof Varps.IF1;
  readonly SLOT: typeof Varps.IF2;
  readonly QUANTITY: typeof Varps.IF3;
  readonly UNIT_VALUE: typeof Varps.IF4;
};

export enum Quantity {
  ONE = 1,
  FIVE = 5,
  ALL = 2147483647,
}

export const ACCEPTABLE_VALUE = 10000;

export const THOUSANDS = ',';

export const MAX_INT = 2147483647;

export declare const CofferArt: {
  readonly BACKING: typeof Sprites.TRADEBACKING_DARK;
};

export declare const CofferFonts: {
  readonly NAME: typeof Fonts.B12_FULL;
  readonly BODY: typeof Fonts.P12_FULL;
  readonly SMALL: typeof Fonts.P11_FULL;
};

export declare const CofferSounds: {
  readonly CLICK: typeof SoundIds.INTERFACE_SELECT1;
};

export enum CofferColor {
  BODY = 0xff981f,
  LIVE = 0xffffff,
  DEAD = 0x9f9f9f,
}

export enum Root {
  W = 460,
  H = 320,
}

export enum Body {
  W_INSET = 20,
  H_INSET = 50,
  BOTTOM = 10,
  LEFT_INSET = 260,
  RIGHT_W = 255,
}

export enum Art {
  COFFER_MODEL = 4620,
  COFFER_W = 70,
  COFFER_H = 70,
  COFFER_PITCH = 512,
  COFFER_ZOOM = 400,
  COFFER_DX = -20,
  COFFER_DY = 50,

  SPINNER_MODEL = 38743,
  SPINNER_ANIM = 7301,
  SPINNER_SIZE = 32,
  SPINNER_YAW = 1024,
  SPINNER_ZOOM = 700,
  SPINNER_DY = 125,
  MODEL_DX = -30,

  ITEM_SIZE = 64,
  ITEM_ZOOM = 400,
}

export enum Button {
  SIZE = 32,
  RIGHT = 10,
  ONE_DY = -63,
  FIVE_DY = -21,
  X_DY = 21,
  ALL_DY = 63,
  CONFIRM_W = 150,
  CONFIRM_H = 35,
  CONFIRM_BOTTOM = 10,
  CONFIRM_TEXT_INSET = 10,
}

export enum Readout {
  NAME_H = 20,
  NAME_Y = 10,
  VALUE_H = 15,
  VALUE_Y = 30,
}

export enum Balance {
  H = 20,
  BLURB_LINE_HEIGHT = 15,
}

export const PRESS_CYCLES = 40;
