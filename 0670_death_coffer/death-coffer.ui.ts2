import { createRef } from 'ts2';
import { DeathCoffer } from './death-coffer.component';
import { Root } from './death-coffer.constants';

const deathCofferRef = createRef('death_coffer');

export const deathCofferUi = (): UI => (
  <ui ref={deathCofferRef} w={Root.W} h={Root.H} x="center" y="center">
    <DeathCoffer />
  </ui>
);
