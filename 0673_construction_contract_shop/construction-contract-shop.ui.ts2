import { createRef } from 'ts2';
import { ConstructionContractShop } from './construction-contract-shop.component';
import { ContractFrame } from './construction-contract-shop.constants';

const constructionContractShopRef = createRef('construction_contract_shop');

export const constructionContractShopUi = (): UI => (
  <ui ref={constructionContractShopRef} w={ContractFrame.ROOT_W} h={ContractFrame.ROOT_H} x="center" y="center">
    <ConstructionContractShop />
  </ui>
);
