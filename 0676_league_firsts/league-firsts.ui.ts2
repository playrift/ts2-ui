import { createRef } from 'ts2';

import { LeagueFirsts } from './league-firsts.component';

const leagueFirstsRef = createRef('league_firsts');

export const leagueFirstsUi = (): UI => (
  <ui ref={leagueFirstsRef}>
    <LeagueFirsts />
  </ui>
);
