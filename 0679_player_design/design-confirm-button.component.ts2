import { CS2, useState } from 'ts2';

import { onMobile } from '../lib/lib.helpers';
import { StoneButtonFilled } from '../lib/stone-button-filled.component';
import { StoneButtonPressedFilled } from '../lib/stone-button-pressed-filled.component';
import { DesignColors, DesignFonts } from './player-design.constants';

export function DesignConfirmButton({ label }: { label: String }): Component {
  const mobile: Boolean = onMobile();
  const [pressed, setPressed] = useState(0);

  const op: String = CS2.tags_remove(label);

  return (
    <layer
      w="fill"
      h="fill"
      options={[op]}
      onMouseRepeat={() => {
        if (!mobile) {
          setPressed(1);
        }
      }}
      onMouseLeave={() => {
        if (!mobile) {
          setPressed(0);
        }
      }}
    >
      {pressed === 1 ? <StoneButtonPressedFilled /> : <StoneButtonFilled />}

      <text
        x="center"
        y="center"
        w="fill"
        h="fill"
        text={label}
        font={DesignFonts.BODY}
        color={DesignColors.BUTTON_LABEL}
        align="center"
        valign="middle"
        shadow
      />
    </layer>
  );
}
