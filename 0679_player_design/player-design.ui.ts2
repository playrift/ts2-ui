import { createRef } from 'ts2';

import { PlayerDesign } from './player-design.component';

const playerDesignRef = createRef('player_design');

export const playerDesignUi = (): UI => (
  <ui ref={playerDesignRef}>
    <PlayerDesign />
  </ui>
);
