import { createRef } from 'ts2';
import { IndexedBook } from './indexed-book.component';

const indexedBookRef = createRef('indexed_book');

export const indexedBookUi = (): UI => (
  <ui ref={indexedBookRef}>
    <IndexedBook />
  </ui>
);
