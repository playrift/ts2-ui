import { createRef } from 'ts2';
import { TemporossStats } from './tempoross-stats.component';

const temporossStatsRef = createRef('tempoross_stats');

export const temporossStatsUi = (): UI => (
  <ui ref={temporossStatsRef} w={512} h={275} y="center">
    <TemporossStats />
  </ui>
);
