import { Fonts } from '../lib/game/fonts';
import { Inventories } from '../lib/game/inventories';
import { Sounds as SoundIds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';
import { Varbits } from '../lib/game/varbits';

export declare const TackleBox: {
  readonly INV: typeof Inventories.TACKLE_BOX;
};

export const SLOTS = 35;

export enum Quantity {
  ONE = 0,
  FIVE = 1,
  ALL = 2,
  X = 3,
}

export declare const TackleBoxVarbits: {
  readonly QUANTITY: typeof Varbits.II_ELNOCK_STORAGE_SELECTEDQUANTITY;
};

export declare const TackleBoxArt: {
  readonly FILL: typeof Sprites.BANKBUTTONS_0;
  readonly EMPTY: typeof Sprites.BANKBUTTONS_4;
};

export declare const TackleBar: {
  readonly TRACK: typeof Sprites.SCROLLBAR_DRAGGER_V2_3;
  readonly CAP_TOP: typeof Sprites.SCROLLBAR_DRAGGER_V2_0;
  readonly THUMB: typeof Sprites.SCROLLBAR_DRAGGER_V2_1;
  readonly CAP_BOTTOM: typeof Sprites.SCROLLBAR_DRAGGER_V2_2;
  readonly ARROW_UP: typeof Sprites.SCROLLBAR_V2_0;
  readonly ARROW_DOWN: typeof Sprites.SCROLLBAR_V2_1;
};

export declare const TackleBoxFonts: {
  readonly LABEL: typeof Fonts.P11_FULL;
};

export declare const TackleBoxSounds: {
  readonly CLICK: typeof SoundIds.INTERFACE_SELECT1;
};

export enum TackleBoxColor {
  SELECTED = 0xffffff,
  IDLE = 0xff981f,
  SLOT_SHADOW = 0x333333,
}

export enum Root {
  W = 388,
  H = 270,
}

export enum Body {
  W_INSET = 20,
  H_INSET = 50,
  BOTTOM = 10,
}

export enum Button {
  SIZE = 37,
  QTY_1_X = 0,
  QTY_5_X = 45,
  QTY_X_X = 90,
  QTY_ALL_X = 135,
  ICON_X = 135,
  FILL_Y = 0,
  EMPTY_Y = 50,
  ICON_W = 29,
  ICON_H = 22,
}

export enum Column {
  W = 190,
  BAR_W = 16,
}

export enum Slot {
  W = 36,
  H = 32,
  COLUMNS = 4,
  DRAG_DEAD = 5,
  OUTLINE = 1,
}

export const BLANK_ITEM = 6512;

export enum Transparency {
  FILLED = 0,
  BLANK = 255,
  FLASH = 120,
  ICON_HOVER = 120,
  ICON_REST = 0,
}

export enum DragScroll {
  TOP_BAND = 15,
  TOP_FAST = 5,
  BOTTOM_BAND = 20,
  BOTTOM_FASTEST = -5,
  BOTTOM_FAST = 5,
  SLOW = 5,
  FAST = 8,
  FASTEST = 10,
}

export const PRESS_CYCLES = 20;

export const EXAMINE_OP = 10;

export enum Decor {
  MODEL = 41966,
  X = 70,
  Y = 125,
  W = 32,
  H = 32,
  ZOOM = 510,
  PITCH = 150,
  YAW = 1920,
}

export const TITLE = 'Tackle Box';
