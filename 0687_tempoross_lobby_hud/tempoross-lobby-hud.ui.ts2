import { createRef } from 'ts2';
import { TemporossLobbyHud } from './tempoross-lobby-hud.component';

const temporossLobbyHudRef = createRef('tempoross_lobby_hud');

export const temporossLobbyHudUi = (): UI => (
  <ui ref={temporossLobbyHudRef}>
    <TemporossLobbyHud />
  </ui>
);
