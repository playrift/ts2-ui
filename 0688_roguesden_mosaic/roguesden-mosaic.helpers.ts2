import { CS2 } from 'ts2';

import { Camera } from './roguesden-mosaic.constants';

export function mosaicTileDistance(tile: ComponentId, zoom: Int): void {
  CS2.if_model_angle_set(0, 0, Camera.PITCH, 0, 0, zoom, tile);
}
