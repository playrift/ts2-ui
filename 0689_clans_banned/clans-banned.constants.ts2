import { createRef } from 'ts2';

import { Fonts } from '../lib/game/fonts';
import { Sounds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';
import { Varbits } from '../lib/game/varbits';

export const clansBannedRef = createRef('clans_banned');

export declare const ClanBannedFonts: {
  readonly BODY: typeof Fonts.P12_FULL;
};

export declare const ClanBannedSounds: {
  readonly CLICK: typeof Sounds.INTERFACE_SELECT1;
};

export declare const ClanBannedSprites: {
  readonly BACKING: typeof Sprites.TRADEBACKING_DARK;
  readonly TRACK: typeof Sprites.SCROLLBAR_DRAGGER_V2_3;
  readonly CAP_TOP: typeof Sprites.SCROLLBAR_DRAGGER_V2_0;
  readonly THUMB: typeof Sprites.SCROLLBAR_DRAGGER_V2_1;
  readonly CAP_BOTTOM: typeof Sprites.SCROLLBAR_DRAGGER_V2_2;
  readonly ARROW_UP: typeof Sprites.SCROLLBAR_V2_0;
  readonly ARROW_DOWN: typeof Sprites.SCROLLBAR_V2_1;
};

export declare const ClanBannedVarbits: {
  readonly LOAD_AS_GUEST: typeof Varbits.CLAN_TRANSMIT_LOADASGUEST;
  readonly GUEST_AFFINITY_STATUS: typeof Varbits.CLAN_GUEST_AFFINITYSTATUS;
  readonly AFFINITY_STATUS: typeof Varbits.CLAN_AFFINITYSTATUS;
  readonly LOOKUP_IN_PROGRESS: typeof Varbits.CLAN_EDITING_LOOKUPINPROGRESS;
};

export enum AffinityStatus {
  LIVE = 3,
}

export const MAX_ROWS = 500;
export const COLUMNS = 3;

export const Layout = {
  PANEL_W: 475,
  PANEL_H: 320,
  PANEL_SLACK: 10,
  LIST_MIN: 180,
  CONTENT_BOTTOM: 10,
  CONTENT_INSET_W: 20,
  CONTENT_INSET_H: 50,
  LIST_BOX_DROP: 34,
  LIST_X: 2,
  LIST_INSET_W: 20,
  LIST_INSET_H: 4,
  BAR_RIGHT: 2,
  BAR_W: 16,
  BAR_INSET_H: 4,
  BLOCKER_INSET: 4,
  BUTTONS_H: 30,
  BACK_W: 80,
  HINT_X: 85,
  HINT_INSET_W: 200,
  SEARCH_RIGHT: 80,
  SEARCH_W: 30,
  ADD_W: 75,
  ROW_H: 25,
} as const;

export enum ClanBannedColor {
  LABEL = 0xff981f,
  LABEL_HOT = 0xffb83f,
  WHITE = 0xffffff,
  BLACK = 0x000000,
}

export enum ClanBannedWash {
  STRIPE_EVEN = 255,
  STRIPE_ODD = 245,
  PRESSED = 200,
  HOVER = 225,
  BLOCKER_CLEAR = 255,
  BLOCKER_FLOOR = 100,
  BLOCKER_TEXT_SHOWS_BELOW = 175,
  BLOCKER_IN_STEP = 6,
  BLOCKER_OUT_STEP = 9,
}

export const HOVER_CYCLES = 2;
export const PRESS_CYCLES = 20;
export const UNBAN_CYCLES = 30;

export const ClanBannedText = {
  ADD: '<col=0dc10d>Add</col>',
  BACK: '<col=ff0000>Back</col>',
  TITLE_SUFFIX: 'Ban list',
  SEARCH_TITLE: 'Showing banned players: ',
  SEARCH_ANY: '*',
  HINT_MAY: ' a name to remove it from the ban list.',
  HINT_LEAGUE: 'You may not administer clans from a League world.',
  HINT_SPEEDRUN: 'You may not administer clans from a Speedrunning world.',
  HINT_THIS_WORLD: 'You may not administer clans from this world.',
  HINT_RANK_PREFIX: 'Only clan members with a rank of ',
  HINT_RANK_SUFFIX: ' may unban players.',
  HINT_MEMBERS_ONLY: 'Only clan members may unban players.',
  EMPTY: 'The ban list is currently empty.',
  NO_MATCH: 'No entries match the current filter.',
  REQUESTING_PREFIX: 'Requesting unban for <col=ffb83f>',
  REQUESTING_SUFFIX: '</col>...',
} as const;

export const ClanBannedOps = {
  SEARCH: 'Search',
  ADD: 'Add',
  BACK: 'Back',
  UNBAN: 'Unban',
} as const;

export const OP_SOUND_MASK = 2;
