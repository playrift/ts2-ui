import { sessionState } from 'ts2';

export const blockerWash = sessionState(255);

export const blockerName = sessionState('');

export const unbanDeadline = sessionState(-1);

export const unbanName = sessionState('');
