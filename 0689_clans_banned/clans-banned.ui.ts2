import { clanSearchInit } from '../0693_clans_members/clan-members-shared.search';
import { ClansBanned } from './clans-banned.component';
import { clansBannedRef } from './clans-banned.constants';

export const clansBannedUi = (): UI => (
  <ui ref={clansBannedRef}>
    <layer x={0} y={0} w={1} h={1} onLoad={() => clanSearchInit()} />
    <ClansBanned />
  </ui>
);
