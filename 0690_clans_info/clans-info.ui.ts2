import { createRef } from 'ts2';
import { ClansInfo } from './clans-info.component';

const clansInfoRef = createRef('clans_info');

export const clansInfoUi = (): UI => (
  <ui ref={clansInfoRef} x="center" y="center">
    <ClansInfo />
  </ui>
);
