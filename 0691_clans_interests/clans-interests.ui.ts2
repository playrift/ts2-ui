import { createRef } from 'ts2';

import { ClansInterests } from './clans-interests.component';

const clansInterestsRef = createRef('clans_interests');

export const clansInterestsUi = (): UI => (
  <ui ref={clansInterestsRef}>
    <ClansInterests />
  </ui>
);
