import { createRef } from 'ts2';
import { ClansHall } from './clans-hall.component';

const clansHallRef = createRef('clans_hall');

export const clansHallUi = (): UI => (
  <ui ref={clansHallRef}>
    <ClansHall />
  </ui>
);
