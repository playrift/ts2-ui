export function modulo(value: Int, base: Int): Int {
  if (base <= 0) {
    return 0;
  }
  if (value >= 0) {
    return value % base;
  }
  const magnitude: Int = 0 - value;
  const lifted: Int = value + (magnitude - (magnitude % base)) + base;
  return lifted % base;
}
