import { createRef } from 'ts2';
import { ClansOutfit } from './clans-outfit.component';

const clansOutfitRef = createRef('clans_outfit');

export const clansOutfitUi = (): UI => (
  <ui ref={clansOutfitRef} x="center" y="center">
    <ClansOutfit />
  </ui>
);
