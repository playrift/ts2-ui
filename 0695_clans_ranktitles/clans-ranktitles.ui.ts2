import { createRef } from 'ts2';

import { ClansRanktitles } from './clans-ranktitles.component';

const clansRanktitlesRef = createRef('clans_ranktitles');

export const clansRanktitlesUi = (): UI => (
  <ui ref={clansRanktitlesRef}>
    <ClansRanktitles />
  </ui>
);
