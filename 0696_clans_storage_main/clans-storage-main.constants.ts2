import { Enums } from '../lib/game/enums';
import { Fonts } from '../lib/game/fonts';
import { Sounds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';
import { Varbits } from '../lib/game/varbits';
import { Varps } from '../lib/game/varps';

export declare const ClanCofferArt: {
  readonly COINS: typeof Sprites.COINS;
};

export declare const ClanCofferFonts: {
  readonly LABEL: typeof Fonts.P12_FULL;
};

export declare const ClanCofferSounds: {
  readonly CLICK: typeof Sounds.INTERFACE_SELECT1;
};

export enum ClanCofferItems {
  COINS = 995,
  COINS_1000 = 1003,
}

export declare const ClanCofferVarbits: {
  readonly AFFINITY_STATUS: typeof Varbits.CLAN_AFFINITYSTATUS;
  readonly GUEST_AFFINITY_STATUS: typeof Varbits.CLAN_GUEST_AFFINITYSTATUS;
  readonly LOAD_AS_GUEST: typeof Varbits.CLAN_TRANSMIT_LOADASGUEST;
};

export declare const ClanCofferVarps: {
  readonly TEMP_TRANSMIT_0: typeof Varps.CLAN_TEMP_TRANSMIT_0;
};

export enum AffinityStatus {
  CONNECTING = 0,
  NO_CLAN = 1,
  FETCHING = 2,
  LIVE = 3,
}

export const AffinityText = {
  FETCHING: '<col=9f9f9f>Fetching data...</col>',
  NO_CLAN: '<col=9f9f9f>No current clan</col>',
  CONNECTING: '<col=9f9f9f>Connecting...</col>',
  UNAVAILABLE: '<col=9f9f9f>System unavailable</col>',
};

export const ClanCofferText = {
  TITLE_SUFFIX: 'Coffer',
  NO_DATA: 'Clan data is not currently available.',
  HINT_WITHDRAW: 'Withdraw from the coffer:',
  HINT_NOT_A_MEMBER: '<col=ff0000>Clan membership is required:</col>',
  HINT_UNAVAILABLE: '<col=ff0000>Clan data is unavailable.</col>',
  INSPECT_FUNDED: 'Eligible members of the clan may withdraw coins via the quantity buttons.',
  INSPECT_EMPTY: 'Eligible members of the clan may deposit coins via the side-panel menu.',
  EXAMINE_EMPTY: 'There are no coins in the coffer.',
  NOT_RANKED: 'Not ranked',
};

export enum ClanCofferColor {
  NO_DATA = 0xff0000,
  SLOT_SHADOW = 0x333333,
  LABEL = 0xff981f,
  HOVER = 0xffffff,
}

export enum CofferFade {
  FUNDED_REST = 0,
  FUNDED_FLASH = 100,
  EMPTY_REST = 125,
  EMPTY_FLASH = 150,
}

export const FLASH_CYCLES = 10;

export const PRESS_CYCLES = 30;

export const DRAWCOINS_POLL_CYCLES = 140;

export declare const ClanRankValues: typeof Enums.ENUM_3795;

export const CLAN_RANK_COUNT = 30;

export declare const ClanRankTitles: typeof Enums.ENUM_3797;

export declare const ClanRankIcons: typeof Enums.ENUM_3798;

export { ClanPermission, ClanPermissions } from './clan-permissions.data';

export enum ClanRank {
  SETTING_OFFSET = 2,
  OWNER = 126,
  NOBODY = 128,
}

export const CLAN_SETTING_UNAVAILABLE = 0;

export enum Layout {
  ROOT_W = 280,
  ROOT_H = 192,
  CONTENT_INSET_W = 20,
  CONTENT_INSET_H = 50,
  CONTENT_BOTTOM = 10,
  PICTURE_W = 77,
  PICTURE_H = 82,
  STRIP_H = 60,
  HINT_H = 20,
  BUTTON_W = 40,
  BUTTON_INSET_H = 20,
  BUTTON_DX_1 = 110,
  BUTTON_DX_2 = 66,
  BUTTON_DX_3 = 22,
  SLOT_W = 36,
  SLOT_H = 32,
  NO_DATA_INSET_W = 10,
}
