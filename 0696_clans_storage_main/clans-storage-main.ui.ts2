import { createRef } from 'ts2';
import { ClansStorageMain } from './clans-storage-main.component';
import { Layout } from './clans-storage-main.constants';

const clansStorageMainRef = createRef('clans_storage_main');

export const clansStorageMainUi = (): UI => (
  <ui ref={clansStorageMainRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <ClansStorageMain />
  </ui>
);
