import { Fonts } from '../lib/game/fonts';
import { Inventories } from '../lib/game/inventories';
import { Sounds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';

export declare const ClanStorage: {
  readonly INV: typeof Inventories.INV;
};

export declare const ClanStorageArt: {
  readonly COINS: typeof Sprites.COINS;
};

export declare const ClanStorageFonts: {
  readonly LABEL: typeof Fonts.P12_FULL;
};

export declare const ClanStorageSounds: {
  readonly CLICK: typeof Sounds.INTERFACE_SELECT1;
};

export enum ClanStorageItems {
  COINS = 995,
  COINS_1000 = 1003,
}

export const ClanStorageColors = {
  LABEL: 0xff981f,
  HOVER: 0xffffff,
  SLOT_SHADOW: 0x333333,
};

export const SlotTransparency = {
  HELD_REST: 0,
  HELD_FLASH: 100,
  EMPTY_REST: 125,
  EMPTY_FLASH: 150,
};

export const FLASH_CYCLES = 10;

export const PRESS_CYCLES = 30;

export const ClanStorageText = {
  ELIGIBLE: 'Eligible members of the clan may deposit coins via the quantity buttons.',
  NO_COINS_HINT: 'If you had some coins in your inventory, you could deposit them.',
  NO_COINS: 'There are no coins in your inventory.',
  HINT: 'Deposit into the coffer:',
};

export enum Layout {
  ROOT_W = 175,
  ROOT_H = 205,
  PICTURE_W = 77,
  PICTURE_H = 82,
  HINT_H = 20,
  BUTTON = 45,
  BUTTON_DX = 60,
  BUTTON_DY = 55,
  SLOT_W = 36,
  SLOT_H = 32,
}
