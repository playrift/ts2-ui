import { createRef } from 'ts2';
import { ClansStorageSide } from './clans-storage-side.component';
import { Layout } from './clans-storage-side.constants';

const clansStorageSideRef = createRef('clans_storage_side');

export const clansStorageSideUi = (): UI => (
  <ui ref={clansStorageSideRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <ClansStorageSide />
  </ui>
);
