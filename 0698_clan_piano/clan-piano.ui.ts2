import { createRef } from 'ts2';

import { ClanPiano } from './clan-piano.component';

const clanPianoRef = createRef('clan_piano');

export const clanPianoUi = (): UI => (
  <ui ref={clanPianoRef}>
    <ClanPiano />
  </ui>
);
