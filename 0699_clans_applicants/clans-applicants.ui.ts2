import { ClansApplicants } from './clans-applicants.component';
import { clansApplicantsRef } from './clans-applicants.constants';

export const clansApplicantsUi = (): UI => (
  <ui ref={clansApplicantsRef} x="center" y="center">
    <ClansApplicants />
  </ui>
);
