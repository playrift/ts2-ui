import { createRef } from 'ts2';
import { ClansSidepanel } from './clans-sidepanel.component';
import { Layout } from './clans-sidepanel.constants';

const clansSidepanelRef = createRef('clans_sidepanel');

export const clansSidepanelUi = (): UI => (
  <ui ref={clansSidepanelRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <ClansSidepanel />
  </ui>
);
