import { createRef } from 'ts2';
import { ClansEvents } from './clans-events.component';

const clansEventsRef = createRef('clans_events');

export const clansEventsUi = (): UI => (
  <ui ref={clansEventsRef} x="center" y="center">
    <ClansEvents />
  </ui>
);
