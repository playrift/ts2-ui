import { createRef } from 'ts2';

import { ClansPermissions } from './clans-permissions.component';

const clansPermissionsRef = createRef('clans_permissions');

export const clansPermissionsUi = (): UI => (
  <ui ref={clansPermissionsRef}>
    <ClansPermissions />
  </ui>
);
