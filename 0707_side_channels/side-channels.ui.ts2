import { createRef } from 'ts2';
import { SideChannels } from './side-channels.component';

const sideChannelsRef = createRef('side_channels');

export const sideChannelsUi = (): UI => (
  <ui ref={sideChannelsRef}>
    <SideChannels />
  </ui>
);
