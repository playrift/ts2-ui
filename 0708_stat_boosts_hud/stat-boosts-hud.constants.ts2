import { createRef } from 'ts2';

export const universeRef = createRef('stat_boosts_hud');
export const dodgerRef = createRef('stat_boosts_hud_dodger');
export const dynamicTimerRef = createRef('stat_boosts_hud_dynamic_timer');
export const contentRef = createRef('stat_boosts_hud_content');
export const tooltipRef = createRef('stat_boosts_hud_tooltip');

export const enum Layout {
  TILE = 35,
  BAR_W = 4,
  BAR_X_STEP = 4,
  BAR_X_BASE = 1,
  TIMERS_W = 11,
  TIMERS_H = 35,
  BAR_TRAVEL = 33,
  BAR_MIN = 1,
  BAR_UNTICKED_Y = 1,
  BAR_UNTICKED_H = 1,
  CYCLES_PER_TICK = 30,
  ICON = 26,
  ICON_LIFT = 1,
  CAPTION_H = 12,
  CAPTION_PAD = 2,
  CONTENT_INSET = 2,
  CONTENT_INSET_MOBILE_Y = 0,
  CONTENT_W = 508,
  CONTENT_H = 330,
  FULL_W = 512,
  FULL_H = 334,
}

export const enum Colour {
  FRAME_OUTER = 0x383023,
  FRAME_INNER = 0x5a5245,
  DRAINED = 0xff8888,
  BOOSTED = 0x93ff7a,
  BLACK = 0x000000,
}

export const enum Trans {
  OPAQUE = 0,
  BACKING = 100,
  BAR = 75,
  CAPTION_STRIP = 153,
}

export const enum Dodge {
  DESKTOP_FLAGS = 30720,
  MOBILE_FLAGS = 2,
}

export const enum CombatSkill {
  ATTACK = 0,
  DEFENCE = 1,
  STRENGTH = 2,
  RANGED = 4,
  MAGIC = 6,
}

export const enum SkippedSkill {
  HITPOINTS = 3,
  PRAYER = 5,
}

export const LAST_STAND_FLOOR_ABOVE_BASE = 15;

export declare const Skills: {
  readonly ATTACK: SkillRef<0>;
  readonly DEFENCE: SkillRef<1>;
  readonly STRENGTH: SkillRef<2>;
  readonly HITPOINTS: SkillRef<3>;
  readonly RANGED: SkillRef<4>;
  readonly PRAYER: SkillRef<5>;
  readonly MAGIC: SkillRef<6>;
  readonly COOKING: SkillRef<7>;
  readonly WOODCUTTING: SkillRef<8>;
  readonly FLETCHING: SkillRef<9>;
  readonly FISHING: SkillRef<10>;
  readonly FIREMAKING: SkillRef<11>;
  readonly CRAFTING: SkillRef<12>;
  readonly SMITHING: SkillRef<13>;
  readonly MINING: SkillRef<14>;
  readonly HERBLORE: SkillRef<15>;
  readonly AGILITY: SkillRef<16>;
  readonly THIEVING: SkillRef<17>;
  readonly SLAYER: SkillRef<18>;
  readonly FARMING: SkillRef<19>;
  readonly RUNECRAFT: SkillRef<20>;
  readonly HUNTER: SkillRef<21>;
  readonly CONSTRUCTION: SkillRef<22>;
  readonly SAILING: SkillRef<23>;
};
