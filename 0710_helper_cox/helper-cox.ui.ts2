import { createRef } from 'ts2';
import { HelperCox } from './helper-cox.component';

const helperCoxRef = createRef('helper_cox');

export const helperCoxUi = (): UI => (
  <ui ref={helperCoxRef}>
    <HelperCox />
  </ui>
);
