import { createRef } from 'ts2';
import { HelperGeneric } from './helper-generic.component';

const helperGenericRef = createRef('helper_generic');

export const helperGenericUi = (): UI => (
  <ui ref={helperGenericRef}>
    <HelperGeneric />
  </ui>
);
