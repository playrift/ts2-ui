import { createRef } from 'ts2';

import { CaOverview } from './ca-overview.component';
import { Layout } from './ca-overview.constants';

export const caOverviewRef = createRef('ca_overview');

export const caOverviewUi = (): UI => (
  <ui ref={caOverviewRef} w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <CaOverview />
  </ui>
);
