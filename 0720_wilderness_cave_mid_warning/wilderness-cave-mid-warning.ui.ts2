import { createRef } from 'ts2';
import { CwsWarning } from '../0319_cws_warning_25/cws-warning.component';

const wildernessCaveMidWarning = createRef('wilderness_cave_mid_warning');

export const wildernessCaveMidWarningUi = (): UI => (
  <ui ref={wildernessCaveMidWarning}>
    <CwsWarning
      message={21}
      body="Be careful! It doesn't look like there is a way<br>back up. If you jump down, you will have to<br>find another exit."
      yes="Let me jump, and don't warn me again!"
      no="I don't want to jump down anymore."
      yesOption="Jump!"
      noOption="Don't jump!"
    />
  </ui>
);
