import { SharedBank } from './shared-bank.component';
import { Layout } from './shared-bank.constants';
import { sharedBank } from './shared-bank.refs';

export const sharedBankUi = (): UI => (
  <ui ref={sharedBank} w={Layout.ROOT_W} x="center" y="center">
    <SharedBank />
  </ui>
);
