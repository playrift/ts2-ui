import { Inventories } from '../lib/game/inventories';
import { Varbits } from '../lib/game/varbits';

export declare const SharedBank: {
  readonly INV: typeof Inventories.INV_PLAYER_TEMP;
};

export declare const SharedBankVarbits: {
  readonly QUANTITY_TYPE: typeof Varbits.BANK_QUANTITY_TYPE;
  readonly REQUESTED_QUANTITY: typeof Varbits.BANK_REQUESTEDQUANTITY;
};

export enum Grid {
  W = 36,
  H = 32,
  COLUMNS = 4,
  STRIDE_X = 42,
  STRIDE_Y = 36,
  ORIGIN_X = 16,
  ORIGIN_Y = 8,
  OUTLINE = 1,
  DRAG_DEAD = 5,
}

export enum SharedBankColor {
  SLOT_SHADOW = 0x333333,
}

export enum Transparency {
  REST = 0,
  FLASH = 100,
  BLANK = 255,
}

export enum Quantity {
  ONE = 0,
  FIVE = 1,
  TEN = 2,
  X = 3,
  ALL = 4,
}

export const BLANK_ITEM = 6512;

export enum Root {
  W = 190,
  H = 261,
}
