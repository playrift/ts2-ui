import { createRef } from 'ts2';
import { SharedBankSide } from './shared-bank-side.component';
import { Root } from './shared-bank-side.constants';

const sharedBankSideRef = createRef('shared_bank_side');

export const sharedBankSideUi = (): UI => (
  <ui ref={sharedBankSideRef} x="center" y="center" w={Root.W} h={Root.H}>
    <SharedBankSide />
  </ui>
);
