import { createRef } from 'ts2';
import { SideChannelsLarge } from './side-channels-large.component';

const sideChannelsLargeRef = createRef('side_channels_large');

export const sideChannelsLargeUi = (): UI => (
  <ui ref={sideChannelsLargeRef}>
    <SideChannelsLarge />
  </ui>
);
