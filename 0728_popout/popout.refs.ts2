import { createRef } from 'ts2';

export const popoutBodyRef = createRef('popout_body');
export const popoutSlotsRef = createRef('popout_slots');
export const popoutTrackerSlotRef = createRef('popout_tracker');
export const popoutTipRef = createRef('popout_tooltip');
export const popoutContentRef = createRef('popout_content');
