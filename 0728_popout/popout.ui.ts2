import { createRef } from 'ts2';

import { Popout } from './popout.component';

const popoutRef = createRef('popout');

export const popoutUi = (): UI => (
  <ui ref={popoutRef} x={{ right: 0 }} y={0}>
    <Popout />
  </ui>
);
