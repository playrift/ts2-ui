import { createRef } from 'ts2';
import { GimSettings } from './gim-settings.component';

const gimSettingsRef = createRef('gim_settings');

export const gimSettingsUi = (): UI => (
  <ui ref={gimSettingsRef} x="center" y="center">
    <GimSettings />
  </ui>
);
