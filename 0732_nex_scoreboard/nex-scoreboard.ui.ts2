import { createRef } from 'ts2';
import { NexScoreboard } from './nex-scoreboard.component';

const nexScoreboardRef = createRef('nex_scoreboard');

export const nexScoreboardUi = (): UI => (
  <ui ref={nexScoreboardRef} x="center" y="center">
    <NexScoreboard />
  </ui>
);
