import { createRef } from 'ts2';
import { LeagueUnlocks } from './league-3-unlocks.component';

const league3UnlocksRef = createRef('league_3_unlocks');

export const league3UnlocksUi = (): UI => (
  <ui ref={league3UnlocksRef}>
    <LeagueUnlocks />
  </ui>
);
