import { createRef } from 'ts2';
import { LeagueFragments } from './league-fragments.component';

const league3FragmentsRef = createRef('league-3-fragments');

export const league3FragmentsUi = (): UI => (
  <ui ref={league3FragmentsRef}>
    <LeagueFragments />
  </ui>
);
