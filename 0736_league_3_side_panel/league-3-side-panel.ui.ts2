import { createRef } from 'ts2';
import { LeagueSidePanel } from './league-3-side-panel.component';

const leagueSidePanelRef = createRef('league_3_side_panel');

export const leagueSidePanelUi = (): UI => (
  <ui ref={leagueSidePanelRef} x="center" y="center">
    <LeagueSidePanel />
  </ui>
);
