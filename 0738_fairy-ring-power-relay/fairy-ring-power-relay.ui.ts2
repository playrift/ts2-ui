import { createRef } from 'ts2';
import { FairyRingPowerRelay } from './fairy-ring-power-relay.component';

const fairyRingPowerRelayRef = createRef('fairy-ring-power-relay');

export const fairyRingPowerRelayUi = (): UI => (
  <ui ref={fairyRingPowerRelayRef} w={484} h={304} x="center" y="center">
    <FairyRingPowerRelay />
  </ui>
);
