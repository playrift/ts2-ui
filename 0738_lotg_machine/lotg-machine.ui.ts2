import { createRef } from 'ts2';
import { FairyRingPowerRelay } from './lotg-machine.component';

const fairyRingPowerRelayRef = createRef('lotg_machine');

export const fairyRingPowerRelayUi = (): UI => (
  <ui ref={fairyRingPowerRelayRef} w={484} h={304} x="center" y="center">
    <FairyRingPowerRelay />
  </ui>
);
