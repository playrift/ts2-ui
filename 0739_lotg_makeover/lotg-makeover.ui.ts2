import { createRef } from 'ts2';
import { LotgMakeover } from './lotg-makeover.component';

const lotgMakeoverRef = createRef('lotg_makeover');

export const lotgMakeoverUi = (): UI => (
  <ui ref={lotgMakeoverRef} x="center" y="center">
    <LotgMakeover />
  </ui>
);
