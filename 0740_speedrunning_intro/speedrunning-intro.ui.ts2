import { createRef } from 'ts2';
import { SpeedrunningIntro } from './speedrunning-intro.component';

const speedrunningIntroRef = createRef('speedrunning_intro');

export const speedrunningIntroUi = (): UI => (
  <ui ref={speedrunningIntroRef}>
    <SpeedrunningIntro />
  </ui>
);
