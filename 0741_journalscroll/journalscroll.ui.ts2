import { createRef } from 'ts2';
import { JournalScroll } from './journalscroll.component';
import { Layout } from './journalscroll.constants';

const journalScrollRef = createRef('journalscroll');

export const journalScrollUi = (): UI => (
  <ui ref={journalScrollRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <JournalScroll />
  </ui>
);
