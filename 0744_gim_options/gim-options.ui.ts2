import { createRef } from 'ts2';

import { GimOptions } from './gim-options.component';

const gimOptionsRef = createRef('gim_options');

export const gimOptionsUi = (): UI => (
  <ui ref={gimOptionsRef}>
    <GimOptions />
  </ui>
);
