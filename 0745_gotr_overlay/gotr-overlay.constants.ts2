import { createRef } from 'ts2';

import { Animations } from '../lib/game/animations';

export const PORTAL_ANIM = Animations.POH_PORTAL_LOOP;

export const Wipe = {
  MODEL_OFFSET_Y: 130,
  START_ZOOM: 4000,
  ZOOM_NUM: 6464,
  ZOOM_DEN: 10000,
  MIN_ZOOM: 1,
  MIN_ZOOM_OUT: 2,
  CLEAR: 255,
  OPAQUE: 0,
  STEP: 12,
  CYCLES: 20,
};

export enum Fade {
  OUT = 0,
  IN = 1,
}

export const atmosphericRef = createRef('gotr_overlay_atmospheric');
