import { sessionState } from 'ts2';

import { Fade, Wipe } from './gotr-overlay.constants';

export const gotrWipeBuilt = sessionState(0);

export const gotrWipeModel = sessionState(0);

export const gotrWipeColour = sessionState(0);

export const gotrWipeZoom = sessionState(Wipe.START_ZOOM);

export const gotrWipeWash = sessionState(Wipe.CLEAR);

export const gotrWipeStartedAt = sessionState(-1);

export const gotrWipeDirection = sessionState(Fade.IN);

export const gotrWipeSteps = sessionState(0);
