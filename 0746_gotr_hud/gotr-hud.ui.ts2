import { createRef } from 'ts2';
import { GotrHud } from './gotr-hud.component';

const gotrHudRef = createRef('gotr_hud');

export const gotrHudUi = (): UI => (
  <ui ref={gotrHudRef}>
    <GotrHud />
  </ui>
);
