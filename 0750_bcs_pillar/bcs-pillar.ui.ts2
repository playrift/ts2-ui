import { createRef } from 'ts2';
import { BcsPillar } from './bcs-pillar.component';
import { Layout } from './bcs-pillar.constants';

const bcsPillarRef = createRef('bcs_pillar');

export const bcsPillarUi = (): UI => (
  <ui ref={bcsPillarRef} w={Layout.PANEL_W} h={Layout.PANEL_H} x="center" y="center">
    <BcsPillar />
  </ui>
);
