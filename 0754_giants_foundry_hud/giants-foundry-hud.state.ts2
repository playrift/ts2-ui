import { sessionState } from 'ts2';

export const foundryLaidOutDifficulty = sessionState(0);

export const foundrySeed1 = sessionState(0);
export const foundrySeed2 = sessionState(0);
export const foundrySeed3 = sessionState(0);
export const foundrySeed4 = sessionState(0);
export const foundrySeed5 = sessionState(0);
export const foundrySeed6 = sessionState(0);
