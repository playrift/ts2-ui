import { Build, SuppliesColor } from '../0758_pvp_arena_stagingarea_supplies/pvp-arena-stagingarea-supplies.constants';
import { binLabelA, binLabelB, binLabelC } from './pvp-arena-unrankedduel.refs';

export function binHover(build: Int, hot: Boolean): void {
  const colour: Int = hot ? SuppliesColor.WHITE : SuppliesColor.LABEL;
  if (build === Build.ZERKER) {
    binLabelB.color = colour;
    return;
  }
  if (build === Build.PURE) {
    binLabelC.color = colour;
    return;
  }
  binLabelA.color = colour;
}
