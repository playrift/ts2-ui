import { Fonts } from '../lib/game/fonts';
import { Sounds } from '../lib/game/sounds';
import { Varbits } from '../lib/game/varbits';

export declare const UnrankedVarbits: {
  readonly PROGRESS: typeof Varbits.PVPA_UNRANKEDDUEL_TRANSMIT_PROGRESS;
  readonly I_ACCEPT_BUILD: typeof Varbits.PVPA_UNRANKEDDUEL_TRANSMIT_IACCEPTBUILD;
  readonly I_ACCEPT_LOADOUT: typeof Varbits.PVPA_UNRANKEDDUEL_TRANSMIT_IACCEPTLOADOUT;
  readonly BUILD: typeof Varbits.PVPA_TRANSMIT_BUILD;
};

export declare const UnrankedFonts: {
  readonly BODY: typeof Fonts.P12_FULL;
  readonly TITLE: typeof Fonts.B12_FULL;
};

export declare const UnrankedSounds: {
  readonly SELECT: typeof Sounds.INTERFACE_SELECT1;
};

export enum UnrankedColor {
  LABEL = 0xff981f,
  HINT = 0xff9815,
  RULE = 0x5d5848,
  RULE_LIGHT = 0x5a5245,
  RULE_DARK = 0x383023,
}

export enum UnrankedTab {
  STATS = -1,
  MY_KIT = 0,
  OPPONENT = 1,
}

export const PROGRESS_AGREED = 2;

export const PRESS_CYCLES = 30;

export const Copy = {
  TAB_STATS: 'Stats',
  TAB_MAX_MED: 'Max/Med kit',
  TAB_ZERKER: 'Zerker kit',
  TAB_PURE: '1 Def Pure kit',
  TAB_OPPONENT: "Opponent's kit",
  BUILD_AGREED:
    "Now you've agreed on a combat style, customise your <col=ffb83f>load-out</col> and check your opponent's.",
  BUILD_WAIT: 'Please <col=ffb83f>wait</col> while your opponent considers your choice.',
  BUILD_READY: 'When you are satisfied with the combat style, select <col=ffb83f>Confirm</col>.',
  LOADOUT_WAIT: 'Please <col=ffb83f>wait</col> while your opponent considers your load-out.',
  LOADOUT_READY:
    "Don't forget to <col=ffb83f>check your load-out</col> for <img=11> warnings!<br><br>When you are satisfied with it, and with <col=ffb83f>your opponent's load-out</col>, select <col=ffb83f>Confirm</col>.",
  BUTTON_DASHES: '<col=9f9f9f>---</col>',
  BUTTON_CONFIRMED: '<col=9f9f9f>Confirmed</col>',
  BUTTON_CONFIRM: '<col=0dc10d>Confirm</col>',
  BUTTON_COPY: '<col=0dc10d>Copy Load-out</col>',
  BUTTON_COPY_GREY: '<col=9f9f9f>Copy Load-out</col>',
  OP_CONFIRM: 'Confirm',
  OP_COPY: 'Copy Load-out',
} as const;

export const Layout = {
  root: { w: 500, h: 326 },
  title: { y: 6, inset: 12, h: 23 },
  body: { inset: 20, drop: 50, bottom: 10 },
  tabs: { inset: 2, h: 21, statsW: 80, kitX: 80, kitW: 110, opponentX: 190, opponentW: 150 },
  rule: { h: 1 },
  page: { drop: 22 },
  strip: { h: 58, backingInset: 2, backingNudge: -1 },
  stats: {
    bottom: 15,
    w: 130,
    h: 170,
    backingInset: 4,
    ruleX: 64,
    ruleW: 1,
    ruleDrop: 4,
    columnInset: 2,
    columnW: 62,
    columnDrop: 4,
  },
  fighter: { w: 170, h: 110, shortH: 95, leftBottom: 85, rightBottom: 100 },
  buildHint: { right: 0, bottom: 40, w: 170, h: 60 },
  buildConfirm: { right: 35, bottom: 0, w: 100, h: 40 },
  buildSpare: { right: 0, bottom: 0, w: 170, h: 40 },
  loadoutHint: { y: 20, inset: 316, drop: 112, lineHeight: 15 },
  loadoutSpare: { bottom: 20, inset: 316, h: 35 },
  loadoutConfirm: { right: 30, bottom: 20, inset: 376, h: 35 },
  loadoutCopy: { right: 30, bottom: 56, inset: 376, h: 35 },
} as const;

export enum FighterModel {
  LEFT = 4502,
  CENTRE = 4496,
  RIGHT = 4504,
}

export const FighterCamera = {
  left: { zoom: 160, pitch: 512, yaw: 1779, offsetY: 4 },
  centre: { zoom: 210, pitch: 512, yaw: 119 },
  right: { zoom: 155, pitch: 512, yaw: 11 },
} as const;
