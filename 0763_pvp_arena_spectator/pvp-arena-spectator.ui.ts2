import { createRef } from 'ts2';
import { PvpArenaSpectator } from './pvp-arena-spectator.component';

const pvpArenaSpectatorRef = createRef('pvp_arena_spectator');

export const pvpArenaSpectatorUi = (): UI => (
  <ui ref={pvpArenaSpectatorRef}>
    <PvpArenaSpectator />
  </ui>
);
