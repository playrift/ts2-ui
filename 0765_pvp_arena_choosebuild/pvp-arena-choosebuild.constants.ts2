import { Enums } from '../lib/game/enums';
import { Fonts } from '../lib/game/fonts';
import { Sounds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';
import { Varbits } from '../lib/game/varbits';

export declare const ChooseBuildFonts: {
  readonly LABEL: typeof Fonts.P12_FULL;
  readonly BOLD: typeof Fonts.B12_FULL;
};

export declare const ChooseBuildSprites: {
  readonly STRIP_BACKING: typeof Sprites.TRADEBACKING_DARK;
  readonly STATS_BACKING: typeof Sprites.TRADEBACKING_LIGHT;
  readonly RADIO_OFF: typeof Sprites.OPTIONS_RADIO_BUTTONS_0;
  readonly RADIO_OFF_DIM: typeof Sprites.OPTIONS_RADIO_BUTTONS_1;
  readonly RADIO_ON: typeof Sprites.OPTIONS_RADIO_BUTTONS_2;
};

export declare const ChooseBuildSounds: {
  readonly SELECT: typeof Sounds.INTERFACE_SELECT1;
};

export declare const SkillIcons: typeof Enums.ENUM_255;

export declare const ChooseBuildVarbits: {
  readonly BATTLE_TYPE: typeof Varbits.PVPA_TRANSMIT_BATTLETYPE;
  readonly BUILD: typeof Varbits.PVPA_TRANSMIT_BUILD;
  readonly CAN_EDIT_BUILD: typeof Varbits.PVPA_TRANSMIT_CANEDITBUILD;
};

export enum Build {
  MAX_MED = 0,
  ZERKER = 1,
  PURE = 2,
}

export enum ChooseBuildSkill {
  ATTACK = 0,
  DEFENCE = 1,
  STRENGTH = 2,
  HITPOINTS = 3,
  RANGED = 4,
  PRAYER = 5,
  MAGIC = 6,
  AGILITY = 16,
}

export enum SettledBattleType {
  A = 1001,
  B = 1002,
  C = 1005,
  D = 1006,
}

export enum ChooseBuildColor {
  LABEL = 0xff981f,
  HIGHLIGHT = 0xffffff,
  RULE_LIGHT = 0x5a5245,
  RULE_DARK = 0x383023,
}

export enum RowWash {
  REST = 255,
  HOVER = 225,
  PRESSED = 200,
}

export enum RadioWash {
  LIT = 0,
  DIMMED = 125,
}

export const FLASH_CYCLES = 20;

export const LABEL_MEASURE_WIDTH = 512;

export const STONEBUTTON_OP_SOUND_MASK = 2046;

export const ChooseBuildLayout = {
  root: { w: 350, h: 280 },
  body: { inset: 20, drop: 50, bottom: 10 },
  strip: { h: 58 },
  stripBacking: { inset: 2, nudge: -1 },
  buildRow: {
    pad: 28,
    labelInset: 3,
    radioGap: 5,
    h: 25,
    bottom: 5,
    radio: 17,
    radioBottom: 9,
    headingY: 2,
    headingH: 23,
    headingInset: 10,
  },
  stats: { w: 130, drop: 60 },
  statsBacking: { inset: 4 },
  statsRule: { x: 64, w: 1, drop: 4 },
  statsColumn: { inset: 2, w: 62, drop: 4 },
  statRow: {
    first: 7,
    pitch: 42,
    ruleLead: 9,
    icon: 25,
    iconX: 5,
    valueH: 25,
    valueInset: 30,
  },
  fighter: { w: 100, h: 100, raise: 65 },
  confirm: { w: 86, h: 40, inset: 5 },
};

export enum ChooseBuildModel {
  LEFT = 4502,
  CENTRE = 4496,
  RIGHT = 4504,
}

export const FighterCamera = {
  left: { zoom: 180, pitch: 512, yaw: 1842, offsetY: 5 },
  centre: { zoom: 250, pitch: 512, yaw: 119, offsetY: -5 },
  right: { zoom: 200, pitch: 512, yaw: 1864, offsetX: 1, offsetY: -6 },
};
