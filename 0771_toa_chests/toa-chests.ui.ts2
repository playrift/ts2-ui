import { createRef } from 'ts2';
import { ToaChests } from './toa-chests.component';

const toaChestsRef = createRef('toa_chests');

export const toaChestsUi = (): UI => (
  <ui ref={toaChestsRef} w={240} h={220} x="center" y="center">
    <ToaChests />
  </ui>
);
