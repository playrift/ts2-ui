import { Fonts } from '../lib/game/fonts';
import { Sprites } from '../lib/game/sprites';

export declare const ToaLobbyFonts: {
  readonly HEADER: typeof Fonts.B12_FULL;
  readonly NAMES: typeof Fonts.P12_FULL;
};

export declare const ToaLobbySprite: {
  readonly BACKING: typeof Sprites.TRADEBACKING;
};

export enum ToaLobbyColor {
  LABEL = 0xff981f,
}

export enum PartyStatus {
  NONE = 0,
  FORMING = 1,
  READY = 2,
}

export enum Flash {
  PERIOD = 90,
  HALF = 45,
  PEAK = 11475,
  FULL = 255,
  EASE = 2,
}

export enum ToaLobbyDodge {
  WORD = 52968,
}

export enum Layout {
  CONTENT_X = 2,
  CONTENT_Y = 2,
  CONTENT_W = 124,
  CONTENT_H = 151,
  DIVIDER = -1,
  HEADER_INSET = 8,
  HEADER_Y = 4,
  HEADER_H = 20,
  NAMES_INSET = 8,
  NAMES_H_INSET = 28,
  NAMES_BOTTOM = 4,
  NAMES_LINE_H = 15,
  BACKING_TRANSPARENCY = 80,
}
