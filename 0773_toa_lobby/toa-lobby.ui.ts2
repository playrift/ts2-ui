import { createRef } from 'ts2';
import { ToaLobby } from './toa-lobby.component';

const toaLobbyRef = createRef('toa_lobby');

export const toaLobbyUi = (): UI => (
  <ui ref={toaLobbyRef}>
    <ToaLobby />
  </ui>
);
