import { useState } from 'ts2';

import { raidsCount, raidsCount2 } from '../lib/game/varcs';

export function resetToaPartyDetails(): void {
  const [, setMembers] = useState(raidsCount);
  const [, setApplicants] = useState(raidsCount2);
  setMembers(0);
  setApplicants(0);
}
