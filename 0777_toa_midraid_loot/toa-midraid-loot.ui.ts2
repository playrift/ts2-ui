import { createRef } from 'ts2';
import { ToaMidraidLoot } from './toa-midraid-loot.component';

const toaMidraidLootRef = createRef('toa_midraid_loot');

export const toaMidraidLootUi = (): UI => (
  <ui ref={toaMidraidLootRef}>
    <ToaMidraidLoot />
  </ui>
);
