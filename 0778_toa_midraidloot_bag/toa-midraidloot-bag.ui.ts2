import { createRef } from 'ts2';
import { ToaMidraidlootBag } from './toa-midraidloot-bag.component';

const toaMidraidlootBagRef = createRef('toa_midraidloot_bag');

export const toaMidraidlootBagUi = (): UI => (
  <ui ref={toaMidraidlootBagRef} w={190} h={261}>
    <ToaMidraidlootBag />
  </ui>
);
