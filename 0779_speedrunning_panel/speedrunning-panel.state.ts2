import { sessionState } from 'ts2';

export const speedrunClockRunning = sessionState(0);

export const speedrunClockOffset = sessionState(0);

export const speedrunClockPrecise = sessionState(0);

export const speedrunClockSeparate = sessionState(0);

export const speedrunClockText = sessionState('-');
