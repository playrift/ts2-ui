import { createRef } from 'ts2';
import { SpeedrunningRewards } from './speedrunning-rewards.component';
import { RewardFrame } from './speedrunning-rewards.constants';

const speedrunningRewardsRef = createRef('speedrunning_rewards');

export const speedrunningRewardsUi = (): UI => (
  <ui ref={speedrunningRewardsRef} w={RewardFrame.ROOT_W} h={RewardFrame.ROOT_H}>
    <SpeedrunningRewards />
  </ui>
);
