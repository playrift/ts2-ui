import { createRef } from 'ts2';
import { FswSpPoints } from './fsw-sp-points.component';

const fswSpPointsRef = createRef('fsw_sp_points');

export const fswSpPointsUi = (): UI => (
  <ui ref={fswSpPointsRef}>
    <FswSpPoints />
  </ui>
);
