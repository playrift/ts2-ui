import { createRef } from 'ts2';
import { FswSpInfo } from './fsw-sp-info.component';

const fswSpInfoRef = createRef('fsw_sp_info');

export const fswSpInfoUi = (): UI => (
  <ui ref={fswSpInfoRef}>
    <FswSpInfo />
  </ui>
);
