import { createRef } from 'ts2';
import { FswSpOptout } from './fsw-sp-optout.component';

const fswSpOptoutRef = createRef('fsw_sp_optout');

export const fswSpOptoutUi = (): UI => (
  <ui ref={fswSpOptoutRef}>
    <FswSpOptout />
  </ui>
);
