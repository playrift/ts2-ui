import { createRef } from 'ts2';
import { FswSp } from './fsw-sp.component';

const fswSpRef = createRef('fsw_sp');

export const fswSpUi = (): UI => (
  <ui ref={fswSpRef} x="center" y="center">
    <FswSp />
  </ui>
);
