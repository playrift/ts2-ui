import { sessionState } from 'ts2';

export const fswOptinBuilt = sessionState(0);

export const fswOptinPublishing = sessionState(0);

export const fswOptinChoice = sessionState(0);

export const fswOptinFinding = sessionState(0);

export const fswOptinInfo = sessionState(0);

export const fswOptinRemaining = sessionState('');

export const fswOptinTransfer = sessionState('');
