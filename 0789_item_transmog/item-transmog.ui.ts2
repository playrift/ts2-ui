import { createRef } from 'ts2';
import { ItemTransmog } from './item-transmog.component';

const itemTransmogRef = createRef('item_transmog');

export const itemTransmogUi = (): UI => (
  <ui ref={itemTransmogRef} x="center" y="center">
    <ItemTransmog />
  </ui>
);
