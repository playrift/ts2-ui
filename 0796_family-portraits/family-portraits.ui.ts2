import { createRef } from 'ts2';
import { FamilyPortraits } from './family-portraits.component';

const familyPortraitsRef = createRef('family-portraits');

export const familyPortraitsUi = (): UI => (
  <ui ref={familyPortraitsRef} w={512} h={334}>
    <FamilyPortraits />
  </ui>
);
