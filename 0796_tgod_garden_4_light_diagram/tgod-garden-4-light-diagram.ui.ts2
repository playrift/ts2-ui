import { createRef } from 'ts2';
import { FamilyPortraits } from './tgod-garden-4-light-diagram.component';

const familyPortraitsRef = createRef('tgod_garden_4_light_diagram');

export const familyPortraitsUi = (): UI => (
  <ui ref={familyPortraitsRef} w={512} h={334}>
    <FamilyPortraits />
  </ui>
);
