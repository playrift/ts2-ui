import { createRef } from 'ts2';

import { OmnishopSide } from './omnishop-side.component';

const omnishopSide = createRef('omnishop_side');

export const omnishopSideUi = (): UI => (
  <ui ref={omnishopSide}>
    <OmnishopSide />
  </ui>
);
