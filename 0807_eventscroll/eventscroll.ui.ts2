import { createRef } from 'ts2';
import { EventScroll } from './eventscroll.component';

const eventScrollRef = createRef('eventscroll');

export const eventScrollUi = (): UI => (
  <ui ref={eventScrollRef}>
    <EventScroll />
  </ui>
);
