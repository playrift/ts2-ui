import { createRef } from 'ts2';
import { Scroll } from './scroll.component';

const scrollRef = createRef('scroll');

export const scrollUi = (): UI => (
  <ui ref={scrollRef}>
    <Scroll />
  </ui>
);
