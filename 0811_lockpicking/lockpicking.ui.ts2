import { createRef } from 'ts2';
import { Lockpicking } from './lockpicking.component';

const lockpickingRef = createRef('lockpicking');

export const lockpickingUi = (): UI => (
  <ui ref={lockpickingRef} w={512} h={334} x="center" y="center">
    <Lockpicking />
  </ui>
);
