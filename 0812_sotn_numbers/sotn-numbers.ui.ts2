import { createRef } from 'ts2';
import { SotnNumbers } from './sotn-numbers.component';

const sotnNumbersRef = createRef('sotn_numbers');

export const sotnNumbersUi = (): UI => (
  <ui ref={sotnNumbersRef}>
    <SotnNumbers />
  </ui>
);
