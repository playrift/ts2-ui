import { createRef } from 'ts2';
import { SotnMap } from './sotn-map.component';

const sotnMapRef = createRef('sotn_map');

export const sotnMapUi = (): UI => (
  <ui ref={sotnMapRef}>
    <SotnMap />
  </ui>
);
