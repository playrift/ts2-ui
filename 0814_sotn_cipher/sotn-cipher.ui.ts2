import { createRef } from 'ts2';
import { SotnCipher } from './sotn-cipher.component';

const sotnCipherRef = createRef('sotn_cipher');

export const sotnCipherUi = (): UI => (
  <ui ref={sotnCipherRef}>
    <SotnCipher />
  </ui>
);
