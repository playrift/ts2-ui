import { createRef } from 'ts2';
import { SotnList } from './sotn-list.component';

const sotnListRef = createRef('sotn_list');

export const sotnListUi = (): UI => (
  <ui ref={sotnListRef}>
    <SotnList />
  </ui>
);
