import { createRef, useInterval } from 'ts2';

import { Fade, OverlayColor } from './muspah-overlay.constants';
import { fadeTintBox } from './muspah-overlay.helpers';

const timerHost = createRef('0818_muspah_overlay_timer_host');

export function MuspahOverlay(): Component {
  useInterval(() => fadeTintBox(timerHost), timerHost);

  return (
    <layer x="center" y="center" w="fill" h="fill">
      <rect w="fill" h="fill" color={OverlayColor.TINT} fill={true} transparency={Fade.REST} ref={timerHost} />

      <rect x="center" y="center" w="fill" h="fill" color={OverlayColor.BLACK} fill={true} transparency={Fade.REST} />

      <rect x="center" y="center" w="fill" h="fill" color={OverlayColor.BLACK} fill={true} transparency={Fade.REST} />
    </layer>
  );
}
