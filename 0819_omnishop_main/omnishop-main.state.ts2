import { sessionState } from 'ts2';

export const omnishopShop = sessionState(-1);

export const omnishopInventory = sessionState(-1);

export const omnishopCalls = sessionState(0);

export const omnishopSelectedSlot = sessionState(-1);

export const omnishopColumnOpen = sessionState(-1);

export const omnishopInfoBuiltAt = sessionState(0);

export const omnishopRingLead = sessionState(0);
