import { createRef } from 'ts2';
import { WorldSwitcherFilter } from './worldswitcher-filter.component';

const worldSwitcherFilterRef = createRef('worldswitcher_filter');

export const worldSwitcherFilterUi = (): UI => (
  <ui ref={worldSwitcherFilterRef} x="center" y="center">
    <WorldSwitcherFilter />
  </ui>
);
