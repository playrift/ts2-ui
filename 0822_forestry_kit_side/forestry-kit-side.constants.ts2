export { ForestryKitItems, isForestryKitItem } from './forestry-kit-items.data';

import { Inventories } from '../lib/game/inventories';
import { Sounds as SoundIds } from '../lib/game/sounds';
import { Varbits } from '../lib/game/varbits';

export declare const ForestryKit: {
  readonly INV: typeof Inventories.INV;
};

export declare const ForestryKitVarbits: {
  readonly QUANTITY: typeof Varbits.FORESTRY_KIT_SELECTEDQUANTITY;
};

export enum Quantity {
  ONE = 0,
  FIVE = 1,
  ALL = 2,
  X = 3,
}

export declare const ForestryKitSounds: {
  readonly CLICK: typeof SoundIds.INTERFACE_SELECT1;
};

export enum Grid {
  W = 36,
  H = 32,
  COLUMNS = 4,
  STRIDE_X = 42,
  STRIDE_Y = 36,
  OUTLINE = 1,
  DRAG_DEAD = 5,
}

export enum ForestryKitColor {
  SLOT_SHADOW = 0x333333,
}

export enum Transparency {
  STORABLE = 0,
  STORABLE_FLASH = 120,
  REFUSED = 150,
  REFUSED_FLASH = 200,
  BLANK = 255,
}

export const BLANK_ITEM = 6512;

export const EXAMINE_OP = 10;

export enum Root {
  X = 16,
  Y = 8,
  W = 162,
  H = 250,
}
