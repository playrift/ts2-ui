import { createRef } from 'ts2';
import { MorseCodeConverter } from './morse-code-converter.component';

const morseCodeConverterRef = createRef('morse_code_converter');

export const morseCodeConverterUi = (): UI => (
  <ui ref={morseCodeConverterRef}>
    <MorseCodeConverter />
  </ui>
);
