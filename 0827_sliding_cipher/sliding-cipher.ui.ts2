import { createRef } from 'ts2';

import { SlidingCipher } from './sliding-cipher.component';

const slidingCipherRef = createRef('sliding_cipher');

export const slidingCipherUi = (): UI => (
  <ui ref={slidingCipherRef} x="center" y="center">
    <SlidingCipher />
  </ui>
);
