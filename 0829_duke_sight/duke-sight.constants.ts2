import { Animations } from '../lib/game/animations';

export enum DukeSightModel {
  EYE = 48318,
}

export const Camera = {
  ZOOM: 1399,
};

export declare const DukeSightAnim: {
  readonly EYE_IDLE: typeof Animations.INTERFACE_ICON_CTHONIAN01_IDLE;
};

export const DukeSightColor = {
  FLASH: 0xffffff,
};

export const Layout = {
  flash: { w: 606, h: 418 },
};
