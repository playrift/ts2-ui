import { createRef } from 'ts2';
import { DukeSight } from './duke-sight.component';

const dukeSightRef = createRef('duke_sight');

export const dukeSightUi = (): UI => (
  <ui ref={dukeSightRef} x="center" y="center">
    <DukeSight />
  </ui>
);
