import { sessionState } from 'ts2';

export const qteIntroCalls = sessionState(0);

export const qteIntroCount = sessionState(0);

export const qteIntroSettleAt = sessionState(-1);

export const qteOutroCalls = sessionState(0);

export const qteOutroCloseAt = sessionState(-1);
