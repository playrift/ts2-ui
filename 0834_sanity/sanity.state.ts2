import { sessionState } from 'ts2';

export const sanityBarPx = sessionState(0);

export const sanityWalkTarget = sessionState(-1);

export const sanityWalkTrackPx = sessionState(-1);

export const sanityMeasuredPx = sessionState(-1);

export const sanityLaidOutAt = sessionState(-1);

export const sanityGlyphAlpha = sessionState(0);
