import { createRef } from 'ts2';
import { Sanity_ } from './sanity.component';

const sanityRef = createRef('sanity');

export const sanityUi = (): UI => (
  <ui ref={sanityRef}>
    <Sanity_ />
  </ui>
);
