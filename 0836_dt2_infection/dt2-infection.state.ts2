import { sessionState } from 'ts2';

export const infectionBarPx = sessionState(0);

export const infectionWalkTarget = sessionState(-1);

export const infectionWalkTrackPx = sessionState(-1);

export const infectionMeasuredPx = sessionState(-1);

export const infectionLaidOutAt = sessionState(-1);

export const infectionGlyphAlpha = sessionState(0);
