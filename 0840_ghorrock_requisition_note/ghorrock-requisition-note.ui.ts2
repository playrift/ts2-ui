import { createRef } from 'ts2';
import { GhorrockRequisitionNote } from './ghorrock-requisition-note.component';

const ghorrockRequisitionNoteRef = createRef('ghorrock_requisition_note');

export const ghorrockRequisitionNoteUi = (): UI => (
  <ui ref={ghorrockRequisitionNoteRef}>
    <GhorrockRequisitionNote />
  </ui>
);
