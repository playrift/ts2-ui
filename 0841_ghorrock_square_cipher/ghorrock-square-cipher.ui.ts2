import { createRef } from 'ts2';
import { GhorrockSquareCipher } from './ghorrock-square-cipher.component';

const ghorrockSquareCipherRef = createRef('ghorrock_square_cipher');

export const ghorrockSquareCipherUi = (): UI => (
  <ui ref={ghorrockSquareCipherRef}>
    <GhorrockSquareCipher />
  </ui>
);
