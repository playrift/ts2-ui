import { createRef } from 'ts2';
import { DeadmanSp } from './deadman-sp.component';

const deadmanSpRef = createRef('deadman_sp');

export const deadmanSpUi = (): UI => (
  <ui ref={deadmanSpRef} x="center" y="center">
    <DeadmanSp />
  </ui>
);
