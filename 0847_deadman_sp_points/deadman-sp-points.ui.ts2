import { createRef } from 'ts2';
import { DeadmanSpPoints } from './deadman-sp-points.component';

const deadmanSpPointsRef = createRef('deadman_sp_points');

export const deadmanSpPointsUi = (): UI => (
  <ui ref={deadmanSpPointsRef}>
    <DeadmanSpPoints />
  </ui>
);
