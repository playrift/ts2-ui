import { itemName } from '../lib/lib.engine';
import { onMobile } from '../lib/lib.helpers';

export function examineLabel(target: Item): String {
  return `Examine <col=ff9040>${itemName(target)}</col>`;
}
