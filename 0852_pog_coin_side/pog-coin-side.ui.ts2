import { createRef } from 'ts2';

const pogCoinSide = createRef('pog_coin_side');

export function PogCoinSide(): Component {
  return <layer x={16} y={8} w={162} h={250} />;
}

export const pogCoinSideUi = (): UI => (
  <ui ref={pogCoinSide}>
    <PogCoinSide />
  </ui>
);
