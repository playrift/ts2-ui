import { createRef } from 'ts2';
import { BondMain } from './bond-main.component';

const bondMainRef = createRef('bond_main');

export const bondMainUi = (): UI => (
  <ui ref={bondMainRef} x="center" y="center">
    <BondMain />
  </ui>
);
