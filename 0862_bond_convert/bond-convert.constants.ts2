import { Fonts } from '../lib/game/fonts';
import { Inventories } from '../lib/game/inventories';
import { Sounds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';
import { Varbits } from '../lib/game/varbits';
import { Varps } from '../lib/game/varps';

export declare const ConvertFonts: {
  readonly LABEL: typeof Fonts.P12_FULL;
  readonly VALUE: typeof Fonts.B12_FULL;
};

export declare const ConvertSprites: {
  readonly BACKING: typeof Sprites.TRADEBACKING_LIGHT;
  readonly FIELD_LEFT: typeof Sprites.GE_TEXTBACKDROP_0;
  readonly FIELD_MIDDLE: typeof Sprites.GE_TEXTBACKDROP_1;
  readonly FIELD_RIGHT: typeof Sprites.GE_TEXTBACKDROP_2;
};

export declare const ConvertSounds: {
  readonly PRESS: typeof Sounds.INTERFACE_SELECT1;
  readonly REFUSED: typeof Sounds.PILLORY_WRONG;
};

export declare const ConvertVarps: {
  readonly CONVERSION_COST: typeof Varps.IF3;
  readonly BANK_COINS: typeof Varps.IF4;
};

export declare const ConvertVarbits: {
  readonly IRONMAN: typeof Varbits.IRONMAN;
};

export declare const ConvertInventories: {
  readonly BACKPACK: typeof Inventories.INV;
};

export enum ConvertItems {
  BOND_UNTRADEABLE = 13192,
  COINS = 995,
}

export enum ConvertColors {
  LABEL = 0xff981f,
  VALUE = 0xffffff,
  SLOT_SHADOW = 0x333333,
  FADER = 0x000000,
}

export enum Fade {
  CLEAR = 255,
  DIM = 150,
  CYCLES = 30,
}

export const MAX_INT = 2147483647;

export const SEPARATOR = ',';

export const Copy = {
  TITLE: 'Convert Bonds',
  AVAILABLE: 'Available items:',
  NUMBER: 'Number to convert:',
  COST: 'Cost:',
  NO_COST: '<col=9f9f9f>---</col>',
  CANCEL: '<col=ff0000>Cancel</col>',
  ACCEPT: '<col=0dc10d>Accept</col>',
  MINUS_FIVE: '-5',
  MINUS_ONE: '-1',
  PLUS_ONE: '+1',
  PLUS_FIVE: '+5',
  ALL: 'All',
  NO_BONDS: "You don't have any more untradeable bonds in your inventory.",
  NO_COINS: "You don't have enough coins in your inventory and bank.",
};

export enum Layout {
  CLAMP_INSET_X = 12,
  CLAMP_INSET_Y = 41,
  CLAMP_BOTTOM = 6,
  WINDOW_W = 440,
  WINDOW_H = 235,
  CONTENTS_INSET_X = 20,
  CONTENTS_INSET_Y = 50,
  CONTENTS_BOTTOM = 10,
  PANE_INSET = 170,
  BLURB_GAP = 5,
  FORM_INSET = 12,

  ROW_ITEMS_Y = 0,
  ROW_ITEMS_H = 32,
  SLOT_W = 36,
  SLOT_H = 32,
  ROW_NUMBER_Y = 40,
  ROW_H = 20,
  FIELD_W = 55,
  CAP_W = 4,
  ROW_STEPPERS_Y = 68,
  STEPPER = 35,
  STEPPERS = 5,
  STEP_FIVE = 5,
  ROW_COST_Y = 111,
  COST_PADDING = 10,
  FOOT_Y = 139,
  BUTTON_PADDING = 30,
  LINE_HEIGHT = 14,
}
