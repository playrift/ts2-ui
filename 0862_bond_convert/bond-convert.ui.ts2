import { createRef } from 'ts2';
import { BondConvert } from './bond-convert.component';

const bondConvertRef = createRef('bond_convert');

export const bondConvertUi = (): UI => (
  <ui ref={bondConvertRef}>
    <BondConvert />
  </ui>
);
