import { createRef } from 'ts2';
import { BondManagement } from './bond-management.component';
import { Layout } from './bond-management.constants';

const bondManagementRef = createRef('bond_management');

export const bondManagementUi = (): UI => (
  <ui ref={bondManagementRef} w={Layout.PANEL_W} h={Layout.PANEL_H} x="center" y="center">
    <BondManagement />
  </ui>
);
