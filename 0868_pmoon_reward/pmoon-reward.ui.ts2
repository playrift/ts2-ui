import { createRef } from 'ts2';
import { PmoonReward } from './pmoon-reward.component';
import { Layout } from './pmoon-reward.constants';

const pmoonRewardRef = createRef('pmoon_reward');

export const pmoonRewardUi = (): UI => (
  <ui ref={pmoonRewardRef} w={Layout.ROOT_W} h={Layout.ROOT_H} x="center" y="center">
    <PmoonReward />
  </ui>
);
