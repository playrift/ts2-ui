import { createRef } from 'ts2';
import { Layout } from './fortis-museum.constants';
import { FortisMuseum } from './fortis-museum.component';

const fortisMuseumRef = createRef('fortis_museum');

export const fortisMuseumUi = (): UI => (
  <ui ref={fortisMuseumRef} w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <FortisMuseum />
  </ui>
);
