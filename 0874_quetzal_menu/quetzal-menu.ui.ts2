import { createRef } from 'ts2';
import { QuetzalMenu } from './quetzal-menu.component';

const quetzalMenuRef = createRef('quetzal_menu');

export const quetzalMenuUi = (): UI => (
  <ui ref={quetzalMenuRef}>
    <QuetzalMenu />
  </ui>
);
