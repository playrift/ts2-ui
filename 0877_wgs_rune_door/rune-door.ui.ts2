import { createRef } from 'ts2';
import { RuneDoor } from './rune-door.component';

const runeDoorRef = createRef('wgs_rune_door');

export const runeDoorUi = (): UI => (
  <ui ref={runeDoorRef}>
    <RuneDoor />
  </ui>
);
