import { createRef } from 'ts2';

import { PrepotDevice } from './prepot-device.component';

const prepotDeviceRef = createRef('prepot_device');

export const prepotDeviceUi = (): UI => (
  <ui ref={prepotDeviceRef}>
    <PrepotDevice />
  </ui>
);
