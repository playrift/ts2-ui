import { createRef } from 'ts2';

export const numberPad = createRef('number_pad');
export const padRef = createRef('number_pad:pad');
export const displayRef = createRef('number_pad:display');
export const confirmRef = createRef('number_pad:confirm_button');
export const closeRef = createRef('number_pad:close_button');
