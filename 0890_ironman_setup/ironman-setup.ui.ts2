import { createRef } from 'ts2';
import { IronmanSetup } from './ironman-setup.component';

const ironmanSetupRef = createRef('ironman_setup');

export const ironmanSetupUi = (): UI => (
  <ui ref={ironmanSetupRef} x="center" y="center">
    <IronmanSetup />
  </ui>
);
