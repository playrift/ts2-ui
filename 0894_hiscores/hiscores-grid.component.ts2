import { Cell, HiscoresColour, HiscoresFonts } from './hiscores.constants';
import { HiscoresActivities, HiscoresBosses, HiscoresSkills } from './hiscores.data';

export function SkillCells({ third, top }: { third: Int; top: Int }): Component {
  return (
    <>
      {HiscoresSkills.map((row, i) => (
        <>
          <sprite
            x={(i % 3) * third + third / 2 - Cell.ICON}
            y={top + (i / 3) * Cell.PITCH}
            w={Cell.ICON}
            h={Cell.ICON}
            sprite={row.icon}
          />
          <text
            x={(i % 3) * third + third / 2}
            y={top + (i / 3) * Cell.PITCH}
            w={Cell.VALUE_W}
            h={Cell.ICON}
            font={HiscoresFonts.VALUE}
            shadow
            color={HiscoresColour.ORANGE}
            align="center"
            valign="middle"
            text={Cell.PLACEHOLDER}
          />
        </>
      ))}
    </>
  );
}

export function ActivityCells({ third, top }: { third: Int; top: Int }): Component {
  return (
    <>
      {HiscoresActivities.map((row, i) => (
        <>
          <sprite
            x={(i % 3) * third + third / 2 - Cell.ICON}
            y={top + (i / 3) * Cell.PITCH}
            w={Cell.ICON}
            h={Cell.ICON}
            sprite={row.icon}
          />
          <text
            x={(i % 3) * third + third / 2}
            y={top + (i / 3) * Cell.PITCH}
            w={Cell.VALUE_W}
            h={Cell.ICON}
            font={HiscoresFonts.VALUE}
            shadow
            color={HiscoresColour.ORANGE}
            align="center"
            valign="middle"
            text={Cell.PLACEHOLDER}
          />
        </>
      ))}
    </>
  );
}

export function BossCells({ third, top }: { third: Int; top: Int }): Component {
  return (
    <>
      {HiscoresBosses.map((row, i) => (
        <>
          <sprite
            x={(i % 3) * third + third / 2 - Cell.ICON}
            y={top + (i / 3) * Cell.PITCH}
            w={Cell.ICON}
            h={Cell.ICON}
            sprite={row.icon}
          />
          <text
            x={(i % 3) * third + third / 2}
            y={top + (i / 3) * Cell.PITCH}
            w={Cell.VALUE_W}
            h={Cell.ICON}
            font={HiscoresFonts.VALUE}
            shadow
            color={HiscoresColour.ORANGE}
            align="center"
            valign="middle"
            text={Cell.PLACEHOLDER}
          />
        </>
      ))}
    </>
  );
}
