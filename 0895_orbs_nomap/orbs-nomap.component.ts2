import { createRef } from 'ts2';

import { HealthOrb, PrayerOrb, RunOrb, SpecOrb } from '../0160_orbs/orb.component';
import {
  AdviserButton,
  StoreButton,
  WikiButton,
  WorldMapButton,
  XpDropsButton,
} from '../0160_orbs/orbs-buttons.component';
import { GridMasterButton, MapTooltip } from '../0160_orbs/orbs-extras.component';
import { GridMasterScript, OrbsPanel } from '../0160_orbs/orbs.constants';

const gridMountRef = createRef('orbs_nomap_grid1');

export function OrbsNomap(): Component {
  return (
    <>
      <layer static ref={gridMountRef} x={0} y={0} w={0} h={0}>
        <sprite x={0} y={0} w={0} h={0} hide />
      </layer>
      <layer x="right" y={0} w={155} h={140}>
        <XpDropsButton left={106} top={38} fromRight />
        <HealthOrb left={38} top={0} fromRight wide={false} clickY={5} clickW={50} clickH={26} />
        <PrayerOrb left={0} top={31} fromRight wide={false} clickY={5} clickW={49} clickH={26} />
        <RunOrb left={98} top={0} fromRight wide={false} clickY={5} clickW={50} clickH={26} />
        <SpecOrb
          left={0}
          top={68}
          fromRight
          panel={OrbsPanel.NOMAP}
          wide={false}
          clickY={5}
          clickW={50}
          clickH={26}
          subject=""
        />
        <StoreButton panel={OrbsPanel.NOMAP} filler wholeClick />
        <AdviserButton panel={OrbsPanel.NOMAP} wholeClick />
        <WorldMapButton panel={OrbsPanel.NOMAP} spare />
        <WikiButton panel={OrbsPanel.NOMAP} compact={0} subject="" />
      </layer>
      <MapTooltip />
      <GridMasterButton mount={gridMountRef} script={GridMasterScript.NOMAP} />
    </>
  );
}
