import { createRef } from 'ts2';
import { OrbsNomap } from './orbs-nomap.component';

const orbsNomapRef = createRef('orbs_nomap');

export const orbsNomapUi = (): UI => (
  <ui ref={orbsNomapRef} w="fill" h="fill">
    <OrbsNomap />
  </ui>
);
