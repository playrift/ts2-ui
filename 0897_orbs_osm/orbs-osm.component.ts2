import { createRef } from 'ts2';

import { HealthOrb, PrayerOrb, RunOrb, SpecOrb } from '../0160_orbs/orb.component';
import {
  AdviserButton,
  StoreButton,
  WikiButton,
  WorldMapButton,
  XpDropsButton,
} from '../0160_orbs/orbs-buttons.component';
import { GridMasterButton } from '../0160_orbs/orbs-extras.component';
import { GridMasterScript, OrbText, OrbsPanel } from '../0160_orbs/orbs.constants';

const gridMountRef = createRef('orbs_osm_grid1');

export function OrbsOsm(): Component {
  return (
    <>
      <layer static ref={gridMountRef} x={0} y={0} w={0} h={0}>
        <sprite x={0} y={0} w={0} h={0} hide />
      </layer>
      <XpDropsButton left={14} top={0} fromRight={false} />
      <HealthOrb left={3} top={35} fromRight={false} wide clickY={5} clickW={58} clickH={25} />
      <PrayerOrb left={0} top={71} fromRight={false} wide clickY={6} clickW={58} clickH={25} />
      <RunOrb left={10} top={107} fromRight={false} wide clickY={6} clickW={58} clickH={25} />
      <SpecOrb
        left={38}
        top={139}
        fromRight={false}
        panel={OrbsPanel.OSM}
        wide
        clickY={6}
        clickW={58}
        clickH={25}
        subject=""
      />
      <StoreButton panel={OrbsPanel.OSM} filler={false} wholeClick={false} />
      <AdviserButton panel={OrbsPanel.OSM} wholeClick={false} />
      <WorldMapButton panel={OrbsPanel.OSM} spare={false} />
      <WikiButton panel={OrbsPanel.OSM} compact={1} subject={OrbText.WIKI_SUBJECT} />
      <GridMasterButton mount={gridMountRef} script={GridMasterScript.OSM} />
    </>
  );
}
