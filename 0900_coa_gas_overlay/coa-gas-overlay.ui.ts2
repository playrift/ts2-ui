import { createRef } from 'ts2';
import { CoaGasOverlay } from './coa-gas-overlay.component';

const coaGasOverlayRef = createRef('coa_gas_overlay');

export const coaGasOverlayUi = (): UI => (
  <ui ref={coaGasOverlayRef}>
    <CoaGasOverlay />
  </ui>
);
