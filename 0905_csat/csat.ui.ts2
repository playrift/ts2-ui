import { createRef } from 'ts2';
import { CustomerSatisfaction } from './customer-satisfaction.component';

const csatRef = createRef('csat');

export const csatUi = (): UI => (
  <ui ref={csatRef} x="center" y="center">
    <CustomerSatisfaction />
  </ui>
);
