import { createRef } from 'ts2';
import { BankSideLocks } from './bank-side-locks.component';

const bankSideLocksRef = createRef('bank_side_locks');

export const bankSideLocksUi = (): UI => (
  <ui ref={bankSideLocksRef} x="center" y="center">
    <BankSideLocks />
  </ui>
);
