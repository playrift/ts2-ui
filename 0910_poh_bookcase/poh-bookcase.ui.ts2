import { createRef } from 'ts2';
import { PohBookcase } from './poh-bookcase.component';

const pohBookcaseRef = createRef('poh_bookcase');

export const pohBookcaseUi = (): UI => (
  <ui ref={pohBookcaseRef}>
    <PohBookcase />
  </ui>
);
