export { TrophyStuffedForms, isMounted } from './trophy-stuffed-forms.data';

import { Inventories } from '../lib/game/inventories';
import { Sounds as SoundIds } from '../lib/game/sounds';

export declare const TrophySideInv: {
  readonly INV: typeof Inventories.INV;
};

export declare const TrophySounds: {
  readonly CLICK: typeof SoundIds.INTERFACE_SELECT1;
};

export enum Grid {
  W = 36,
  H = 32,
  COLUMNS = 4,
  ORIGIN_X = 16,
  ORIGIN_Y = 8,
  STRIDE_X = 42,
  STRIDE_Y = 36,
  OUTLINE = 1,
  DRAG_DEAD_ZONE = 5,
  DRAG_DEAD_TIME = 6,
}

export enum TrophyColor {
  SLOT_SHADOW = 0x333333,
}

export enum Transparency {
  MOUNTABLE = 0,
  MOUNTABLE_FLASH = 120,
  OTHER = 100,
  OTHER_FLASH = 150,
}

export const MOUNT_OP = 1;

export enum Root {
  W = 190,
  H = 261,
}
