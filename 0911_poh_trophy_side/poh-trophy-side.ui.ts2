import { createRef } from 'ts2';
import { PohTrophySide } from './poh-trophy-side.component';
import { Root } from './poh-trophy-side.constants';

const pohTrophySideRef = createRef('poh_trophy_side');

export const pohTrophySideUi = (): UI => (
  <ui ref={pohTrophySideRef} w={Root.W} h={Root.H} x="center" y="center">
    <PohTrophySide />
  </ui>
);
