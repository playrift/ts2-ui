import { createRef } from 'ts2';
import { PohTrophyMenu } from './poh-trophy-menu.component';

const pohTrophyMenuRef = createRef('poh_trophy_menu');

export const pohTrophyMenuUi = (): UI => (
  <ui ref={pohTrophyMenuRef} x="center" y="center">
    <PohTrophyMenu />
  </ui>
);
