import { createRef } from 'ts2';
import { PopupLoading } from './popuploading.component';

const popupLoadingRef = createRef('popuploading');

export const popupLoadingUi = (): UI => (
  <ui ref={popupLoadingRef}>
    <PopupLoading />
  </ui>
);
