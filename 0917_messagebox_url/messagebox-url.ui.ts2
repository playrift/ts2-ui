import { createRef } from 'ts2';
import { MessageBoxUrl } from './messagebox-url.component';

const messageBoxUrlRef = createRef('messagebox_url');

export const messageBoxUrlUi = (): UI => (
  <ui ref={messageBoxUrlRef} w={479} h={96} x="center" y="center">
    <MessageBoxUrl />
  </ui>
);
