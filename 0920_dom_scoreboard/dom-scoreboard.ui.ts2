import { createRef } from 'ts2';
import { DomScoreboard } from './dom-scoreboard.component';

const domScoreboardRef = createRef('dom_scoreboard');

export const domScoreboardUi = (): UI => (
  <ui ref={domScoreboardRef} x="center" y="center">
    <DomScoreboard />
  </ui>
);
