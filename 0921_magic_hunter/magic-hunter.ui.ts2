import { createRef } from 'ts2';
import { MagicHunter } from './magic-hunter.component';

const magicHunterRef = createRef('magic_hunter');

export const magicHunterUi = (): UI => (
  <ui ref={magicHunterRef}>
    <MagicHunter />
  </ui>
);
