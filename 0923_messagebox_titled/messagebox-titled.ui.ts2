import { createRef } from 'ts2';
import { MessageBoxTitled } from './messagebox-titled.component';

const messageBoxTitledRef = createRef('messagebox_titled');

export const messageBoxTitledUi = (): UI => (
  <ui ref={messageBoxTitledRef}>
    <MessageBoxTitled />
  </ui>
);
