import { createRef } from 'ts2';
import { BingoWelcome } from './bingo-welcome.component';

const bingoWelcomeRef = createRef('bingo_welcome');

export const bingoWelcomeUi = (): UI => (
  <ui ref={bingoWelcomeRef}>
    <BingoWelcome />
  </ui>
);
