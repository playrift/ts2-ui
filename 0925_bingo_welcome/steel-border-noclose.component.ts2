import { SteelborderColor } from '../lib/lib.constants';
import { SteelBorder } from '../lib/steel-border.component';

export function SteelBorderNoClose({ title }: { title: String }): Component {
  return <SteelBorder title={title} titleColor={SteelborderColor.TITLE} flags={1} />;
}
