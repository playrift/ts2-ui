import { createRef } from 'ts2';
import { BingoOverlay } from './bingo-overlay.component';

const bingoOverlayRef = createRef('bingo_overlay');

export const bingoOverlayUi = (): UI => (
  <ui ref={bingoOverlayRef}>
    <BingoOverlay />
  </ui>
);
