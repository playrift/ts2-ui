import { createRef } from 'ts2';
import { EventRewards } from './event-rewards.component';
import { EventFrame } from './event-rewards.constants';

const eventRewardsRef = createRef('event_rewards');

export const eventRewardsUi = (): UI => (
  <ui ref={eventRewardsRef} w={EventFrame.ROOT_W} h={EventFrame.ROOT_H}>
    <EventRewards />
  </ui>
);
