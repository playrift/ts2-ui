import { createRef } from 'ts2';
import { FletchingTable } from './fletching-table.component';

const fletchingTableRef = createRef('fletching_table');

export const fletchingTableUi = (): UI => (
  <ui ref={fletchingTableRef}>
    <FletchingTable />
  </ui>
);
