import { createRef } from 'ts2';
import { SailingBtStatistics } from './sailing-bt-statistics.component';
import { Layout } from './sailing-bt-statistics.constants';

const sailingBtStatisticsRef = createRef('sailing_bt_statistics');

export const sailingBtStatisticsUi = (): UI => (
  <ui ref={sailingBtStatisticsRef} w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <SailingBtStatistics />
  </ui>
);
