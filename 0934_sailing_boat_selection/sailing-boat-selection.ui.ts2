import { createRef } from 'ts2';
import { SailingBoatSelection } from './sailing-boat-selection.component';

const sailingBoatSelectionRef = createRef('sailing_boat_selection');

export const sailingBoatSelectionUi = (): UI => (
  <ui ref={sailingBoatSelectionRef} x="center" y="center">
    <SailingBoatSelection />
  </ui>
);
