import { createRef } from 'ts2';
import { SailingSpyglass } from './sailing-spyglass.component';

const sailingSpyglassRef = createRef('sailing_spyglass');

export const sailingSpyglassUi = (): UI => (
  <ui ref={sailingSpyglassRef}>
    <SailingSpyglass />
  </ui>
);
