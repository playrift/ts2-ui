import { createRef } from 'ts2';
import { SailingCustomisation } from './sailing-customisation.component';

const sailingCustomisationRef = createRef('sailing_customisation');

export const sailingCustomisationUi = (): UI => (
  <ui ref={sailingCustomisationRef} x="center" y="center">
    <SailingCustomisation />
  </ui>
);
