import { createRef } from 'ts2';
import { PortTaskBoard } from './port-task-board.component';

const portTaskBoardRef = createRef('port_task_board');

export const portTaskBoardUi = (): UI => (
  <ui ref={portTaskBoardRef} w="fill" h="fill">
    <PortTaskBoard />
  </ui>
);
