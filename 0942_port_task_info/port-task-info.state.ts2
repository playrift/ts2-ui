import { sessionState } from 'ts2';

export const portTaskInfoTask = sessionState(-1);

export const portTaskInfoPhase = sessionState(-1);

export const portTaskInfoAnswered = sessionState(0);

export const portTaskInfoLarge = sessionState(-1);

export const portTaskInfoSlotW = sessionState(-1);
export const portTaskInfoSlotH = sessionState(-1);
