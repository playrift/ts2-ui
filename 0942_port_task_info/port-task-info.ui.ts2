import { createRef } from 'ts2';
import { PortTaskInfo } from './port-task-info.component';

const portTaskInfoRef = createRef('port_task_info');

export const portTaskInfoUi = (): UI => (
  <ui ref={portTaskInfoRef} w="fill" h="fill">
    <PortTaskInfo />
  </ui>
);
