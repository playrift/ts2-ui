import { Fonts } from '../lib/game/fonts';
import { Inventories } from '../lib/game/inventories';
import { Sounds } from '../lib/game/sounds';
import { Sprites } from '../lib/game/sprites';
import { Varbits } from '../lib/game/varbits';
import { Varps } from '../lib/game/varps';

export declare const Cargo: {
  readonly INV: typeof Inventories.INV;
};

export declare const CargoVarbits: {
  readonly WARNING_DISMISSED: typeof Varbits.SAILING_BOAT_CARGOHOLD_WARNING_DISMISSED;
  readonly DEPOSIT_MODE: typeof Varbits.DEPOSITBOX_MODE;
};

export declare const CargoVarps: {
  readonly WHITELIST: typeof Varps.SAILING_BOAT_CARGOHOLD_SIDE_WHITELIST;
};

export declare const CargoFonts: {
  readonly BODY: typeof Fonts.P12_FULL;
};

export declare const CargoSounds: {
  readonly SELECT: typeof Sounds.INTERFACE_SELECT1;
};

export declare const CargoSprites: {
  readonly BACKING: typeof Sprites.TRADEBACKING_LIGHT;
};

export enum CargoItems {
  BLANK = 6512,
}

export enum CargoColors {
  DIM = 0x808080,
  LABEL = 0xff981f,
  WHITE = 0xffffff,
  SLOT_SHADOW = 0x333333,
}

export enum Grid {
  W = 36,
  H = 32,
  COLUMNS = 4,
  ORIGIN_X = 16,
  ORIGIN_Y = 8,
  STRIDE_X = 42,
  STRIDE_Y = 36,
  DRAG_DEAD_ZONE = 5,
  DRAG_DEAD_TIME = 6,
}

export enum Transparency {
  DIM = 210,
  FLASH = 125,
  CLEAR = 0,
  REFUSED = 150,
  EMPTY = 255,
}

export const FADE_CYCLES = 10;

export const PRESS_CYCLES = 20;

export enum Overlay {
  DIM_OUTER_INSET = 2,
  DIM_INNER_INSET = 4,
  BOX_W = 155,
  BOX_H = 150,
  TEXT_Y = 10,
  TEXT_INSET_X = 20,
  TEXT_INSET_Y = 50,
  BUTTON_W = 100,
  BUTTON_H = 30,
  BUTTON_Y = 10,
}
