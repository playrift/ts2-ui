import { createRef } from 'ts2';
import { Patchy } from './patchy.component';

const patchyRef = createRef('patchy');

export const patchyUi = (): UI => (
  <ui ref={patchyRef}>
    <Patchy />
  </ui>
);
