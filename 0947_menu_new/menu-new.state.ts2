import { sessionState } from 'ts2';

export const menuNewCall = sessionState(0);

export const menuNewKeys = sessionState(0);

export const menuNewIcon = sessionState(-1);

export const menuNewIconW = sessionState(0);
export const menuNewIconH = sessionState(0);

export const menuNewTitle = sessionState('');

export const menuNewTitleW = sessionState(0);

export const menuNewOptions = sessionState('');

export const menuNewOpaque = sessionState(0);
