import { menuNewRef } from './menu-new.constants';
import { MenuNew } from './menu-new.component';

export const menuNewUi = (): UI => (
  <ui ref={menuNewRef} x="center" y="center" w="fill" h="fill">
    <MenuNew />
  </ui>
);
