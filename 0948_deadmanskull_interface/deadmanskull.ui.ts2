import { createRef } from 'ts2';
import { DeadmanSkull } from './deadmanskull.component';

const deadmanSkullRef = createRef('deadmanskull_interface');

export const deadmanSkullUi = (): UI => (
  <ui ref={deadmanSkullRef}>
    <DeadmanSkull />
  </ui>
);
