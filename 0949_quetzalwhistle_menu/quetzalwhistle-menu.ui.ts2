import { createRef } from 'ts2';
import { QuetzalMenu } from './quetzalwhistle-menu.component';

const quetzalWhistleMenuRef = createRef('quetzalwhistle_menu');

export const quetzalWhistleMenuUi = (): UI => (
  <ui ref={quetzalWhistleMenuRef}>
    <QuetzalMenu />
  </ui>
);
