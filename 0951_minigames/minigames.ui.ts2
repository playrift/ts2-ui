import { createRef } from 'ts2';
import { MinigamesPanel } from './minigames.component';

const minigamesRef = createRef('minigames');

export const minigamesUi = (): UI => (
  <ui ref={minigamesRef}>
    <MinigamesPanel />
  </ui>
);
