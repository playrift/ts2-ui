import { createRef } from 'ts2';
import { CanoeMapLum } from './canoe-map-lum.component';
import { Layout } from './canoe-map-lum.constants';

const canoeMapLumRef = createRef('canoe_map_lum');

export const canoeMapLumUi = (): UI => (
  <ui ref={canoeMapLumRef} w={Layout.ROOT_W} h={Layout.ROOT_H}>
    <CanoeMapLum />
  </ui>
);
