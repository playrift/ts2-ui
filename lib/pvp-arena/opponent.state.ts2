import { sessionState } from 'ts2';

export const wornWarnings = sessionState(0);

export const loadoutStatusCode = sessionState(0);

export enum LoadoutStatus {
  SAME = 0,
  STATS = 1,
  TOO_MANY = 2,
  AMMO = 3,
  MISMATCH = 4,
}
